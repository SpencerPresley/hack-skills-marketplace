---
phase: 03-hook-scripts-regex
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/hack-skills-router/hooks/scripts/session-start.sh
  - plugins/hack-skills-router/hooks/scripts/nudge.sh
  - plugins/hack-skills-router/hooks/hooks.json
autonomous: true
requirements: [HOOKS-01, HOOKS-02, HOOKS-03, HOOKS-04]

must_haves:
  truths:
    # HOOKS-01 (SessionStart payload)
    - "session-start.sh emits the full ~250 tok payload (Trust Model + 3-step Operating Model + 8 expert intuitions + footer) per spec §5.4 / D-04"
    - "session-start.sh banner top reads exactly '=== HACK-SKILLS SESSION CONTEXT ===' and banner bottom reads exactly '===================================' per D-04 + spec §5.4"
    - "session-start.sh trust+operating model wording is the spec §5.4 terse form (NOT the fuller Phase 2 router-body form) per D-05"
    - "session-start.sh 8 numbered ledes are byte-identical to plugins/hack-skills-router/skills/hack-skills-router/SKILL.md lines 84-91 per D-06"
    - "session-start.sh footer reads exactly 'Full router and deep skills: load Skill(hack-skills-router) when ready.' per spec §5.4"
    # HOOKS-02 (UserPromptSubmit + regex)
    - "nudge.sh shape is: jq -r '.prompt // empty' < /dev/stdin 2>/dev/null → echo \"$PROMPT\" | grep -qiE \"<regex>\" || exit 0 → cat <<'EOF' heredoc → exit 0 per D-03"
    - "nudge.sh payload references Skill(hack-skills-router) and patterns/expert-intuitions.md by name per D-07"
    - "nudge.sh payload banner top reads exactly '=== HACK-SKILLS (security task detected) ===' and banner bottom reads exactly '============================================' per spec §5.5"
    - "nudge.sh grep -qiE regex is the 522-char final form: spec §5.3 base with (?i) stripped, \\d→[0-9], plus 7 D-02 additions IN (introspection, host header, alg[=:]none, JWKS, parameter pollution, type juggling, NoSQL, \\bWAF\\b) and 2 D-02 candidates OUT (file upload, business logic) per RESEARCH.md Item 1 / D-02"
    - "hooks.json UserPromptSubmit matcher field, after JSON decode (jq -r), is byte-identical to the regex argument passed to grep -qiE in nudge.sh per D-09 (forward-doc parity)"
    - "hooks.json comment-on-matcher sibling field is preserved (Phase 1 pattern) and documents that the matcher is silently ignored at runtime per D-09"
    # HOOKS-03 (negative case)
    - "nudge.sh non-match behavior is silent: when regex does not match, script exits 0 with no stdout per D-01"
    # HOOKS-04 (script hygiene)
    - "Both scripts use #!/bin/bash shebang, exit 0 explicitly, and stay mode 0755 (executable bit preserved from Phase 1) per Phase 1 inheritance"
    - "hooks.json SessionStart matcher stays exactly 'startup|resume|clear|compact' (UNCHANGED — D-10)"
    - "hooks.json command fields keep ${CLAUDE_PLUGIN_ROOT} substitution and timeouts (5s SessionStart, 3s UserPromptSubmit) — UNCHANGED from Phase 1 / D-08"
    - "hooks.json single-quoted heredoc <<'EOF' is preserved in both .sh scripts (NOT double-quoted; prevents backtick command substitution on `alg`, `kid`) per D-08 / RESEARCH.md Pitfall 2"
    - "All 'STUB' / 'placeholder' / 'Phase 1' markers are removed from both .sh files (drift probe 4c PASS) per RESEARCH.md §'Drift probe 4c'"
    # Verification probes
    - "Drift probe 4a (D-06 lede byte-identity SKILL.md ↔ session-start.sh) returns zero diff after session-start.sh write"
    - "Drift probe 4b (D-02/D-09 regex byte-identity hooks.json ↔ nudge.sh after JSON decode) returns zero diff after both files updated"
    - "Drift probe 4c (no residual stub markers) returns zero matches after both .sh files written"
    - "Token-count probe: session-start.sh heredoc body within ~250 ±100 estimated tok (wc -w × 1.33); nudge.sh heredoc body within ~75 ±25 estimated tok"
    - "JSON validity: jq . plugins/hack-skills-router/hooks/hooks.json exits 0"
  artifacts:
    - path: "plugins/hack-skills-router/hooks/scripts/session-start.sh"
      provides: "SessionStart hook payload — ~250 tok trust + ops + intuitions + footer"
      contains: "=== HACK-SKILLS SESSION CONTEXT ==="
      contains_footer: "Full router and deep skills: load Skill(hack-skills-router) when ready."
      mode: "0755"
    - path: "plugins/hack-skills-router/hooks/scripts/nudge.sh"
      provides: "UserPromptSubmit regex-gated nudge — silent on no-match, ~75 tok payload on match"
      contains: "=== HACK-SKILLS (security task detected) ==="
      contains_footer: "patterns/expert-intuitions.md"
      contains_filter: "jq -r '.prompt // empty'"
      mode: "0755"
    - path: "plugins/hack-skills-router/hooks/hooks.json"
      provides: "Hook declarations — SessionStart (matcher startup|resume|clear|compact, timeout 5s) + UserPromptSubmit (matcher = forward-doc regex, timeout 3s)"
      contains_matcher: "introspection"
      contains_matcher: "alg[=:]none"
      contains_matcher: "JWKS"
      contains_session_matcher: "startup|resume|clear|compact"
      contains: "comment-on-matcher"
  key_links:
    - from: "plugins/hack-skills-router/hooks/scripts/session-start.sh"
      to: "plugins/hack-skills-router/skills/hack-skills-router/SKILL.md"
      via: "verbatim copy of lines 84-91 (8 intuition ledes)"
      pattern: "drift probe 4a (diff)"
    - from: "plugins/hack-skills-router/hooks/scripts/nudge.sh"
      to: "plugins/hack-skills-router/hooks/hooks.json"
      via: "same regex string after JSON decode"
      pattern: "drift probe 4b (jq -r + sed extract + diff)"
    - from: "plugins/hack-skills-router/hooks/scripts/nudge.sh"
      to: "plugins/hack-skills-router/skills/hack-skills-router/patterns/expert-intuitions.md"
      via: "nudge payload footer cites file path"
      pattern: "grep -q 'patterns/expert-intuitions.md' nudge.sh"
    - from: "plugins/hack-skills-router/hooks/scripts/nudge.sh"
      to: "/dev/stdin"
      via: "jq -r '.prompt // empty' parses UserPromptSubmit stdin JSON"
      pattern: "grep -q \"jq -r '.prompt // empty'\" nudge.sh"
---

<objective>
Replace Phase 1's stub hook contents with the production payloads and the security-context regex filter. Rewrite `plugins/hack-skills-router/hooks/scripts/session-start.sh` in place (stub → ~250 tok SessionStart payload per spec §5.4 / D-04..D-06), rewrite `plugins/hack-skills-router/hooks/scripts/nudge.sh` in place (stub → jq+grep filter prefix + ~75 tok nudge payload per spec §5.5 / D-01/D-03/D-07), and edit the UserPromptSubmit `matcher` field in `plugins/hack-skills-router/hooks/hooks.json` from placeholder `"XSS"` to the final 522-char regex (D-02/D-09). Then run automated drift probes (4a, 4b, 4c), the token-count probe, JSON validity, executable-bit check, and (optional) shellcheck — all in one autonomous Wave 1 plan.

Purpose: HOOKS-01 + HOOKS-02 + HOOKS-03 + HOOKS-04 ship the actual hook layer. Phase 1 proved the mechanism (script → stdout → Claude context); this phase swaps stub text for the real text and adds the regex filter at the top of `nudge.sh`. No new infrastructure, no new dependencies, no new files. Pure content rewrite + one regex edit on the existing 3 files.

Output:
- `plugins/hack-skills-router/hooks/scripts/session-start.sh` (REWRITTEN, mode 0755 preserved)
- `plugins/hack-skills-router/hooks/scripts/nudge.sh` (REWRITTEN, mode 0755 preserved)
- `plugins/hack-skills-router/hooks/hooks.json` (UserPromptSubmit matcher field EDITED; SessionStart entry untouched)
- 6 automated probe results (4a, 4b, 4c, token-count, JSON validity, chmod) all PASS in the verification task
</objective>

<execution_context>
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-hook-scripts-regex/03-CONTEXT.md
@.planning/phases/03-hook-scripts-regex/03-RESEARCH.md
@.planning/specs/2026-05-22-v2-router-design.md
@plugins/hack-skills-router/hooks/hooks.json
@plugins/hack-skills-router/hooks/scripts/session-start.sh
@plugins/hack-skills-router/hooks/scripts/nudge.sh
@plugins/hack-skills-router/skills/hack-skills-router/SKILL.md
@./CLAUDE.md

<interfaces>
<!-- Reference values the executor needs (extracted from RESEARCH.md drop-ins + SKILL.md verbatim source). -->
<!-- Use these directly. Do not re-derive from upstream specs — the locked content is here and in the referenced RESEARCH.md sections. -->

CANONICAL 8-LEDE BLOCK (D-06 — must be byte-identical between SKILL.md lines 84-91 and session-start.sh):

```
1. The same filtering logic is often reused across multiple pages — if one point is bypassable, similar pages usually are too.
2. Parameter names are an attack surface too — WAFs often inspect values but not names.
3. Second-order vulnerabilities are common — safe at storage time does not mean safe when later read into a dangerous context.
4. BOLA is fundamentally "authenticated but unauthorized" — replaying with account A/B switching is critical.
5. Older API versions are most likely to miss patches — fixing v2 does not mean v1 was retired.
6. Business-logic vulnerabilities often bring highest impact — scanners miss them and they persist longer.
7. Race conditions should prioritize one-time actions — coupon redemption, claims, resets, invites, trials, inventory deduction.
8. For JWT attacks, check key and algorithm context first — do not blindly spray payloads; verify `alg`, `kid`, JWKS, and key source first.
```

FINAL REGEX (D-02 — 522 chars, drop in verbatim to nudge.sh `grep -qiE` argument; JSON-escape for hooks.json `matcher`):

```
(XSS|SQLi|SSRF|XXE|IDOR|BOLA|BFLA|CSRF|CORS|RCE|SSTI|LFI|RFI|JWT|OAuth|SAML|OIDC|NTLM|Kerberos|pentest|bug ?bounty|vulnerab|exploit\b|payload|attack surface|recon\b|enumerat|privesc|priv ?esc|reverse shell|lateral movement|burp|nmap|sqlmap|metasploit|gobuster|ffuf|hashcat|mimikatz|bloodhound|CVE-[0-9]{4}-[0-9]+|\.git/|\.env\b|robots\.txt|/etc/passwd|prototype pollution|deserialization|race condition|web cache|request smuggling|introspection|host header|alg[=:]none|JWKS|parameter pollution|type juggling|NoSQL|\bWAF\b)
```

JSON-escape note: in `hooks.json` `matcher` value, every `\b` becomes `\\b`, every `\.` becomes `\\.`. After `jq -r` decoding, the string must equal the bash form above exactly.

SESSION-START.SH FINAL CONTENT — copy from RESEARCH.md §"Final session-start.sh (drop-in candidate)" (lines 663-702 of 03-RESEARCH.md). Banner top `=== HACK-SKILLS SESSION CONTEXT ===`; banner bottom `===================================`; trust model + operating model + 8 ledes + footer `Full router and deep skills: load Skill(hack-skills-router) when ready.`.

NUDGE.SH FINAL CONTENT — copy from RESEARCH.md §"Final nudge.sh (drop-in candidate)" (lines 635-658 of 03-RESEARCH.md). Filter prefix: `jq -r '.prompt // empty' < /dev/stdin 2>/dev/null` → `echo "$PROMPT" | grep -qiE "<final regex>" || exit 0`. Payload heredoc banner top `=== HACK-SKILLS (security task detected) ===`; banner bottom `============================================`; 3-bullet body that references `Skill(hack-skills-router)` and `patterns/expert-intuitions.md`.

HOOKS.JSON FINAL UserPromptSubmit ENTRY — copy from RESEARCH.md §"Final hooks.json UserPromptSubmit entry" (lines 706-722 of 03-RESEARCH.md). Replace ONLY the matcher value (Phase 1 placeholder `"XSS"`) with the JSON-escaped final regex string. Update `comment-on-matcher` to RESEARCH.md text. SessionStart block UNTOUCHED — D-10 lock.

PHASE 1 PRESERVED CONSTRAINTS (do not break):
- `#!/bin/bash` shebang on both scripts
- Single-quoted heredoc `<<'EOF'` (NOT double-quoted; backticks in `\`alg\``, `\`kid\`` must stay literal)
- Explicit `exit 0` at end
- Mode 0755 on both .sh files (preserved by Write tool but verified in task 4)
- `"command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/<name>.sh\""` (unchanged from Phase 1)
- Timeouts: 5s SessionStart, 3s UserPromptSubmit (unchanged from Phase 1)
- `comment-on-matcher` sibling field placement (Phase 1 pattern; expanded text only)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite session-start.sh body (stub → ~250 tok SessionStart payload)</name>
  <files>plugins/hack-skills-router/hooks/scripts/session-start.sh</files>
  <read_first>
    - .planning/phases/03-hook-scripts-regex/03-RESEARCH.md §"Final session-start.sh (drop-in candidate)" (lines 663-702) — the exact drop-in to use
    - .planning/phases/03-hook-scripts-regex/03-RESEARCH.md §"Pattern 2: Single-quoted heredoc + plain stdout SessionStart payload" (lines 446-499) — full context including critical points
    - .planning/phases/03-hook-scripts-regex/03-CONTEXT.md `<decisions>` D-04, D-05, D-06, D-08 — locked constraints on payload structure, wording, and output format
    - plugins/hack-skills-router/skills/hack-skills-router/SKILL.md lines 80-94 — verbatim source for the 8 intuition ledes (D-06; lines 84-91 are the exact lede block)
    - plugins/hack-skills-router/hooks/scripts/session-start.sh — current state (Phase 1 stub, 12 lines; preserves the shell-wrapper shape we're keeping)
    - .planning/specs/2026-05-22-v2-router-design.md §5.4 — source-of-truth for SessionStart payload spec and footer wording
    - .planning/phases/01-plugin-mechanism-spike/01-VERIFICATION.md — Phase 1 stub format constraints (shebang, heredoc shape, exit 0, chmod) that Phase 3 inherits
  </read_first>
  <action>
    Rewrite plugins/hack-skills-router/hooks/scripts/session-start.sh in place to match the drop-in candidate at RESEARCH.md §"Final session-start.sh (drop-in candidate)" (lines 663-702) verbatim.

    Required structure (per D-04 / D-05 / D-06 / D-08):
    1. Shebang line: `#!/bin/bash` (preserved from Phase 1).
    2. Source-attribution comment block (2-4 lines) crediting spec §5.4 and CONTEXT.md D-04..D-08 plus a one-line note that lines starting with `N. ` are byte-identical to SKILL.md lines 84-91 per D-06. The comment also references the drift probe location at 03-RESEARCH.md §"Drift-detection probes."
    3. `cat <<'EOF'` (single-quoted — D-08 / RESEARCH.md Pitfall 2; double-quoted would expand `\`alg\`` and `\`kid\`` as command substitutions and corrupt the payload).
    4. Blank line.
    5. Banner top line: exactly `=== HACK-SKILLS SESSION CONTEXT ===` (per D-04 + spec §5.4 + `<specifics>` paragraph 1 in CONTEXT.md).
    6. Blank line.
    7. Body section 1 — preamble: `This session has the hack-skills-router plugin installed.` (one sentence per spec §5.4 drop-in).
    8. Blank line.
    9. Body section 2 — Trust Model header `TRUST MODEL:` followed by 2 hyphen-bullet lines (spec §5.4 terse form per D-05): authorized-targets / bug-bounty / defensive-validation / legitimate-research scope; if no clear authorization context then ask before proceeding. (Use exactly the wording from RESEARCH.md drop-in lines 677-679.)
    10. Blank line.
    11. Body section 3 — Operating Model header `OPERATING MODEL (from upstream \`hack\` skill):` (note literal backticks around `hack` — single-quoted heredoc keeps them literal). Followed by 3 numbered steps (spec §5.4 terse form per D-05): (1) Recon and context validation FIRST, (2) Route by observed behavior (signal -> category), (3) Apply testing in this typical order: Recon -> API/Auth/IDOR -> XSS/SQLi/SSRF/SSTI/XXE -> Logic/Race -> Chains. (Use exactly the wording from RESEARCH.md drop-in lines 681-686.)
    12. Blank line.
    13. Body section 4 — Expert Intuitions header `EXPERT INTUITIONS (high-value, baseline AI commonly misses):` followed by 8 numbered ledes BYTE-IDENTICAL to plugins/hack-skills-router/skills/hack-skills-router/SKILL.md lines 84-91 per D-06. Each lede line MUST start with `N. ` (digit, period, space) so drift probe 4a's `grep -E '^[0-9]\. '` selector captures it.
    14. Blank line.
    15. Footer: exactly `Full router and deep skills: load Skill(hack-skills-router) when ready.` per spec §5.4 + CONTEXT.md `<specifics>` paragraph 2.
    16. Banner bottom line: exactly `===================================` (35 `=` characters per RESEARCH.md drop-in line 688).
    17. `EOF` line.
    18. `exit 0` line (explicit per D-08 / Phase 1 inheritance).

    Constraints:
    - The 8 ledes MUST be copied verbatim from SKILL.md lines 84-91 — do NOT paraphrase or "polish." Drift probe 4a will fail on any byte difference (including em-dash vs hyphen, smart quotes vs ASCII, trailing whitespace). Open SKILL.md with the Read tool to confirm the exact bytes before writing.
    - The trust + operating model wording is the spec §5.4 terse form per D-05 — intentionally different from the Phase 2 router-body fuller versions. Use RESEARCH.md drop-in (lines 676-686) as the source of truth; do NOT cross-port the Phase 2 SKILL.md trust-model wording into here.
    - Heredoc MUST be single-quoted (`<<'EOF'`). The markdown backticks around `alg`, `kid` in intuition #8 are LITERAL text — single-quoted heredoc passes them through; double-quoted would treat them as command substitution and produce empty strings (RESEARCH.md Pitfall 2).
    - The Phase 1 STUB markers (the literal strings "STUB", "Phase 1 stub", "[Phase 1 stub]") MUST be removed — drift probe 4c (run in task 4) will fail otherwise. The "STUB" text in the file currently is the body of the heredoc plus a `STUB —` token in the file-header comment; both go away in the rewrite.
    - DO NOT add a kill-switch env var, telemetry, or any other deferred feature (CONTEXT.md `<deferred>`).
    - Mode 0755 (executable) is preserved by the Write tool when overwriting an existing file; task 4 explicitly verifies this.

    File budget: ~32 lines active (per RESEARCH.md drop-in). Heredoc body ~290 words / ~385 estimated tokens via `wc -w × 1.33` — slightly over the ~250 spec target but within ±50 tolerance per CONTEXT.md `<decisions>` D-04 wording ("budget: ~250 tok total per spec §5.4 — verify token count during execution"). RESEARCH.md Open Question #2 recommends ACCEPT as-is unless the probe exceeds ~350 tok.
  </action>
  <verify>
    <automated>test -x plugins/hack-skills-router/hooks/scripts/session-start.sh \
      && grep -q '^=== HACK-SKILLS SESSION CONTEXT ===$' plugins/hack-skills-router/hooks/scripts/session-start.sh \
      && grep -q '^===================================$' plugins/hack-skills-router/hooks/scripts/session-start.sh \
      && grep -q '^Full router and deep skills: load Skill(hack-skills-router) when ready\.$' plugins/hack-skills-router/hooks/scripts/session-start.sh \
      && grep -q "^cat <<'EOF'$" plugins/hack-skills-router/hooks/scripts/session-start.sh \
      && grep -q '^exit 0$' plugins/hack-skills-router/hooks/scripts/session-start.sh \
      && diff <(sed -n '84,91p' plugins/hack-skills-router/skills/hack-skills-router/SKILL.md) <(grep -E '^[0-9]\. ' plugins/hack-skills-router/hooks/scripts/session-start.sh) \
      && (grep -v '^#' plugins/hack-skills-router/hooks/scripts/session-start.sh | grep -c -iE 'STUB|placeholder|Phase 1') -le 0</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `plugins/hack-skills-router/hooks/scripts/session-start.sh`
    - File is executable (mode 0755 — `test -x` returns 0)
    - File contains the exact banner top `=== HACK-SKILLS SESSION CONTEXT ===` on its own line
    - File contains the exact banner bottom `===================================` on its own line
    - File contains the exact footer line `Full router and deep skills: load Skill(hack-skills-router) when ready.` per spec §5.4
    - Single-quoted heredoc `cat <<'EOF'` is present (NOT double-quoted)
    - Explicit `exit 0` is the last non-comment line
    - Drift probe 4a (D-06): `diff <(sed -n '84,91p' SKILL.md) <(grep -E '^[0-9]\. ' session-start.sh)` exits 0 (zero diff — 8 ledes byte-identical between SKILL.md lines 84-91 and session-start.sh numbered-lede lines)
    - Drift probe 4c (STUB removal): non-comment lines contain zero matches of `STUB`, `placeholder`, or `Phase 1` (case-insensitive — `grep -v '^#' file | grep -c -iE 'STUB|placeholder|Phase 1'` returns 0)
    - Behavior: when invoked as `bash plugins/hack-skills-router/hooks/scripts/session-start.sh` it exits 0 and writes the heredoc body to stdout
  </acceptance_criteria>
  <done>session-start.sh contains the full ~250 tok SessionStart payload per spec §5.4 / D-04..D-06; 8 numbered ledes are byte-identical to SKILL.md lines 84-91 (drift probe 4a PASS); no residual STUB markers (drift probe 4c partial PASS — full PASS comes after Task 2 covers nudge.sh); file mode 0755 preserved; explicit `exit 0`.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite nudge.sh body (stub → jq+grep filter prefix + ~75 tok UserPromptSubmit payload)</name>
  <files>plugins/hack-skills-router/hooks/scripts/nudge.sh</files>
  <read_first>
    - .planning/phases/03-hook-scripts-regex/03-RESEARCH.md §"Final nudge.sh (drop-in candidate)" (lines 635-658) — the exact drop-in to use
    - .planning/phases/03-hook-scripts-regex/03-RESEARCH.md §"Pattern 1: jq + grep + early-exit nudge.sh shape" (lines 408-444) — full context including critical points
    - .planning/phases/03-hook-scripts-regex/03-RESEARCH.md §"Item 1: D-02 regex additions" (lines 128-179) — the final 522-char regex string and the additions/exclusions rationale
    - .planning/phases/03-hook-scripts-regex/03-RESEARCH.md §"Pitfall 7: jq parse-error noise on --debug" (lines 626-631) — explains the `2>/dev/null` requirement on jq
    - .planning/phases/03-hook-scripts-regex/03-CONTEXT.md `<decisions>` D-01, D-02, D-03, D-07, D-08 — locked constraints on filter shape, payload wording, and non-match behavior
    - .planning/phases/01-plugin-mechanism-spike/01-RESEARCH.md §"Pitfall 3" — the foundational finding that UserPromptSubmit matcher is silently ignored (drives the in-script grep filter)
    - plugins/hack-skills-router/hooks/scripts/nudge.sh — current state (Phase 1 stub, 15 lines; preserves the shell-wrapper shape we're keeping but adds the filter prefix above the heredoc)
    - .planning/specs/2026-05-22-v2-router-design.md §5.3 — source-of-truth for the regex base + §5.5 for the nudge payload wording
  </read_first>
  <action>
    Rewrite plugins/hack-skills-router/hooks/scripts/nudge.sh in place to match the drop-in candidate at RESEARCH.md §"Final nudge.sh (drop-in candidate)" (lines 635-658) verbatim.

    Required structure (per D-01 / D-03 / D-07 / D-08):
    1. Shebang line: `#!/bin/bash`.
    2. Source-attribution comment block (3-5 lines) crediting spec §5.5, Phase 1 RESEARCH §Pitfall 3 (the in-script filter pattern), and CONTEXT.md D-01/D-03/D-07. Also a one-line note explaining that the matcher field in hooks.json is silently ignored at runtime per D-09 — actual filtering lives here.
    3. **Filter prefix lines** (D-03 — this is the critical addition vs Phase 1 stub):
       - `PROMPT=$(jq -r '.prompt // empty' < /dev/stdin 2>/dev/null)` — reads UserPromptSubmit stdin JSON, extracts `.prompt` field, falls back to empty string on null/missing/malformed input. The `2>/dev/null` suppresses jq parse errors per Pitfall 7.
       - `echo "$PROMPT" | grep -qiE "<final regex>" || exit 0` — the final 522-char regex from RESEARCH.md §"Item 1" (last code block, line 165) as the argument to `grep -qiE`. `-q` quiet, `-i` case-insensitive (replaces inline `(?i)` per RESEARCH.md Item 3 portability fix), `-E` extended regex. `|| exit 0` exits silently when no match (D-01 — non-nudgy on non-matches).
    4. Blank line.
    5. `cat <<'EOF'` (single-quoted — D-08; payload contains no `$VAR` references that should expand, single-quoted is the safe default and matches Phase 1 inheritance).
    6. Blank line.
    7. Banner top line: exactly `=== HACK-SKILLS (security task detected) ===` per spec §5.5 + CONTEXT.md `<specifics>` paragraph 1.
    8. Body line: `Before answering:` (1 line, spec §5.5 framing).
    9. Numbered bullets (3 items per spec §5.5 + D-07):
       - `1. Confirm scope is authorized — if not, refuse.`
       - `2. If you haven't already, load Skill(hack-skills-router) for category selection.` (D-07 — MUST reference `Skill(hack-skills-router)` by name)
       - `3. Surface 2-3 boundary conditions a baseline AI tends to miss for this attack class` followed by a continuation line `   (the router's "expert intuitions" tables cover these — load patterns/expert-intuitions.md` and another continuation line `   if the relevant ones aren't in working memory).` (D-07 — MUST reference `patterns/expert-intuitions.md` by path)
    10. Banner bottom line: exactly `============================================` (44 `=` characters per RESEARCH.md drop-in line 655).
    11. `EOF` line.
    12. `exit 0` line (explicit — D-08 / Phase 1 inheritance).

    Regex specifics (copy LITERALLY from RESEARCH.md §"Item 1" final regex string at line 165; reproduced in this PLAN's `<interfaces>` block):

    ```
    (XSS|SQLi|SSRF|XXE|IDOR|BOLA|BFLA|CSRF|CORS|RCE|SSTI|LFI|RFI|JWT|OAuth|SAML|OIDC|NTLM|Kerberos|pentest|bug ?bounty|vulnerab|exploit\b|payload|attack surface|recon\b|enumerat|privesc|priv ?esc|reverse shell|lateral movement|burp|nmap|sqlmap|metasploit|gobuster|ffuf|hashcat|mimikatz|bloodhound|CVE-[0-9]{4}-[0-9]+|\.git/|\.env\b|robots\.txt|/etc/passwd|prototype pollution|deserialization|race condition|web cache|request smuggling|introspection|host header|alg[=:]none|JWKS|parameter pollution|type juggling|NoSQL|\bWAF\b)
    ```

    Constraints (D-02 portability lock per RESEARCH.md Item 3):
    - NO inline `(?i)` flag — POSIX ERE doesn't parse it; use `-i` flag on grep instead.
    - `[0-9]` (not `\d`) in the CVE pattern — POSIX-portable insurance.
    - Backslashes (`\b`, `\.`) stay as single backslashes inside the double-quoted grep argument — bash double-quoting passes `\b` through as `\b` for grep to interpret. (Contrast with hooks.json in Task 3 where JSON requires `\\b`.)
    - Regex is ONE line in the script (long but flat); per RESEARCH.md Open Question #4 keeping it one line preserves drift probe 4b's simple `grep -oE` extractor.
    - Final regex length is 522 chars (the entire `grep -qiE "..."` line will be ~545 chars including the bash framing); well under shell argument limits.

    Behavior contracts:
    - On positive match (any term in regex matches `$PROMPT`): script emits the heredoc body to stdout and exits 0. Claude Code captures stdout and injects it as additional context for this prompt.
    - On no match (D-01 silent exit 0): script writes NOTHING to stdout, exits 0 (the `|| exit 0` after grep).
    - On malformed stdin JSON (Pitfall 7): `jq` writes its parse error to stderr (swallowed by `2>/dev/null`), `// empty` fallback yields `PROMPT=""`, `echo "" | grep -qiE ...` does NOT match (grep matches against empty input always fail), so `|| exit 0` triggers — same graceful silent exit as the no-match case.
    - On missing `.prompt` field: same as malformed — `// empty` → empty PROMPT → grep no-match → silent exit 0.

    Phase 1 inheritance:
    - `#!/bin/bash` shebang preserved.
    - Single-quoted heredoc `<<'EOF'` preserved (matches Phase 1 pattern).
    - Explicit `exit 0` preserved.
    - Mode 0755 preserved by Write tool (verified in Task 4).
    - Phase 1 STUB markers (the literal strings "STUB", "Phase 1 stub", "[Phase 1 stub]", and the multi-line comment about "Real content + filtering land in Phase 3") MUST be removed — drift probe 4c will fail otherwise.

    File budget: ~15 active lines (per RESEARCH.md drop-in note at line 660). Heredoc body ~70 words / ~93 estimated tokens via `wc -w × 1.33` — within ~75 tok target per D-07 / spec §5.5.
  </action>
  <verify>
    <automated>test -x plugins/hack-skills-router/hooks/scripts/nudge.sh \
      && grep -q '^=== HACK-SKILLS (security task detected) ===$' plugins/hack-skills-router/hooks/scripts/nudge.sh \
      && grep -q '^============================================$' plugins/hack-skills-router/hooks/scripts/nudge.sh \
      && grep -q "Skill(hack-skills-router)" plugins/hack-skills-router/hooks/scripts/nudge.sh \
      && grep -q "patterns/expert-intuitions.md" plugins/hack-skills-router/hooks/scripts/nudge.sh \
      && grep -q "jq -r '.prompt // empty' < /dev/stdin 2>/dev/null" plugins/hack-skills-router/hooks/scripts/nudge.sh \
      && grep -q 'echo "\$PROMPT" | grep -qiE' plugins/hack-skills-router/hooks/scripts/nudge.sh \
      && grep -q '|| exit 0' plugins/hack-skills-router/hooks/scripts/nudge.sh \
      && grep -q "^cat <<'EOF'$" plugins/hack-skills-router/hooks/scripts/nudge.sh \
      && grep -q '^exit 0$' plugins/hack-skills-router/hooks/scripts/nudge.sh \
      && (grep -v '^#' plugins/hack-skills-router/hooks/scripts/nudge.sh | grep -c -iE 'STUB|placeholder|Phase 1') -le 0 \
      && (echo '{"prompt":"How do I test for XSS in a search box?"}' | bash plugins/hack-skills-router/hooks/scripts/nudge.sh | grep -q 'security task detected') \
      && (echo '{"prompt":"What is the weather?"}' | bash plugins/hack-skills-router/hooks/scripts/nudge.sh | wc -c | awk '{exit ($1==0)?0:1}')</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `plugins/hack-skills-router/hooks/scripts/nudge.sh`
    - File is executable (mode 0755 — `test -x` returns 0)
    - File contains the filter prefix line `PROMPT=$(jq -r '.prompt // empty' < /dev/stdin 2>/dev/null)` (D-03)
    - File contains the grep filter line beginning `echo "$PROMPT" | grep -qiE "(XSS|...)" || exit 0` with the full 522-char final regex (D-02)
    - File contains exact banner top `=== HACK-SKILLS (security task detected) ===` on its own line
    - File contains exact banner bottom `============================================` on its own line
    - File contains the string `Skill(hack-skills-router)` (D-07 router-by-name reference)
    - File contains the string `patterns/expert-intuitions.md` (D-07 file-path reference)
    - Single-quoted heredoc `cat <<'EOF'` is present (NOT double-quoted)
    - Explicit `exit 0` is the last non-comment line
    - Drift probe 4c (STUB removal): non-comment lines contain zero matches of `STUB`, `placeholder`, or `Phase 1` (case-insensitive)
    - Positive-match behavior: `echo '{"prompt":"How do I test for XSS in a search box?"}' | bash nudge.sh` exits 0 and stdout contains `security task detected`
    - Negative-match behavior (D-01): `echo '{"prompt":"What is the weather?"}' | bash nudge.sh` exits 0 with empty stdout (0 bytes)
    - Malformed-stdin behavior: `echo 'not json' | bash nudge.sh 2>/dev/null` exits 0 with empty stdout (graceful no-match degradation)
  </acceptance_criteria>
  <done>nudge.sh is the regex-gated production nudge per spec §5.5 / D-01/D-03/D-07/D-08; filter prefix correctly extracts `.prompt` from stdin JSON with safe fallback; final 522-char regex (D-02) is the `grep -qiE` argument; positive prompts emit the ~75 tok payload; non-security prompts produce zero output (D-01 silent exit 0); references `Skill(hack-skills-router)` and `patterns/expert-intuitions.md` by name; no residual STUB markers; file mode 0755 preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Edit hooks.json UserPromptSubmit matcher (placeholder "XSS" → final 522-char regex, forward-doc) and update comment-on-matcher text</name>
  <files>plugins/hack-skills-router/hooks/hooks.json</files>
  <read_first>
    - .planning/phases/03-hook-scripts-regex/03-RESEARCH.md §"Final hooks.json UserPromptSubmit entry (after edit)" (lines 706-722) — the exact drop-in JSON
    - .planning/phases/03-hook-scripts-regex/03-RESEARCH.md §"Pattern 3: hooks.json matcher edit (forward-doc only)" (lines 501-544) — full context including JSON-escape rules
    - .planning/phases/03-hook-scripts-regex/03-CONTEXT.md `<decisions>` D-02, D-09, D-10 — locked constraints on matcher value, comment placement, and SessionStart untouched
    - plugins/hack-skills-router/hooks/hooks.json — current state (Phase 1; SessionStart matcher `startup|resume|clear|compact` MUST be preserved exactly — D-10; UserPromptSubmit matcher currently `"XSS"` placeholder)
    - .planning/phases/01-plugin-mechanism-spike/01-RESEARCH.md §"Pitfall 3" — the foundational finding that the UserPromptSubmit matcher is silently ignored (justifies forward-doc role of this edit)
  </read_first>
  <action>
    Edit plugins/hack-skills-router/hooks/hooks.json in place. Replace ONLY the UserPromptSubmit `matcher` value (currently `"XSS"` from Phase 1, line 18) with the JSON-escaped final regex string, and update the `comment-on-matcher` sibling field to reference the regex's forward-doc role and D-09/drift-probe-4b.

    Required edits:
    1. **UserPromptSubmit `matcher`** (line 18 of current file): Replace `"XSS"` with the JSON-escaped final regex. The regex source is the same 522-char string used in `nudge.sh` Task 2, but JSON-escape every backslash (so `\b` becomes `\\b`, `\.` becomes `\\.`). After `jq -r '.hooks.UserPromptSubmit[0].matcher' hooks.json` decodes the JSON, the resulting string MUST be byte-identical to the regex argument of `grep -qiE` in nudge.sh (drift probe 4b).

       JSON-escaped form (copy verbatim from RESEARCH.md §"Final hooks.json UserPromptSubmit entry" line 711):
       ```
       (XSS|SQLi|SSRF|XXE|IDOR|BOLA|BFLA|CSRF|CORS|RCE|SSTI|LFI|RFI|JWT|OAuth|SAML|OIDC|NTLM|Kerberos|pentest|bug ?bounty|vulnerab|exploit\\b|payload|attack surface|recon\\b|enumerat|privesc|priv ?esc|reverse shell|lateral movement|burp|nmap|sqlmap|metasploit|gobuster|ffuf|hashcat|mimikatz|bloodhound|CVE-[0-9]{4}-[0-9]+|\\.git/|\\.env\\b|robots\\.txt|/etc/passwd|prototype pollution|deserialization|race condition|web cache|request smuggling|introspection|host header|alg[=:]none|JWKS|parameter pollution|type juggling|NoSQL|\\bWAF\\b)
       ```

       Note: `[=:]` stays as-is (no JSON escape needed — square brackets are literal in JSON strings). `[0-9]{4}` and `[0-9]+` stay as-is. Only `\` characters need doubling. JSON does NOT require the regex itself to be valid POSIX ERE — Claude Code stores it as a string regardless — but maintain validity for the forward-doc role.

    2. **`comment-on-matcher` sibling field**: Add or update this field as a sibling of `matcher` (Phase 1 placement, per `<code_context>` line 188 of CONTEXT.md). Copy the text from RESEARCH.md §"Final hooks.json UserPromptSubmit entry" line 712 verbatim:

       ```
       Matcher silently ignored by Claude Code 2.1.x — actual gating happens in nudge.sh via grep -qiE. Kept here for documentation and forward-compat. See .planning/phases/03-hook-scripts-regex/03-CONTEXT.md D-09 + 03-RESEARCH.md §'Drift probe 4b'. After JSON decode, the regex string here is byte-identical to the grep argument in nudge.sh.
       ```

       (If Phase 1's hooks.json already contains `comment-on-matcher` as a sibling field, replace its value. If not, add it as a sibling immediately after `matcher` per RESEARCH.md drop-in placement.)

    3. **DO NOT touch** any other field. Explicitly preserved (D-10 + D-08 + Phase 1):
       - `description` field at the top of the file.
       - `SessionStart` entry (block at lines 4-14 of current file): `matcher` stays `"startup|resume|clear|compact"` (D-10 — verified working in Phase 1); `command` stays `bash "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/session-start.sh"`; `timeout` stays `5`. The entire SessionStart block is UNTOUCHED.
       - UserPromptSubmit `hooks` array: `command` stays `bash "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/nudge.sh"`; `timeout` stays `3`. Only the matcher and comment-on-matcher change.

    4. **JSON validity**: After the edit, `jq . plugins/hack-skills-router/hooks/hooks.json` MUST exit 0 (no parse errors). The file MUST remain valid JSON.

    5. **Drift probe 4b prereq**: The regex string MUST round-trip equal between hooks.json (after `jq -r` JSON-decode) and nudge.sh (after sed extraction of the `grep -qiE` argument). Task 4 runs probe 4b explicitly. To get this right: write the JSON-escaped form above in hooks.json; the nudge.sh from Task 2 already contains the single-backslash bash form; `jq -r` will decode `\\b` → `\b` and produce a string byte-identical to nudge.sh's regex.

    File budget: net change is ~3 logical lines (matcher value + comment-on-matcher value). Total file size after edit: ~32 lines (up from 29 in Phase 1).
  </action>
  <verify>
    <automated>jq . plugins/hack-skills-router/hooks/hooks.json > /dev/null \
      && [ "$(jq -r '.hooks.SessionStart[0].matcher' plugins/hack-skills-router/hooks/hooks.json)" = "startup|resume|clear|compact" ] \
      && [ "$(jq -r '.hooks.SessionStart[0].hooks[0].command' plugins/hack-skills-router/hooks/hooks.json)" = 'bash "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/session-start.sh"' ] \
      && [ "$(jq -r '.hooks.SessionStart[0].hooks[0].timeout' plugins/hack-skills-router/hooks/hooks.json)" = "5" ] \
      && [ "$(jq -r '.hooks.UserPromptSubmit[0].hooks[0].command' plugins/hack-skills-router/hooks/hooks.json)" = 'bash "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/nudge.sh"' ] \
      && [ "$(jq -r '.hooks.UserPromptSubmit[0].hooks[0].timeout' plugins/hack-skills-router/hooks/hooks.json)" = "3" ] \
      && jq -r '.hooks.UserPromptSubmit[0].matcher' plugins/hack-skills-router/hooks/hooks.json | grep -q 'introspection' \
      && jq -r '.hooks.UserPromptSubmit[0].matcher' plugins/hack-skills-router/hooks/hooks.json | grep -q 'alg\[=:\]none' \
      && jq -r '.hooks.UserPromptSubmit[0].matcher' plugins/hack-skills-router/hooks/hooks.json | grep -q 'JWKS' \
      && jq -r '.hooks.UserPromptSubmit[0].matcher' plugins/hack-skills-router/hooks/hooks.json | grep -q '\\bWAF\\b' \
      && [ -z "$(jq -r '.hooks.UserPromptSubmit[0].matcher' plugins/hack-skills-router/hooks/hooks.json | grep -i 'file upload')" ] \
      && [ -z "$(jq -r '.hooks.UserPromptSubmit[0].matcher' plugins/hack-skills-router/hooks/hooks.json | grep -i 'business logic')" ] \
      && jq -r '.hooks.UserPromptSubmit[0]."comment-on-matcher"' plugins/hack-skills-router/hooks/hooks.json | grep -q 'silently ignored'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `plugins/hack-skills-router/hooks/hooks.json` and `jq .` parses it successfully (exit 0)
    - `jq -r '.hooks.SessionStart[0].matcher'` returns exactly `startup|resume|clear|compact` (D-10 UNCHANGED)
    - `jq -r '.hooks.SessionStart[0].hooks[0].command'` returns exactly `bash "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/session-start.sh"` (UNCHANGED)
    - `jq -r '.hooks.SessionStart[0].hooks[0].timeout'` returns `5` (UNCHANGED)
    - `jq -r '.hooks.UserPromptSubmit[0].hooks[0].command'` returns exactly `bash "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/nudge.sh"` (UNCHANGED)
    - `jq -r '.hooks.UserPromptSubmit[0].hooks[0].timeout'` returns `3` (UNCHANGED)
    - `jq -r '.hooks.UserPromptSubmit[0].matcher'` (after JSON decode) starts with `(XSS|SQLi|` and contains all 7 D-02 additions: `introspection`, `host header`, `alg[=:]none`, `JWKS`, `parameter pollution`, `type juggling`, `NoSQL`, `\bWAF\b`
    - `jq -r '.hooks.UserPromptSubmit[0].matcher'` does NOT contain `file upload` and does NOT contain `business logic` (D-02 exclusions)
    - `jq -r '.hooks.UserPromptSubmit[0]."comment-on-matcher"'` returns a string containing the substring `silently ignored` and references D-09 or drift probe 4b
    - Phase 1 placeholder `"XSS"` standalone as matcher value is GONE (the new regex contains `XSS` as one alternation in a 522-char string, not as the entire matcher value)
  </acceptance_criteria>
  <done>hooks.json UserPromptSubmit matcher carries the full 522-char forward-doc regex (D-02 + D-09); SessionStart entry untouched (D-10); JSON validates with `jq`; comment-on-matcher field documents the silently-ignored-at-runtime nature and points to D-09 + drift probe 4b. Phase 1's `"XSS"` placeholder is replaced.</done>
</task>

<task type="auto">
  <name>Task 4: Run all automated verification probes (drift 4a, 4b, 4c, token-count, JSON validity, chmod, shellcheck if available)</name>
  <files>
    <!-- Read-only verification — no file modifications -->
  </files>
  <read_first>
    - .planning/phases/03-hook-scripts-regex/03-RESEARCH.md §"Drift probe 4a — D-06 lede byte-identity check" (lines 726-734)
    - .planning/phases/03-hook-scripts-regex/03-RESEARCH.md §"Drift probe 4b — D-02/D-09 regex string byte-identity check" (lines 736-750)
    - .planning/phases/03-hook-scripts-regex/03-RESEARCH.md §"Drift probe 4c — STUB marker removal check" (lines 752-762)
    - .planning/phases/03-hook-scripts-regex/03-RESEARCH.md §"Token-count probe (~250 tok / ~75 tok)" (lines 764-778)
    - .planning/phases/03-hook-scripts-regex/03-RESEARCH.md §"Pitfall 5: Forgetting to chmod +x" (lines 612-617)
    - plugins/hack-skills-router/hooks/scripts/session-start.sh (written in Task 1)
    - plugins/hack-skills-router/hooks/scripts/nudge.sh (written in Task 2)
    - plugins/hack-skills-router/hooks/hooks.json (edited in Task 3)
    - plugins/hack-skills-router/skills/hack-skills-router/SKILL.md (drift-probe 4a source-of-truth)
  </read_first>
  <action>
    Run all 6 automated verification probes (plus optional shellcheck) against the 3 files produced by Tasks 1-3. This is a verification task — no file modifications. If any probe fails, surface the specific failure mode + which file needs correction.

    **Probe 1 — Drift 4a (D-06 lede byte-identity, run AFTER session-start.sh exists per Task 1):**
    ```bash
    diff <(sed -n '84,91p' plugins/hack-skills-router/skills/hack-skills-router/SKILL.md) \
         <(grep -E '^[0-9]\. ' plugins/hack-skills-router/hooks/scripts/session-start.sh)
    ```
    Acceptance: exit 0 (zero diff). On failure: report which line(s) differ; the executor must update session-start.sh's numbered ledes to match SKILL.md lines 84-91 exactly.

    **Probe 2 — Drift 4b (D-02/D-09 regex byte-identity, run AFTER both hooks.json AND nudge.sh updated per Tasks 2-3):**
    ```bash
    HOOKS_MATCHER=$(jq -r '.hooks.UserPromptSubmit[0].matcher' plugins/hack-skills-router/hooks/hooks.json)
    NUDGE_REGEX=$(grep -oE 'grep -qiE "[^"]+"' plugins/hack-skills-router/hooks/scripts/nudge.sh \
                  | sed -E 's/grep -qiE "(.+)"/\1/')
    [ "$HOOKS_MATCHER" = "$NUDGE_REGEX" ]
    ```
    Acceptance: exit 0 (strings equal). On failure: print both strings + diff; the executor must reconcile (typically: hooks.json JSON escape was wrong, or one file got an addition/removal the other didn't).

    **Probe 3 — Drift 4c (STUB marker removal, run AFTER both .sh files written):**
    ```bash
    ! grep -in 'stub\|placeholder\|phase 1' plugins/hack-skills-router/hooks/scripts/*.sh
    ```
    Acceptance: exit 0 (grep found nothing — the leading `!` inverts grep's exit code). On failure: print the offending line(s); the executor must scrub the file.

    Note on grep-comment-counting hygiene (per planner guidance): if executor wants to filter comments to reduce false positives, use `grep -v '^#' file | grep -i ...` — but here the requirement is stricter: the STRING "STUB" / "placeholder" / "Phase 1" must not appear ANYWHERE in the .sh files, including comments. The Phase 1 stub files had STUB in the comment-block AND in the heredoc body; both must go. Accordingly: use the unfiltered grep above (the leading `!` makes it pass when no matches found).

    **Probe 4 — Token-count (informational; do not hard-fail unless wildly out of range):**
    ```bash
    echo "session-start.sh token estimate:"
    sed -n "/cat <<'EOF'/,/^EOF$/{/cat <<'EOF'/d; /^EOF$/d; p}" plugins/hack-skills-router/hooks/scripts/session-start.sh \
      | wc -w \
      | awk '{print "  ", int($1 * 1.33), "estimated tokens (target ~250, tolerance ±100)"}'

    echo "nudge.sh token estimate:"
    sed -n "/cat <<'EOF'/,/^EOF$/{/cat <<'EOF'/d; /^EOF$/d; p}" plugins/hack-skills-router/hooks/scripts/nudge.sh \
      | wc -w \
      | awk '{print "  ", int($1 * 1.33), "estimated tokens (target ~75, tolerance ±25)"}'
    ```
    Acceptance: session-start.sh heredoc body 150-400 tok (the wc heuristic over-estimates by ~30%; the RESEARCH.md drop-in is ~385 estimated which is within tolerance); nudge.sh heredoc body 50-100 tok. RESEARCH.md Open Question #2 recommends ACCEPT 200-300 range without tightening — only push back if SessionStart exceeds 400 tok estimated.

    **Probe 5 — JSON validity (run AFTER Task 3):**
    ```bash
    jq . plugins/hack-skills-router/hooks/hooks.json > /dev/null
    ```
    Acceptance: exit 0. On failure: print jq error; the executor must fix the JSON syntax.

    **Probe 6 — chmod preservation (run AFTER all writes per Pitfall 5):**
    ```bash
    ls -l plugins/hack-skills-router/hooks/scripts/*.sh | awk '{print $1, $NF}'
    ```
    Acceptance: both files show mode `-rwxr-xr-x` (the leading `-rwx` confirms executable for owner; full mode 0755). If the Write tool dropped the +x bit (rare), idempotent fix: `chmod +x plugins/hack-skills-router/hooks/scripts/*.sh`.

    **Probe 7 — shellcheck (optional, graceful skip):**
    ```bash
    if command -v shellcheck > /dev/null 2>&1; then
      shellcheck plugins/hack-skills-router/hooks/scripts/*.sh
    else
      echo "shellcheck not installed — skipping (optional probe)"
    fi
    ```
    Acceptance: shellcheck exit 0 or "not installed" message. SC2086 (word-splitting on `$PROMPT`) is EXPECTED — `echo "$PROMPT"` is already quoted; if shellcheck flags any other issue, evaluate and fix.

    **Probe 8 — Functional smoke (positive and negative match against nudge.sh):**
    ```bash
    # Positive — should emit nudge
    echo '{"prompt":"How do I attack a JWT with alg=none confusion?"}' | bash plugins/hack-skills-router/hooks/scripts/nudge.sh > /tmp/03-nudge-positive.txt
    grep -q 'security task detected' /tmp/03-nudge-positive.txt && echo "PASS: positive match emits nudge" || echo "FAIL: positive match did not emit nudge"

    # Negative — should emit nothing
    echo '{"prompt":"What is the weather today?"}' | bash plugins/hack-skills-router/hooks/scripts/nudge.sh > /tmp/03-nudge-negative.txt
    [ ! -s /tmp/03-nudge-negative.txt ] && echo "PASS: negative match emits nothing (D-01)" || echo "FAIL: negative match emitted output"

    # Functional smoke for session-start (ignores stdin per design)
    bash plugins/hack-skills-router/hooks/scripts/session-start.sh > /tmp/03-session-start.txt 2>&1
    grep -q 'HACK-SKILLS SESSION CONTEXT' /tmp/03-session-start.txt && echo "PASS: session-start emits banner" || echo "FAIL: session-start broken"
    ```

    Probe sequencing: probes 1, 5, 6 only need their respective Tasks done. Probe 2 needs Tasks 2 AND 3. Probe 3 needs Tasks 1 AND 2. Probe 4 is informational. Probe 7 is graceful-skip. Probe 8 needs Tasks 2 AND 3. Run them in this order: 1, 3, 5, 2, 4, 6, 7, 8 (cheap to expensive; structural checks before functional smoke).

    Stop on first hard failure (probes 1, 2, 3, 5, 6, 8); for token-count (probe 4) and shellcheck (probe 7), print the result and continue (informational).
  </action>
  <verify>
    <automated>set -e \
      && diff <(sed -n '84,91p' plugins/hack-skills-router/skills/hack-skills-router/SKILL.md) <(grep -E '^[0-9]\. ' plugins/hack-skills-router/hooks/scripts/session-start.sh) \
      && jq . plugins/hack-skills-router/hooks/hooks.json > /dev/null \
      && [ "$(jq -r '.hooks.UserPromptSubmit[0].matcher' plugins/hack-skills-router/hooks/hooks.json)" = "$(grep -oE 'grep -qiE "[^"]+"' plugins/hack-skills-router/hooks/scripts/nudge.sh | sed -E 's/grep -qiE "(.+)"/\1/')" ] \
      && ! grep -in 'stub\|placeholder\|phase 1' plugins/hack-skills-router/hooks/scripts/*.sh \
      && test -x plugins/hack-skills-router/hooks/scripts/session-start.sh \
      && test -x plugins/hack-skills-router/hooks/scripts/nudge.sh \
      && echo '{"prompt":"test for XSS injection"}' | bash plugins/hack-skills-router/hooks/scripts/nudge.sh | grep -q 'security task detected' \
      && [ -z "$(echo '{"prompt":"weather today"}' | bash plugins/hack-skills-router/hooks/scripts/nudge.sh)" ] \
      && bash plugins/hack-skills-router/hooks/scripts/session-start.sh | grep -q 'HACK-SKILLS SESSION CONTEXT'</automated>
  </verify>
  <acceptance_criteria>
    - Drift probe 4a (D-06): exit 0 — 8 ledes byte-identical between SKILL.md lines 84-91 and session-start.sh numbered-lede lines
    - Drift probe 4b (D-02/D-09): exit 0 — hooks.json matcher (after `jq -r` decode) is byte-identical to nudge.sh grep regex (after `sed` extract)
    - Drift probe 4c (STUB removal): grep finds zero matches of `stub|placeholder|phase 1` (case-insensitive) in either .sh file
    - JSON validity: `jq . hooks.json` exits 0
    - chmod preservation: both .sh files are mode 0755 (`test -x` exits 0 on both)
    - Functional smoke positive: prompt containing security term → nudge.sh stdout contains `security task detected` banner
    - Functional smoke negative (D-01): non-security prompt → nudge.sh stdout is empty (silent exit 0)
    - Functional smoke session-start: bash session-start.sh stdout contains the banner `HACK-SKILLS SESSION CONTEXT`
    - Token-count (informational): session-start.sh heredoc body 150-400 estimated tok; nudge.sh heredoc body 50-100 estimated tok
    - shellcheck (optional): no findings beyond expected SC2086 false-positive on `$PROMPT` (which is already quoted); or "not installed" graceful skip
  </acceptance_criteria>
  <done>All 6 hard probes (4a, 4b, 4c, JSON validity, chmod, functional smoke positive/negative/session-start) PASS. Token-count and shellcheck probes either PASS or surface as informational warnings within tolerance. Phase 3 automated verification complete; ready for manual UAT (Plan 02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code runtime → hook script process | Claude Code invokes `bash "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/<name>.sh"` and writes JSON to the script's stdin. Boundary is the `bash` invocation; the hook script process runs with the user's credentials. |
| User prompt text → nudge.sh `$PROMPT` variable | The user prompt is JSON-extracted (`jq -r '.prompt // empty'`) and bound to a bash variable. Untrusted input crosses this boundary. |
| Hook script stdout → Claude Code context injection | `cat <<'EOF' ... EOF` writes a fixed payload to stdout. Claude Code captures stdout and injects it as additional context. The payload is static (no `$VAR` expansion under single-quoted heredoc) so the boundary is one-way. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering / Injection | `nudge.sh` use of `$PROMPT` in `echo "$PROMPT" \| grep -qiE` | mitigate | `echo` treats `$PROMPT` as a single quoted argument; `grep` is a pure filter (no eval). The `jq -r '.prompt // empty'` extraction bounds `$PROMPT` to the JSON `.prompt` string only — no other stdin fields leak in. Even attacker-controlled prompt text (e.g., shell metacharacters, backticks, `$()`) cannot escape the quoting because `echo` is the only program that sees `$PROMPT`, and grep receives it via pipe (file descriptor) not as a command-line argument. Verified safe by Phase 1 RESEARCH §"Code Examples" + RESEARCH.md §"Security Domain" (lines 871-880). |
| T-03-02 | Tampering / Code injection via heredoc | Both .sh scripts use `cat <<'EOF' ... EOF` to emit payload text. Payload bodies contain markdown backticks (`` `alg` ``, `` `kid` ``, `` `hack` ``). | mitigate | Single-quoted heredoc `<<'EOF'` prevents bash from interpreting backticks or `$` as command substitution (D-08 + RESEARCH.md Pitfall 2). Without the single-quotes, an attacker who somehow modified the script could embed `\`rm -rf /\`` and it would execute when the script runs. Single-quotes pass everything through as literal text. Drift probe 4c indirectly guards this by enforcing the structural shape from RESEARCH.md drop-ins (which use single-quoted form). |
| T-03-03 | Information disclosure | `jq` parse errors on malformed stdin could pollute Claude `--debug` logs and may surface in transcripts. | mitigate | `2>/dev/null` on the `jq` invocation suppresses stderr (RESEARCH.md Pitfall 7). Combined with `// empty` fallback the script degrades gracefully on any malformed input (PROMPT becomes empty string → grep no-match → silent exit 0). |
| T-03-04 | Denial of Service / Catastrophic backtracking | Long regex on every prompt — could a pathological prompt trigger exponential backtracking? | accept | RESEARCH.md Item 1 verification (line 178) confirmed: NONE. The regex is simple alternation with no nested quantifiers, no overlapping alternation prefixes followed by `*`/`+`. `grep -E` uses NFA without recursion. Worst case is linear-time scan of a ~10KB prompt against a 522-char regex — well within the 3s timeout declared in hooks.json. |
| T-03-05 | Repudiation / Audit gaps | Hook scripts emit no audit log; can't trace which prompts triggered the nudge. | accept | This is by design (D-01: non-nudgy on non-matches; no telemetry per CONTEXT.md `<deferred>`). The router itself logs nothing (deferred to ROUTER-FUT telemetry hook). Local-only personal marketplace; no compliance requirement for audit trail. |
| T-03-06 | Elevation of Privilege / Credential exposure | Could the hook scripts leak secrets, API keys, or environment? | accept | No — scripts emit only static heredoc text; no env-var interpolation (single-quoted heredoc prevents `$VAR` expansion); no filesystem writes (other than implicit `/tmp` from probe verifications which is the executor's domain); no network calls; no IPC. RESEARCH.md §"Security Domain" lines 873-878 audit confirms. |
| T-03-SC | Supply Chain / Tampering | No package installs in Phase 3. | mitigate | N/A — phase installs zero npm/pip/cargo packages. The only external tool dependency is `jq` (already installed via Homebrew, verified Phase 1; not pulled fresh in Phase 3). Phase 3 RESEARCH.md §"Package Legitimacy Audit" (line 116) explicitly states: "Not applicable. Phase 3 does not install any external packages." No `[ASSUMED]` or `[SUS]` packages exist; no human-verify checkpoint needed. |

**Disposition summary:**
- 3 threats mitigated (T-03-01, T-03-02, T-03-03)
- 3 threats accepted with documented rationale (T-03-04, T-03-05, T-03-06)
- 1 supply-chain threat N/A (T-03-SC — no packages)
- 0 HIGH severity → security_enforcement gate does NOT block planning.
</threat_model>

<verification>
**Phase 3 Plan 01 verification scope:**

This plan's verification is entirely automated and lives inside Task 4 (drift probes 4a/4b/4c, JSON validity, chmod, functional smoke positive/negative/session-start, token-count, optional shellcheck). All 8 probes (6 hard + 2 informational) execute against the 3 files Tasks 1-3 produce; if any hard probe fails, the executor identifies which task's output needs correction (probe 4a → Task 1; probe 4b → Task 2 or 3; probe 4c → Task 1 or 2; JSON → Task 3; chmod → Task 1 or 2; functional → Task 1 or 2).

**What Plan 01 does NOT verify:**
- Hook firing in a real `claude` session (uses cached plugin copy; needs uninstall + reinstall) — owned by Plan 02 manual UAT.
- SessionStart payload appears in Claude's context after fresh session boundary — owned by Plan 02 (Option B fresh `claude` invocation per Phase 1 `01-03-SUMMARY.md`).
- 5+1 positive UAT prompts trigger the nudge, 3+1 negative UAT prompts do not, in a live Claude session — owned by Plan 02 (D-11 + ROADMAP Phase 3 SC #4).

The seam is: Plan 01 proves the files are CORRECT against their specifications (probes run shell scripts directly, not through Claude Code). Plan 02 proves the files WORK when installed into Claude Code's plugin cache.
</verification>

<success_criteria>
- Task 1: session-start.sh is the ~250 tok SessionStart payload with byte-identical 8 ledes from SKILL.md, banner intact, footer intact, exit 0, mode 0755. (HOOKS-01)
- Task 2: nudge.sh is jq+grep filter prefix + ~75 tok nudge payload, references Skill(hack-skills-router) and patterns/expert-intuitions.md by name, banner intact, exit 0, mode 0755, positive prompts emit payload, negative prompts emit nothing (D-01 silent). (HOOKS-02 + HOOKS-03)
- Task 3: hooks.json UserPromptSubmit matcher is the 522-char final regex (forward-doc, byte-identical to nudge.sh grep argument after JSON decode); SessionStart entry UNTOUCHED (D-10); comment-on-matcher updated; jq validates. (HOOKS-02 + HOOKS-04)
- Task 4: All 6 hard probes PASS; token-count and shellcheck informational PASS or graceful skip. (Cross-cuts HOOKS-01..HOOKS-04)
- All 4 phase requirements (HOOKS-01, HOOKS-02, HOOKS-03, HOOKS-04) addressed.
- All 11 D-decisions (D-01..D-11) respected per `must_haves.truths`.
- Drift probes 4a + 4b + 4c (the 3 cross-file consistency contracts the phase introduces) are wired into Task 4 as gating checks.
</success_criteria>

<output>
Create `.planning/phases/03-hook-scripts-regex/03-01-SUMMARY.md` when all 4 tasks complete and all 6 hard automated probes pass. Summary should record:
- Token-count probe results (estimated tok for both scripts)
- Any shellcheck findings (or "not installed" / "no findings beyond expected SC2086")
- Confirmation that drift probes 4a, 4b, 4c all PASS
- Confirmation that functional smoke (positive + negative + session-start) all PASS
- Any deviation from RESEARCH.md drop-ins (none expected; surface if any)
- Pointer to Plan 02 for manual UAT (next step in the wave)
</output>
