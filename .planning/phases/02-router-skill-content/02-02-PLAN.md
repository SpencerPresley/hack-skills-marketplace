---
phase: 02-router-skill-content
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/hack-skills-router/skills/hack-skills-router/patterns/expert-intuitions.md
autonomous: true
requirements: [ROUTER-03]
tags: [expert-intuitions, boundary-conditions, content-authoring, paraphrase, attribution]

must_haves:
  truths:
    - "patterns/expert-intuitions.md exists at the canonical sidecar path"
    - "File opens with a file-level attribution header paragraph citing yaklang/hack-skills (MIT) and the 4-file attribution chain"
    - "All 8 intuitions are present, in the upstream order, addressed by H3 headings of the form '### Intuition N: <topic>'"
    - "Each intuition entry contains a one-sentence Lede (matches the body's boundary-conditions summary), a 3-5 sentence Mechanism paragraph, and a concrete Example"
    - "No per-paragraph attribution annotations — attribution is file-level only"
    - "No Chinese characters or bilingual upstream phrasing — English only"
    - "File contains zero clickable markdown links to sibling sub-files (routing-tables.md, workflow-walkthroughs.md)"
  artifacts:
    - path: "plugins/hack-skills-router/skills/hack-skills-router/patterns/expert-intuitions.md"
      provides: "8 paraphrased boundary-condition intuitions with file-level attribution"
      contains: ["yaklang/hack-skills", "MIT", "Intuition 1:", "Intuition 8:", "Mechanism", "Example"]
  key_links:
    - from: "plugins/hack-skills-router/skills/hack-skills-router/patterns/expert-intuitions.md"
      to: "upstream hack SKILL.md lines 119-130"
      via: "8 ledes preserve the upstream's framing and rank order; attribution header names yaklang/hack-skills as MIT-licensed source"
      pattern: "grep -q 'yaklang/hack-skills' && grep -q 'MIT' && 8 unique '### Intuition N:' headings"
---

<objective>
Create `patterns/expert-intuitions.md` — the progressive-disclosure sub-file holding the
8 high-value boundary-condition intuitions paraphrased from upstream yaklang/hack-skills.
This file is loaded on demand when the SKILL.md body's "Boundary conditions" section
cross-references it for paragraphs + examples.

Purpose: ROUTER-03 (router's expert-intuitions.md documents 8 boundary conditions with
attribution to upstream).

Output: One new markdown file at the sidecar path with file-level attribution header
followed by 8 H3-keyed entries (each ~6-10 lines: Lede + Mechanism paragraph + concrete
Example). Total ~80-100 lines.
</objective>

<execution_context>
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-router-skill-content/02-CONTEXT.md
@.planning/phases/02-router-skill-content/02-RESEARCH.md
@.planning/phases/02-router-skill-content/02-PATTERNS.md
@.planning/specs/2026-05-22-v2-router-design.md

<interfaces>
<!-- Source-of-truth content the executor must mirror. Extracted from upstream hack SKILL.md
     (verbatim ledes) and 02-RESEARCH.md §"The 8 Upstream Intuitions" (paraphrased
     Mechanism + Example slots already drafted by the researcher). -->

8 intuition ledes (verbatim from upstream hack SKILL.md lines 121-130, MIT-licensed):
1. "The same filtering logic is often reused across multiple pages: if one point is bypassable, similar pages usually are too."
2. "Parameter names are an attack surface too: WAFs often inspect values but not names."
3. "Second-order vulnerabilities are common: safe at storage time does not mean safe when later read into a dangerous context."
4. "BOLA is fundamentally 'authenticated but unauthorized': replaying with account A/B switching is critical."
5. "Older API versions are most likely to miss patches: fixing v2 does not mean v1 was retired."
6. "Business-logic vulnerabilities often bring highest impact: scanners miss them and they persist longer."
7. "Race conditions should prioritize one-time actions: coupon redemption, claims, resets, invites, trials, inventory deduction."
8. "For JWT attacks, check key and algorithm context first: do not blindly spray payloads; verify `alg`, `kid`, JWKS, and key source first."

Pre-paraphrased Mechanism + Example bodies are in 02-RESEARCH.md §"The 8 Upstream Intuitions
(verbatim source + example slots)" — lines 897-941 of 02-RESEARCH.md. Each intuition has:
- Upstream lede (verbatim quote — keep this as the Lede field in the entry, optionally
  prefixed by an attribution-free paraphrase)
- Mechanism (3-5 sentence prose explanation pre-drafted by the researcher)
- Example slot (concrete example pre-drafted by the researcher)

The executor copies the Mechanism and Example text from RESEARCH lines 905-941 with minor
cleanup (remove the "**Upstream lede (verbatim):**" / "**Mechanism:**" / "**Example slot
(concrete):**" researcher-side meta-labels and use the PLAN's per-entry format below).

Per-entry format (per D-10):
```
### Intuition N: <short topic>

**Lede:** <one-sentence summary; matches what router SKILL.md body uses verbatim>

**Mechanism:** <3-5 sentence prose paragraph explaining WHY this intuition matters>

**Example:** <concrete worked example (e.g., specific payload, specific endpoint pattern,
specific tool flow)>
```

Header attribution paragraph (verbatim — 02-PATTERNS.md §"Pattern 3" + 02-CONTEXT.md D-09):
  These 8 boundary-condition intuitions are paraphrased from `yaklang/hack-skills`'s
  `hack` SKILL.md (MIT-licensed). Each entry preserves the upstream's framing and rank
  order; the explanatory paragraphs and concrete examples are written for this router.
  See `.planning/specs/2026-05-22-v2-router-design.md` §11 for the full attribution chain.

8 short-topic suffixes for H3 headings (use these for the `<short topic>` slot):
1. "Filter logic reuses across pages"
2. "Parameter names as attack surface"
3. "Second-order vulnerabilities"
4. "BOLA — authenticated but unauthorized"
5. "Older API versions miss patches"
6. "Business-logic flaws bring highest impact"
7. "Race conditions on one-time operations"
8. "JWT — verify key and algorithm context first"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author patterns/expert-intuitions.md — 8 entries (Lede + Mechanism + Example) in upstream order with file-level attribution</name>
  <files>plugins/hack-skills-router/skills/hack-skills-router/patterns/expert-intuitions.md</files>
  <read_first>
    - plugins/hack-skills-router/skills/hack-skills-router/SKILL.md (current Phase 1 stub — confirms the parent skill directory exists)
    - /Users/spencerpresley/.claude/plugins/cache/hack-skills-marketplace/hack-skills-recon/c6f732befcae-32c1cf49/hack/SKILL.md (upstream MIT-licensed source — lines 119-130 carry the 8 verbatim intuition ledes)
    - .planning/phases/02-router-skill-content/02-RESEARCH.md lines 897-941 (researcher's pre-paraphrased Mechanism + Example for each of the 8 intuitions — drop these in)
    - .planning/phases/02-router-skill-content/02-PATTERNS.md lines 363-441 (pattern assignment #3 — expert-intuitions.md structure, per-entry shape, file-header attribution, critical constraints)
    - .planning/phases/02-router-skill-content/02-CONTEXT.md §Implementation Decisions D-08, D-09, D-10, D-15 (locked structural decisions)
    - .planning/specs/2026-05-22-v2-router-design.md §5.9 + §11 (design source-of-truth outline + attribution chain)
  </read_first>
  <action>
    Create `plugins/hack-skills-router/skills/hack-skills-router/patterns/expert-intuitions.md`. The parent `patterns/` directory will already exist by the time this task runs in the same wave as plan 02-01 (since Plan 02-01 creates it), but if it does not, create it with `mkdir -p` first. Use the Write tool — no heredoc / cat-EOF.

    Structure:

    1. **H1 title:** `# Expert Intuitions — Boundary Conditions`
    2. **Attribution header paragraph** (blockquote `>` form), verbatim from `<interfaces>` block above: "These 8 boundary-condition intuitions are paraphrased from `yaklang/hack-skills`'s `hack` SKILL.md (MIT-licensed). Each entry preserves the upstream's framing and rank order; the explanatory paragraphs and concrete examples are written for this router. See `.planning/specs/2026-05-22-v2-router-design.md` §11 for the full attribution chain."
    3. **8 H3 entries** in upstream order (per D-08 — NO reordering, NO additions, NO removals), each using exactly this template:
       ```
       ### Intuition N: <short topic from interfaces block>

       **Lede:** <one-sentence summary — match the body's boundary-conditions summary
                 line so the body's inline list and the sub-file entry use identical
                 phrasing for the lede>

       **Mechanism:** <3-5 sentence prose paragraph — paraphrase from upstream + the
                      researcher's pre-drafted text in 02-RESEARCH.md lines 905-941.
                      Explain WHY this intuition matters in real testing>

       **Example:** <concrete worked example — copy from RESEARCH §"Example slot
                    (concrete):" for that intuition; specific payload, endpoint
                    pattern, or tool flow>
       ```
    4. Use the exact 8 short-topic suffixes from `<interfaces>` for the H3 headings (e.g. `### Intuition 1: Filter logic reuses across pages`).

    Content sourcing rules:
    - **Lede:** Adapt the upstream verbatim text from `<interfaces>` (lines 1-8 of the intuition list) into a single-sentence summary phrased to match what the SKILL.md body (Plan 02-04) will use inline. Goal: the body's one-line summary and this file's Lede field are identical text.
    - **Mechanism:** Use the researcher's pre-paraphrased Mechanism text from 02-RESEARCH.md lines 905, 910, 915, 920, 925, 930, 935, 940 (one Mechanism per intuition). Light copy-edit allowed (remove researcher-side meta-language like "**Mechanism:**" label and trailing "**Example slot (concrete):**" labels); preserve substance.
    - **Example:** Use the researcher's pre-drafted Example text from 02-RESEARCH.md lines 906, 911, 916, 921, 926, 931, 936, 941 (one Example per intuition).

    Style constraints (enforced by acceptance criteria):
    - English only — do NOT carry over any Chinese phrasing from the upstream's bilingual original (REQUIREMENTS Out of Scope).
    - Attribution is file-level only — do NOT add "(from upstream)" or "(paraphrased from yaklang)" annotations to individual entries (D-09, D-15).
    - No clickable markdown links from this file to `routing-tables.md` or `workflow-walkthroughs.md` (Pitfall 5). Prose mentions by name are allowed but use plain backticks, not link syntax.
    - Each entry: ~6-10 lines (per D-10). File total: ~80-100 lines.
    - NO `**Lede (one-sentence summary, matches the body's boundary-condition line):**` style label — use the cleaner `**Lede:**` shape from the per-entry template above. Researcher's headings in 02-RESEARCH.md are scaffolding; the production file uses the cleaner labels.

    After writing the file, run the verification probes under `<verify>` to confirm correctness.
  </action>
  <verify>
    <automated>
ROUTER_DIR=/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/plugins/hack-skills-router/skills/hack-skills-router
F="$ROUTER_DIR/patterns/expert-intuitions.md"

# Probe 1: File exists
test -f "$F" || { echo "FAIL: file not created"; exit 1; }

# Probe 2: H1 title present
grep -q '^# Expert Intuitions' "$F" || { echo "FAIL: missing H1 title"; exit 1; }

# Probe 3: Attribution header cites yaklang/hack-skills + MIT + design-spec §11
grep -q 'yaklang/hack-skills' "$F" || { echo "FAIL: missing yaklang/hack-skills attribution"; exit 1; }
grep -qi 'MIT' "$F" || { echo "FAIL: missing MIT license note"; exit 1; }
grep -q '2026-05-22-v2-router-design.md' "$F" || { echo "FAIL: missing reference to design spec §11 attribution chain"; exit 1; }

# Probe 4: Exactly 8 H3 entries of the form '### Intuition N:'
INTUITIONS=$(grep -cE '^### Intuition [1-8]:' "$F")
[ "$INTUITIONS" = "8" ] || { echo "FAIL: expected 8 '### Intuition N:' headings, got $INTUITIONS"; exit 1; }

# Probe 5: Each intuition number 1..8 appears exactly once
for N in 1 2 3 4 5 6 7 8; do
  COUNT=$(grep -cE "^### Intuition ${N}:" "$F")
  [ "$COUNT" = "1" ] || { echo "FAIL: Intuition $N appears $COUNT times (expected 1)"; exit 1; }
done

# Probe 6: Each entry has Lede, Mechanism, Example fields
LEDE=$(grep -cE '^\*\*Lede:\*\*' "$F")
MECH=$(grep -cE '^\*\*Mechanism:\*\*' "$F")
EXAM=$(grep -cE '^\*\*Example:\*\*' "$F")
[ "$LEDE" = "8" ] || { echo "FAIL: expected 8 Lede fields, got $LEDE"; exit 1; }
[ "$MECH" = "8" ] || { echo "FAIL: expected 8 Mechanism fields, got $MECH"; exit 1; }
[ "$EXAM" = "8" ] || { echo "FAIL: expected 8 Example fields, got $EXAM"; exit 1; }

# Probe 7: No clickable markdown link to sibling sub-files (Pitfall 5)
! grep -E '\]\((\.\.?/)?(patterns/routing-tables\.md|examples/workflow-walkthroughs\.md)' "$F" \
  || { echo "FAIL: clickable link to sibling sub-file detected (Pitfall 5)"; exit 1; }

# Probe 8: English only — no Chinese characters (CJK Unified Ideographs U+4E00-U+9FFF)
! LC_ALL=C grep -P '[\x{4E00}-\x{9FFF}]' "$F" 2>/dev/null \
  || { echo "FAIL: Chinese characters detected (English-only constraint)"; exit 1; }
# Fallback non-regex check: any byte >= 0xE4 with continuation pattern indicates likely CJK in UTF-8
! perl -ne 'exit 1 if /\p{Han}/' "$F" 2>/dev/null && true || { echo "FAIL: CJK character detected via perl probe"; exit 1; }

# Probe 9: Reasonable file length (60-200 lines — guard against either truncation or runaway)
LINES=$(wc -l < "$F")
[ "$LINES" -ge 60 ] && [ "$LINES" -le 200 ] || { echo "FAIL: file has $LINES lines (expected 60-200)"; exit 1; }

# Probe 10: Intuition order matches upstream — each H3 appears AFTER the previous numerically
ORDER_OK=$(awk '/^### Intuition [1-8]:/{
  n = substr($3, 1, length($3)-1) + 0
  if (n != prev + 1) { print "FAIL"; exit }
  prev = n
} END { print "OK" }' "$F")
[ "$ORDER_OK" = "OK" ] || { echo "FAIL: intuitions not in 1-8 order (D-08 violation)"; exit 1; }

echo "PASS: all 10 probes succeeded"
    </automated>
  </verify>
  <done>
    All 10 verification probes return PASS:
    1. File exists at `plugins/hack-skills-router/skills/hack-skills-router/patterns/expert-intuitions.md`.
    2. H1 title `# Expert Intuitions ...` present.
    3. Attribution header cites yaklang/hack-skills, MIT, and the design-spec §11 attribution chain.
    4. Exactly 8 entries with `### Intuition N:` headings.
    5. Each of 1..8 appears exactly once.
    6. Each entry has `**Lede:**`, `**Mechanism:**`, `**Example:**` fields (8 of each, 24 total).
    7. No clickable links to `routing-tables.md` or `workflow-walkthroughs.md`.
    8. No Chinese characters (English-only constraint).
    9. File length 60-200 lines.
    10. Intuitions in upstream order 1, 2, 3, 4, 5, 6, 7, 8.
  </done>
</task>

</tasks>

<acceptance_criteria>
- `plugins/hack-skills-router/skills/hack-skills-router/patterns/expert-intuitions.md` exists.
- File opens with `# Expert Intuitions — Boundary Conditions` H1 + a blockquote attribution paragraph mentioning yaklang/hack-skills (MIT) and the design spec §11 attribution chain.
- Exactly 8 H3 entries `### Intuition N: <short topic>` in upstream order (1 through 8).
- Each entry has `**Lede:**`, `**Mechanism:**`, `**Example:**` fields (24 total label occurrences).
- All Mechanism + Example bodies are paraphrased from upstream / the researcher's pre-drafted text in 02-RESEARCH.md lines 905-941, not invented.
- Attribution is file-level only — no per-entry attribution annotations.
- No Chinese characters anywhere in the file.
- No clickable markdown links to sibling sub-files.
- File length is 60-200 lines (D-10 target ~80-100).
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream paraphrase → router file | Content paraphrased from yaklang/hack-skills MIT-licensed source; license/attribution boundary |
| Multi-hop link chain | Cross-references to sibling sub-files; Claude's progressive-disclosure engine penalizes >1-hop links from SKILL.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure (license) | Paraphrased upstream content | mitigate | File-header attribution paragraph naming yaklang/hack-skills + MIT + design-spec §11 — probes 2, 3 enforce presence |
| T-02-06 | Tampering (drift in intuition order) | 8 intuition entries | mitigate | Probes 4, 5, 10 confirm exact-8 count, no duplicates, upstream rank order — D-08 violation = task failure |
| T-02-07 | Information Disclosure (Chinese content carry-over) | Paraphrased content from bilingual upstream | mitigate | Probe 8 fails if any CJK character present (REQUIREMENTS Out of Scope: English only for v2.0) |
| T-02-08 | Tampering (multi-hop link) | Cross-references to sibling sub-files | mitigate | Probe 7 rejects clickable markdown links to `routing-tables.md` or `workflow-walkthroughs.md` (Pitfall 5) |
</threat_model>

<verification>
- Wave gate: This is a Wave 1 plan with `depends_on: []` — no upstream plan dependencies.
- Per-task automated verify: see Task 1 `<verify>` block — 10 probes covering file existence, H1, attribution chain (3 markers: yaklang, MIT, spec §11), exact 8-count of H3 entries + duplicate detection, presence of Lede/Mechanism/Example fields (24 total), Pitfall 5 sub-file links, CJK character absence, line-count sanity, and upstream rank-order preservation (D-08).
- Cross-file consistency (run after all Wave 1 plans complete): each Lede here must be byte-identical to the corresponding inline summary in SKILL.md body's "Boundary conditions" section (Plan 02-04 enforces this match).
</verification>

<success_criteria>
- File exists at the canonical sidecar path.
- All 10 Task 1 verification probes pass.
- 8 paraphrased intuitions in upstream rank order with Lede + Mechanism + Example.
- File-level attribution to yaklang/hack-skills (MIT) + design spec §11.
- English only.
- Zero clickable links to sibling sub-files.
</success_criteria>

<output>
Create `.planning/phases/02-router-skill-content/02-02-SUMMARY.md` when done.
</output>
