---
phase: 02-router-skill-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/hack-skills-router/skills/hack-skills-router/patterns/routing-tables.md
autonomous: true
requirements: [ROUTER-02, ROUTER-05]
tags: [routing, signal-table, dual-load, plugin-recommendation, content-authoring]

must_haves:
  truths:
    - "patterns/routing-tables.md exists at the canonical sidecar path"
    - "File opens with an attribution header paragraph naming yaklang/hack-skills (MIT)"
    - "Exactly 13 plugin-keyed H3 sections appear, one per topical marketplace plugin"
    - "Every section heading is byte-exact against a marketplace.json plugins[].name"
    - "Every section starts with an Install line carrying the canonical /plugin install @hack-skills-marketplace shape (no @branch suffix)"
    - "Each section's Signal-terms table contains 2-3 rows; total across all 13 sections is 35 rows"
    - "Every deep-skill name in the Deep-skill column is byte-exact against a marketplace.json plugins[].skills[] entry (with leading ./ stripped)"
    - "A 'Dual-load rules' H2 section contains exactly 6 rules, each with Trigger / Load both / Rationale fields"
    - "A 'Plugin-recommendation template' H2 section exists at the bottom of the file with the two-line block format and a stacked dual-load example"
    - "File contains zero relative markdown links targeting ../patterns/ or ../examples/ (no sub-file-to-sub-file links per Pitfall 5)"
  artifacts:
    - path: "plugins/hack-skills-router/skills/hack-skills-router/patterns/routing-tables.md"
      provides: "Signal -> deep-skill routing table (13 plugin-keyed sections, 35 rows), 6 dual-load rules, plugin-recommendation template"
      contains: ["yaklang/hack-skills", "### hack-skills-", "Install:", "Dual-load rules", "Plugin-recommendation template"]
  key_links:
    - from: "plugins/hack-skills-router/skills/hack-skills-router/patterns/routing-tables.md"
      to: ".claude-plugin/marketplace.json"
      via: "every plugin name and deep-skill name is a byte-exact copy of an entry in plugins[].name or plugins[].skills[]"
      pattern: "comm -23 mentioned-names canonical-names returns zero lines"
    - from: "plugins/hack-skills-router/skills/hack-skills-router/patterns/routing-tables.md"
      to: "upstream hack SKILL.md"
      via: "header paragraph cites yaklang/hack-skills as MIT-licensed source"
      pattern: "grep -q 'yaklang/hack-skills' file && grep -qi 'MIT' file"
---

<objective>
Create `patterns/routing-tables.md` — the progressive-disclosure sub-file that holds the
signal -> deep-skill routing surface for the router skill. The file is the source of truth
for how the router maps prompt signals to specific deep skills inside the marketplace's
13 topical plugins.

Purpose: ROUTER-02 (router has a signal -> topic-skill table covering all 13 v1 plugins
with dual-load rules) and ROUTER-05 (router doubles as discovery surface — every section's
top install command plus the bottom template surface install commands for missing plugins).

Output: One new markdown file at the sidecar path with attribution header, 13 plugin-keyed
sections (~35 rows total), 6 dual-load rules, and the plugin-recommendation template.
</objective>

<execution_context>
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-router-skill-content/02-CONTEXT.md
@.planning/phases/02-router-skill-content/02-RESEARCH.md
@.planning/phases/02-router-skill-content/02-PATTERNS.md
@.planning/specs/2026-05-22-v2-router-design.md
@.planning/phases/01-plugin-mechanism-spike/01-01-SUMMARY.md
@.claude-plugin/marketplace.json

<interfaces>
<!-- Source-of-truth identifiers the executor must mirror byte-exact. Extracted from
     .claude-plugin/marketplace.json and 02-RESEARCH.md. Executor copies these verbatim. -->

Canonical 13 topical plugin names (one H3 section per name, in this order):
- hack-skills-active-directory-and-windows
- hack-skills-ai-and-supply-chain
- hack-skills-auth-bypass
- hack-skills-binary-exploitation
- hack-skills-crypto-attacks
- hack-skills-forensics-and-misc-recovery
- hack-skills-linux-and-post-exploit
- hack-skills-mobile
- hack-skills-recon
- hack-skills-server-side-execution
- hack-skills-web-client-attacks
- hack-skills-web-injection
- hack-skills-web-protocol-attacks

Canonical install command shape (no @branch suffix):
  /plugin install <plugin-name>@hack-skills-marketplace

Per-section row counts (35 total across 13 sections):
- 3 rows: active-directory-and-windows, ai-and-supply-chain, auth-bypass, forensics-and-misc-recovery, recon, server-side-execution, web-client-attacks, web-injection, web-protocol-attacks (9 sections × 3 = 27 rows)
- 2 rows: binary-exploitation, crypto-attacks, linux-and-post-exploit, mobile (4 sections × 2 = 8 rows)
- Total: 35

Signal terms and deep-skill assignments per row are the 35 proposed rows in 02-RESEARCH.md
§"Signal Terms Per Skill (High-Value Routing Targets)" (lines 738-849). Copy verbatim.

6 dual-load rule names/shapes (from 02-RESEARCH.md §"Final Dual-Load Rule Set" lines 852-895):
1. Recon + auth context
2. API testing + auth + IDOR
3. SSRF + business-flow context
4. Auth bypass + JWT/OAuth (same-plugin)
5. Business logic + race condition (same-plugin)
6. Web target, signals unclear

Header attribution paragraph (verbatim — 02-PATTERNS.md §"Pattern 3" + §"Shared Patterns"):
  Signal-to-category mappings derived from upstream `hack` SKILL.md's Signal/Priority table
  (MIT-licensed), expanded and reshaped to address this marketplace's 13-plugin topical
  structure rather than upstream's flat skill list. Each section is keyed by a marketplace
  plugin name; the install command at the top of each section doubles as the plugin-discovery
  surface when the destination plugin is not currently installed.

Plugin-recommendation template (verbatim — 02-RESEARCH.md §"Example 5", 02-PATTERNS.md §2):
  Single-plugin form:
    > **Recommended deep skill:** <skill> (in `<plugin>`, not currently installed)
    > **Install:** `/plugin install <plugin>@hack-skills-marketplace`
  Stacked dual-load form: two blocks separated by `> ` blank line, NOT merged into one comma-list.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author patterns/routing-tables.md with 13 plugin-keyed sections, 6 dual-load rules, and the plugin-recommendation template</name>
  <files>plugins/hack-skills-router/skills/hack-skills-router/patterns/routing-tables.md</files>
  <read_first>
    - plugins/hack-skills-router/skills/hack-skills-router/SKILL.md (current Phase 1 stub — confirms the parent directory exists; this task creates a new patterns/ subdir under it)
    - .claude-plugin/marketplace.json (canonical source for the 13 plugin names + 95 deep-skill paths)
    - .planning/phases/02-router-skill-content/02-RESEARCH.md lines 586-895 (the 14-plugin canonical list, the 13 plugin -> skills map, the 35 proposed rows in §"Signal Terms Per Skill", the 6 dual-load rules in §"Final Dual-Load Rule Set")
    - .planning/phases/02-router-skill-content/02-RESEARCH.md lines 990-1043 (spelling-drift catalog + the verification probes for skill names and plugin names)
    - .planning/phases/02-router-skill-content/02-PATTERNS.md lines 234-360 (pattern assignment #2 — routing-tables.md structure: header attribution, 13 plugin-keyed sections shape, dual-load rules H2, plugin-rec template H2)
    - .planning/phases/02-router-skill-content/02-CONTEXT.md §Implementation Decisions D-03 through D-07, D-15, D-16 (locked structural decisions for this file)
    - .planning/specs/2026-05-22-v2-router-design.md §5.8 (design source-of-truth outline)
  </read_first>
  <action>
    Create the file at `plugins/hack-skills-router/skills/hack-skills-router/patterns/routing-tables.md` (parent `patterns/` directory must be created — it does not yet exist). Use `mkdir -p` followed by Write tool; no heredoc / cat-EOF.

    Structure the file in this exact order:

    1. **H1 title:** `# Routing Tables`
    2. **Attribution header paragraph** (blockquote form, verbatim from `<interfaces>` block above): "Signal-to-category mappings derived from upstream `hack` SKILL.md's Signal/Priority table (MIT-licensed), expanded and reshaped to address this marketplace's 13-plugin topical structure rather than upstream's flat skill list. Each section is keyed by a marketplace plugin name; the install command at the top of each section doubles as the plugin-discovery surface when the destination plugin is not currently installed."
    3. **13 plugin-keyed H3 sections** in alphabetical/marketplace.json order. For each section produce: (a) the H3 heading `### <plugin-name>` byte-exact against marketplace.json's `.plugins[].name`; (b) an Install line under the heading: `**Install:** \`/plugin install <plugin-name>@hack-skills-marketplace\``; (c) a markdown table with header row `| Signal terms | Deep skill (in this plugin) |` and the per-section number of rows from `<interfaces>` (3 or 2). For each row, copy the signal-term list and the deep-skill directory name byte-exact from 02-RESEARCH.md §"Signal Terms Per Skill" (lines 738-849). DO NOT modify, abbreviate, or paraphrase the signal terms; DO NOT add or drop hyphens in deep-skill names; DO NOT wrap deep-skill names in backticks in the table cell (per D-04 — bare directory name).
    4. **H2 section `## Dual-load rules`** with the 6 rules from 02-RESEARCH.md §"Final Dual-Load Rule Set" (lines 852-895). For each rule produce an H3 in the form `### Rule N: <short name>` and three fields: `**Trigger:** <pattern>` on one line, `**Load both:** \`<skill-1>\` (in \`<plugin-1>\`) + \`<skill-2>\` (in \`<plugin-2>\`)` on the next, and `**Rationale:** <one-sentence justification>` on the third. The 6 rules in order: (1) Recon + auth context, (2) API testing + auth + IDOR, (3) SSRF + business-flow context, (4) Auth bypass + JWT/OAuth, (5) Business logic + race condition, (6) Web target, signals unclear. Use the wording from 02-RESEARCH.md verbatim.
    5. **H2 section `## Plugin-recommendation template`** at the bottom of the file. Open with a single-sentence framing per D-07 ("When the router selects a deep skill whose plugin isn't currently installed, surface it in this two-line shape (don't compress into one comma-line):"). Then emit (a) a single-plugin blockquote example using `jwt-oauth-token-attacks` / `hack-skills-auth-bypass` per RESEARCH §"Example 5", (b) a transition sentence ("For dual-load cases where two plugins are missing, stack two blocks — don't merge:"), (c) a stacked dual-load example using `business-logic-vulnerabilities` / `hack-skills-web-client-attacks` and `ssrf-server-side-request-forgery` / `hack-skills-server-side-execution`, exactly as shown in RESEARCH §"Example 5".

    Style constraints (enforced by acceptance criteria):
    - No backticks around deep-skill names inside the per-section tables (D-04). Backticks are used elsewhere (Install line, dual-load rule skill identifiers, template examples) to mark identifiers.
    - No `@branch` suffix on any `/plugin install` command (CLAUDE.md constraint).
    - No clickable markdown link from this file to `expert-intuitions.md` or `workflow-walkthroughs.md` — sub-file-to-sub-file links violate Pitfall 5. Prose mentions by file name are allowed.
    - File-level attribution only — no per-row or per-rule attribution annotations (D-15).
    - English only — no Chinese characters from upstream (REQUIREMENTS Out of Scope).
    - File length is the natural result of structure (estimate ~120-150 lines incl. tables and rules) — no hard cap, but no expansion beyond the locked structure.

    After writing the file, run the verification probes listed under `<verify>` to confirm correctness before considering the task done.
  </action>
  <verify>
    <automated>
ROUTER_DIR=/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/plugins/hack-skills-router/skills/hack-skills-router
F="$ROUTER_DIR/patterns/routing-tables.md"
REPO=/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace

# Probe 1: File exists
test -f "$F" || { echo "FAIL: file not created"; exit 1; }

# Probe 2: Attribution header present (yaklang/hack-skills + MIT)
grep -q 'yaklang/hack-skills' "$F" || { echo "FAIL: missing yaklang/hack-skills attribution"; exit 1; }
grep -qi 'MIT' "$F" || { echo "FAIL: missing MIT license note in attribution"; exit 1; }

# Probe 3: Exactly 13 H3 sections keyed by plugin name (no comments inside markdown, but use -v '^#' guard for safety)
SECTIONS=$(grep -v '^<!--' "$F" | grep -c '^### hack-skills-')
[ "$SECTIONS" = "13" ] || { echo "FAIL: expected 13 plugin-keyed sections, got $SECTIONS"; exit 1; }

# Probe 4: Every H3 plugin name is in canonical list (no drift, no extras)
jq -r '.plugins[].name' "$REPO/.claude-plugin/marketplace.json" | grep -v 'hack-skills-router$' | sort -u > /tmp/canonical-plugins.txt
grep -oE '^### hack-skills-[a-z-]+' "$F" | sed 's/^### //' | sort -u > /tmp/mentioned-plugins.txt
DIFF=$(comm -23 /tmp/mentioned-plugins.txt /tmp/canonical-plugins.txt)
[ -z "$DIFF" ] || { echo "FAIL: plugin-name drift in section headings: $DIFF"; exit 1; }
DIFF2=$(comm -13 /tmp/mentioned-plugins.txt /tmp/canonical-plugins.txt)
[ -z "$DIFF2" ] || { echo "FAIL: missing sections for canonical plugins: $DIFF2"; exit 1; }

# Probe 5: Every section has an Install line in canonical shape (no @branch suffix)
INSTALL_LINES=$(grep -cE '^\*\*Install:\*\* `/plugin install hack-skills-[a-z-]+@hack-skills-marketplace`$' "$F")
[ "$INSTALL_LINES" = "13" ] || { echo "FAIL: expected 13 Install lines, got $INSTALL_LINES"; exit 1; }
! grep -E '/plugin install hack-skills-[a-z-]+@hack-skills-marketplace@' "$F" || { echo "FAIL: @branch suffix detected"; exit 1; }

# Probe 6: Deep-skill names byte-exact against marketplace.json
jq -r '.plugins[].skills[]?' "$REPO/.claude-plugin/marketplace.json" | sed 's|^\./||' | sort -u > /tmp/canonical-skills.txt
# Extract column-2 entries from per-section tables: any line of form "| <signals> | <skill-name> |"
grep -oE '\| [a-z][a-z0-9-]+ \|$' "$F" | tr -d '| ' | sort -u > /tmp/table-skills.txt
DRIFT=$(comm -23 /tmp/table-skills.txt /tmp/canonical-skills.txt)
[ -z "$DRIFT" ] || { echo "FAIL: deep-skill drift in table column 2: $DRIFT"; exit 1; }

# Probe 7: 35 rows total in the per-plugin sections (count data rows: lines starting with "| " but not the header "| Signal" or separator "|---")
ROWS=$(awk '/^### hack-skills-/{flag=1; next} /^## /{flag=0} flag && /^\| [^|]* \| [a-z][a-z0-9-]+ \|/{count++} END{print count+0}' "$F")
[ "$ROWS" = "35" ] || { echo "FAIL: expected 35 routing rows total, got $ROWS"; exit 1; }

# Probe 8: 6 dual-load rules under "## Dual-load rules"
DUAL=$(awk '/^## Dual-load rules/{flag=1; next} flag && /^## /{flag=0} flag && /^### Rule [1-6]:/{count++} END{print count+0}' "$F")
[ "$DUAL" = "6" ] || { echo "FAIL: expected 6 dual-load rules, got $DUAL"; exit 1; }

# Probe 9: Plugin-recommendation template H2 present and contains both single-plugin and stacked examples
grep -q '^## Plugin-recommendation template' "$F" || { echo "FAIL: missing Plugin-recommendation template H2"; exit 1; }
grep -q "Recommended deep skill:.*jwt-oauth-token-attacks" "$F" || { echo "FAIL: missing single-plugin template example"; exit 1; }
grep -q "Recommended deep skill:.*business-logic-vulnerabilities" "$F" || { echo "FAIL: missing dual-load template example (block 1)"; exit 1; }
grep -q "Recommended deep skill:.*ssrf-server-side-request-forgery" "$F" || { echo "FAIL: missing dual-load template example (block 2)"; exit 1; }

# Probe 10: No clickable markdown link to sibling sub-files (Pitfall 5 — multi-hop links)
! grep -E '\]\((\.\.?/)?(patterns/expert-intuitions\.md|examples/workflow-walkthroughs\.md)' "$F" \
  || { echo "FAIL: clickable link to sibling sub-file detected (Pitfall 5)"; exit 1; }

echo "PASS: all 10 probes succeeded"
    </automated>
  </verify>
  <done>
    All 10 verification probes return PASS:
    1. File exists at `plugins/hack-skills-router/skills/hack-skills-router/patterns/routing-tables.md`.
    2. Attribution header cites `yaklang/hack-skills` and MIT.
    3. Exactly 13 plugin-keyed H3 sections.
    4. All 13 section names appear in `marketplace.json .plugins[].name` (zero drift, zero missing).
    5. Exactly 13 Install lines in canonical shape (no `@branch` suffix).
    6. Every column-2 deep-skill name in the per-section tables matches a `marketplace.json .plugins[].skills[]` entry.
    7. Exactly 35 data rows across the per-plugin sections.
    8. Exactly 6 dual-load rules under the `## Dual-load rules` H2.
    9. `## Plugin-recommendation template` H2 contains the single-plugin example (`jwt-oauth-token-attacks`) and the stacked dual-load example (`business-logic-vulnerabilities` + `ssrf-server-side-request-forgery`).
    10. No clickable markdown link to `expert-intuitions.md` or `workflow-walkthroughs.md`.
  </done>
</task>

</tasks>

<acceptance_criteria>
- `plugins/hack-skills-router/skills/hack-skills-router/patterns/routing-tables.md` exists.
- File opens with the `# Routing Tables` H1 and an attribution paragraph mentioning yaklang/hack-skills (MIT).
- Exactly 13 H3 sections keyed `### hack-skills-<topic>`, byte-exact against marketplace.json.
- Each section has an `**Install:** `/plugin install <name>@hack-skills-marketplace`` line.
- 35 total data rows across the 13 sections (3 rows × 9 sections + 2 rows × 4 sections).
- Every deep-skill name byte-exact against `marketplace.json .plugins[].skills[]`.
- Exactly 6 dual-load rules under `## Dual-load rules`.
- `## Plugin-recommendation template` H2 contains both single-plugin and stacked dual-load examples.
- Zero clickable markdown links to sibling sub-files (`expert-intuitions.md`, `workflow-walkthroughs.md`).
- No `@branch` suffix on any install command.
- File-level attribution only (no per-row or per-rule attribution annotations).
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router → marketplace install | Routing recommendations surface install commands that the user runs in their own shell; if a plugin name is wrong, the user invokes a missing plugin (silent UX failure, not a security incident) |
| Upstream paraphrase → router file | Content paraphrased from yaklang/hack-skills (MIT) — license/attribution boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (drift) | routing-tables.md skill/plugin names | mitigate | Per-task verification probes 4, 5, 6 — `jq` extract canonical names + `comm -23` against table contents; any drift fails the task |
| T-02-02 | Information Disclosure (license) | Paraphrased upstream content without attribution | mitigate | Header attribution paragraph naming yaklang/hack-skills + MIT — probe 2 enforces presence |
| T-02-03 | Tampering (install command shape) | Install lines on each section | mitigate | Probe 5 fails if any install command carries `@branch` suffix (CLAUDE.md constraint) |
| T-02-04 | Repudiation | Routing recommendations could be misused on unauthorized targets | accept | Out of scope for this file — the SKILL.md body (Plan 02-04) carries the Trust Model section; per design spec §3 decision 2, no forced output template here |
</threat_model>

<verification>
- Wave gate: This is a Wave 1 plan with `depends_on: []` — no upstream plan dependencies.
- Per-task automated verify: see Task 1 `<verify>` block — 10 probes covering file existence, attribution, section count, plugin-name byte-exactness, install-line shape, deep-skill byte-exactness, row count (35), dual-load rule count (6), template presence, and Pitfall 5 (sub-file linking).
- Cross-file consistency (run after all Wave 1 plans complete): the `routing-tables.md` deep-skill names mentioned in scenario rationales of `workflow-walkthroughs.md` (Plan 02-03) must also appear in this file's per-section tables. This is verified holistically by the Phase 2 checker, not gated on this plan.
</verification>

<success_criteria>
- File exists at the canonical sidecar path.
- All 10 Task 1 verification probes pass.
- File-level attribution to yaklang/hack-skills (MIT) present.
- 13 plugin-keyed sections, 35 routing rows, 6 dual-load rules, 1 plugin-recommendation template.
- Zero spelling drift against marketplace.json (jq + comm probes return zero diff).
- Zero clickable links to sibling sub-files.
</success_criteria>

<output>
Create `.planning/phases/02-router-skill-content/02-01-SUMMARY.md` when done.
</output>
