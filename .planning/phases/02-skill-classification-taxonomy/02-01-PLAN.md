---
phase: 02-skill-classification-taxonomy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-skill-classification-taxonomy/02-extract-descriptions.sh
  - .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md
autonomous: false
requirements: [GROUP-01, GROUP-02, GROUP-03, GROUP-04]

must_haves:
  truths:
    - "All 102 SKILL.md description fields extracted verbatim (no paraphrase, no truncation)"
    - "Every one of the 102 skills is assigned to exactly one bucket in the first-pass proposal (primary topical bucket OR catch-all bucket)"
    - "Each proposed bucket's first-pass member count is either within 8-15 OR explicitly flagged with a sizing-exception reason (catch-all per D-06, or sized-with-reason per D-02)"
    - "Misfit skills are clustered into themed catch-all bucket(s) per D-06 preferred shape, OR demoted to a single `hack-skills-misc` catch-all if they do not cluster, with the case documented"
    - "The proposed taxonomy (bucket names, member counts, merge/split moves, catch-all shape, borderline list with alternates) is surfaced to the user in a compact checkpoint message before the plan completes"
    - "User approval (or change requests) on the proposed taxonomy is captured in writing before Plan 02-02 may begin"
  artifacts:
    - path: ".planning/phases/02-skill-classification-taxonomy/02-extract-descriptions.sh"
      provides: "Repeatable extraction tooling — reads each /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/*/SKILL.md frontmatter, emits TSV of (skill-name, verbatim-description) pairs"
      contains: "shebang; loop over skills/*/; awk or python YAML-frontmatter extraction; 102-line output verification"
    - path: ".planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md"
      provides: "Internal intermediate state — first-pass bucket assignments + checkpoint summary; consumed by Plan 02-02 after user approval; NOT a final deliverable (note the leading `.` — dotfile signals intermediate)"
      contains: "bucket-name -> [skill-name, ...] map for all 102 skills; merge/split moves with origin notes; catch-all bucket shape; borderline list with alternate-bucket candidates per D-09"
  key_links:
    - from: "/Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/*/SKILL.md (102 files)"
      to: ".planning/phases/02-skill-classification-taxonomy/02-extract-descriptions.sh stdout"
      via: "YAML frontmatter `description: >-` folded scalar extraction (multi-line continuation until next top-level key or `---`)"
      pattern: "awk '/^---$/{flag=!flag} flag && /^description:/'"
    - from: "Extracted 102 (skill-name, description) pairs"
      to: "first-pass bucket assignment in .first-pass-classification.md"
      via: "Apply D-01 starter scaffold (recon, auth-bypass, injection, payloads, mobile, binary, ad, crypto), then D-02 resize policy, then D-03 emergent buckets"
      pattern: "every skill in extracted set MUST appear in exactly one bucket OR catch-all"
    - from: "First-pass bucket map"
      to: "User checkpoint message (D-16)"
      via: "Compact summary: bucket names + counts + merge/split notes + catch-all shape + borderline list with alternates"
      pattern: "NOT a full member-by-member dump per PATTERNS.md SPECIFICS — bucket-shape only"
---

<objective>
Extract verbatim YAML-frontmatter `description` fields from all 102 SKILL.md files in the upstream source, produce a first-pass bucket assignment using the D-01 hybrid taxonomy approach (starter buckets + D-02 resize + D-03 emergent), and surface the proposed taxonomy shape to the user at the mandatory mid-phase checkpoint (CONTEXT.md D-16) before the plan completes.

Purpose: Phase 2's load-bearing procedural pattern is the mid-phase user-review checkpoint. A first-pass-then-pause workflow catches bad bucket assignments before they harden into the committed artifacts and propagate to Phase 3. The two-plan split (this plan = first-pass + checkpoint; Plan 02-02 = finalize after approval) makes the user-approval gate structural rather than implicit.

Output:
- `.planning/phases/02-skill-classification-taxonomy/02-extract-descriptions.sh` — repeatable extraction tool committed alongside the artifacts.
- `.planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md` — internal intermediate (dotfile) capturing the bucket assignments + checkpoint summary; consumed by Plan 02-02.
- A user-facing checkpoint message presenting bucket names + counts + merge/split moves + catch-all shape + borderline list, blocking on user approval.
</objective>

<execution_context>
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase 2 source-of-truth context — load-bearing
@.planning/phases/02-skill-classification-taxonomy/02-CONTEXT.md
@.planning/phases/02-skill-classification-taxonomy/02-PATTERNS.md

# Phase 1 locked decisions carried forward
@.planning/phases/01-schema-verification/01-CONTEXT.md
@.planning/phases/01-schema-verification/01-VERIFICATION.md

<interfaces>
<!-- Verified frontmatter shape — all 102 SKILL.md files follow this pattern -->
<!-- Sampled: 401-403-bypass-techniques, recon-and-methodology, active-directory-acl-abuse, ai-ml-security -->
<!-- A `grep -l "^description: >-$" .../skills/*/SKILL.md | wc -l` over the source tree confirms the folded-scalar form is uniform -->

From /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/<skill-name>/SKILL.md (102 such files):
```yaml
---
name: <skill-name>
description: >-
  <verbatim description text, one or more indented continuation lines>
  <continuation lines end at the next top-level key OR the closing `---`>
---

# SKILL: ... (body content, NOT relevant to extraction)
```

Extraction target: the unfolded `description` value (continuation lines joined with single spaces, leading/trailing whitespace trimmed).

D-01 starter scaffold (8 buckets, from PLAN.md):
  recon, auth-bypass, injection, payloads, mobile, binary, ad, crypto

D-02 resize policy: <5 skills → merge into closest neighbor; >15 skills → split along natural subtopic axis.
D-03 emergent buckets: spawn new bucket when 8+ skills cluster around a coherent topic that does not fit existing buckets.
D-04 grouping lens: skill-content topicality (NOT purplehaze-feature-surface, NOT session-use, NOT attack-stage).
D-05/D-06 misfit handling: catch-all bucket(s) — preferred is multiple themed catch-alls (~8-15 each); fallback is single `hack-skills-misc`.
D-07 invariant: every skill appears exactly once across all buckets and catch-alls.
D-09 tiebreaker: skill's own `description` field is primary signal; RELATED ROUTING cross-references are secondary; close calls fall to executor judgment.
D-16: MANDATORY mid-phase checkpoint after first-pass assignment, BEFORE final artifacts written.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author extraction script and produce 102-skill verbatim description corpus</name>
  <files>
    .planning/phases/02-skill-classification-taxonomy/02-extract-descriptions.sh
    .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md
  </files>
  <read_first>
    - .planning/phases/02-skill-classification-taxonomy/02-CONTEXT.md (D-14 verbatim source, D-15 phase-local artifact location, code_context "frontmatter shape verified" block, SPECIFICS bullet 1 on mechanical-not-interpretive extraction)
    - .planning/phases/02-skill-classification-taxonomy/02-PATTERNS.md (Extraction-tooling pattern section with the awk pipeline; Source-repo immutability cross-cutting constraint)
    - /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/401-403-bypass-techniques/SKILL.md (first 5 lines — verifies the `---` + `name:` + `description: >-` + indented continuation + `---` frontmatter shape)
    - /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/ai-ml-security/SKILL.md (first 5 lines — confirms the same shape applies to AI/ML skill; second sample to lock the extraction regex)
    - /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/active-directory-acl-abuse/SKILL.md (first 5 lines — third sample confirming folded-scalar pattern is uniform across skill categories)
    - CLAUDE.md (source-immutability constraint: read-only access to hack-skills/skills/, NO writes back to the source tree)
  </read_first>
  <action>
    Create `02-extract-descriptions.sh` under the Phase 2 directory implementing the PATTERNS-recommended shell pipeline (PATTERNS.md "Extraction-tooling" section). Script requirements:

    1. Iterate over every directory under `/Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/` (the local sibling clone — PROJECT.md source-immutability applies, the script reads ONLY).
    2. For each `<skill-name>/SKILL.md`, extract the YAML frontmatter `description` field. Frontmatter shape: a `---`-fenced block beginning at line 1, with `name: <slug>` followed by `description: >-` and one or more indented continuation lines (folded YAML scalar). Continuation ends at the next top-level key (`^[a-z_]+:`) or the closing `---`.
    3. Unfold the folded scalar: join continuation lines with single spaces, collapse runs of whitespace, trim leading/trailing whitespace. This matches YAML scalar resolution rules per PATTERNS.md verbatim-source-convention.
    4. Emit one TSV row per skill: `<skill-name>\t<unfolded-verbatim-description>` to stdout. The 102 rows are the corpus.
    5. Add validation: script MUST exit non-zero if the row count is not exactly 102 (matches `ls /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/ | wc -l`).
    6. Add a `#!/usr/bin/env bash` shebang, `set -euo pipefail`, and a top-of-file comment block documenting: purpose, source path (absolute), output format, the 102-row invariant, and a sample one-skill TSV row showing the verbatim form.

    NEVER write derived files into the source tree (`/Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/`) — outputs go to stdout or to `.planning/phases/02-skill-classification-taxonomy/` per PATTERNS.md Source-repo immutability cross-cutting constraint and CLAUDE.md.

    After authoring the script, make it executable (`chmod +x`), run it, capture stdout to a temporary location (e.g., `/tmp/skill-descriptions.tsv`), and write the extracted corpus into `.first-pass-classification.md` as an embedded section. The `.first-pass-classification.md` file is the dotfile-prefixed intermediate (per the dotfile convention — `.task01-evidence-after-fix.txt` from Phase 1 is the precedent for intermediate phase artifacts using a leading `.`). Its initial content should be:

    - YAML frontmatter capturing: `phase`, `plan: 01`, `status: corpus_extracted`, `total_skills: 102`, `extraction_date: <ISO date>`, `extraction_script: 02-extract-descriptions.sh`.
    - A `## Extracted Corpus` section with a TSV-style or pipe-table dump of all 102 (skill-name, description) pairs in alphabetical order by skill-name. Use the verbatim description text — no editing, no paraphrasing per CONTEXT.md D-14.

    The bucket assignments will be appended to this same file in Task 2; do NOT attempt bucket assignment in this task.
  </action>
  <verify>
    <automated>
      bash .planning/phases/02-skill-classification-taxonomy/02-extract-descriptions.sh | wc -l
      # MUST output exactly: 102

      bash .planning/phases/02-skill-classification-taxonomy/02-extract-descriptions.sh | awk -F'\t' 'NF != 2 {print "MALFORMED ROW:", $0; exit 1} END {print "OK", NR, "rows"}'
      # MUST output: OK 102 rows

      # Verify a known-good extraction against the source file (verbatim, no truncation):
      EXPECTED='401/403 bypass playbook. Use when encountering access-denied responses on admin panels, API endpoints, or restricted paths. Covers path manipulation, HTTP method tampering, header injection, protocol downgrade, and automated bypass tools.'
      ACTUAL=$(bash .planning/phases/02-skill-classification-taxonomy/02-extract-descriptions.sh | awk -F'\t' '$1 == "401-403-bypass-techniques" {print $2}')
      [ "$ACTUAL" = "$EXPECTED" ] && echo "VERBATIM OK" || { echo "VERBATIM MISMATCH"; diff <(echo "$EXPECTED") <(echo "$ACTUAL"); exit 1; }

      # Verify the corpus is embedded in the intermediate file at the expected count:
      grep -c '^| [a-z0-9-]\+ |' .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md
      # OR (if TSV embedded in a fenced block) grep -c $'^[a-z0-9-]\\+\\t' on the embedded TSV content
      # Either way, the embedded count MUST equal 102.

      # Confirm source tree is untouched (no derived files written into hack-skills/skills/):
      find /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/ -name "*.tsv" -o -name "*.json" -o -name "*.classification*" | wc -l
      # MUST output: 0
    </automated>
  </verify>
  <acceptance_criteria>
    - `02-extract-descriptions.sh` exists at `.planning/phases/02-skill-classification-taxonomy/02-extract-descriptions.sh`, is executable, and runs without error.
    - Script output is exactly 102 TSV rows, each with 2 tab-separated fields (skill-name, unfolded-verbatim-description).
    - Extracted description for `401-403-bypass-techniques` is character-for-character identical to the upstream SKILL.md frontmatter `description` field (no truncation, no paraphrase, whitespace-normalized per YAML folded-scalar rules).
    - `.first-pass-classification.md` exists with `status: corpus_extracted` frontmatter, `total_skills: 102`, and embeds all 102 (skill-name, description) pairs in alphabetical order by skill-name.
    - Zero derived files written into `/Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/` (source-immutability invariant).
  </acceptance_criteria>
  <done>
    `02-extract-descriptions.sh` exists, is executable, runs without error, and emits exactly 102 TSV rows. The extracted `description` for `401-403-bypass-techniques` matches the upstream verbatim text exactly (CONTEXT.md D-14 invariant). `.first-pass-classification.md` exists with `status: corpus_extracted` frontmatter and embeds all 102 (skill-name, description) pairs in alphabetical order. The source tree at `/Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/` has zero derived files written into it.
  </done>
</task>

<task type="auto">
  <name>Task 2: First-pass bucket assignment with D-01 hybrid taxonomy + D-02 resize + D-03 emergent</name>
  <files>
    .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md
  </files>
  <read_first>
    - .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md (the extracted corpus from Task 1 — the 102 verbatim descriptions are the input to assignment)
    - .planning/phases/02-skill-classification-taxonomy/02-CONTEXT.md (D-01 hybrid derivation with the 8 starter buckets `recon`, `auth-bypass`, `injection`, `payloads`, `mobile`, `binary`, `ad`, `crypto`; D-02 resize policy `<5 merge, >15 split`; D-03 emergent-bucket threshold `8+ clustering skills`; D-04 grouping lens `skill-content topicality`; D-05 misfit criteria `(a) standalone (b) too generic/redundant`; D-06 catch-all preferred shape `multiple themed > single misc`; D-07 every-skill-exactly-once invariant; D-08 hard assignment; D-09 tiebreaker `skill's own description is primary signal`; D-10 `also_relevant_to` metadata for close calls)
    - .planning/phases/02-skill-classification-taxonomy/02-PATTERNS.md (Bucket-naming convention section: kebab-case, single-word where possible, multi-word with hyphens; catch-all naming guidance; Mid-phase user-review checkpoint section for the format requirements)
    - /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/ (only `ls` — confirms 102-directory total; per-skill SKILL.md body reads are OPTIONAL and only justified for genuine D-09 tiebreaker cases via RELATED ROUTING cross-references)
  </read_first>
  <action>
    Using the verbatim 102-skill corpus already embedded in `.first-pass-classification.md` from Task 1, produce a first-pass bucket assignment by applying the CONTEXT.md decisions in this exact order:

    1. **Seed with D-01 starter scaffold**: Initialize 8 candidate buckets from PLAN.md — `recon`, `auth-bypass`, `injection`, `payloads`, `mobile`, `binary`, `ad`, `crypto`. Bucket names are working keys; per D-02 they may diverge from these starters as resize moves happen.

    2. **First-pass assign** every one of the 102 skills to exactly one starter bucket using D-09's primary signal — the skill's own `description` text just extracted in Task 1. Reading SKILL.md bodies is NOT required for the first pass; the description field is the canonical input per D-09 + D-14. If a skill genuinely does not fit any of the 8 starters, set it aside in a `misfit-pool` working list — do NOT force it into a bucket. (Per D-05, misfits split into (a) standalone or (b) redundant; tagging this distinction now will help D-06 catch-all bucket shape decisions in step 5.)

    3. **Apply D-02 resize moves**:
       - For any bucket with `<5` members: merge into the closest topically-adjacent neighbor and rename the absorbed bucket to whatever describes the merged content (e.g., `crypto` (4) + `payloads` (12) → keep `payloads` if crypto skills are payload-shaped, OR rename to `payloads-and-crypto` if the merge changes the bucket's scope meaningfully — pick the name that matches the merged content's actual topicality per D-04).
       - For any bucket with `>15` members: split along the natural subtopic axis. PATTERNS.md and CONTEXT.md SPECIFICS bullet 2 give examples like `injection` → `web-injection` + `protocol-injection` if injection over-fills. Document the split axis as a one-line note (e.g., "split by injection target: web vs. protocol-level").
       - Record EVERY resize move (origin bucket, destination bucket(s), reason) as a `merge_moves` and `split_moves` entry list — these are presented at the checkpoint per D-16 and committed to the final artifacts in Plan 02-02.

    4. **Apply D-03 emergent-bucket allowance**: If the `misfit-pool` from step 2 contains 8+ skills clustering around a coherent topic not covered by the starters (e.g., `cloud/container/k8s`, `supply-chain`, `forensics`, `ai-ml-security` are the candidates CONTEXT.md SPECIFICS bullet 2 flagged), spawn a new bucket for that cluster. Name it kebab-case per PATTERNS.md Bucket-naming convention. Move the clustered skills out of `misfit-pool` and into the new bucket. Repeat until no 8+ clusters remain in `misfit-pool`.

    5. **Apply D-06 catch-all strategy** to whatever remains in `misfit-pool`:
       - **Preferred**: split the remaining misfits into 2-3 themed catch-all buckets each aiming for ~8-15 (e.g., `crypto-attacks` if crypto-flavored isolated skills cluster, `forensics-and-recovery` if a forensics cluster exists). Themed catch-alls are still buckets — they get a `description` and become marketplace plugins in Phase 3.
       - **Fallback**: if the remainder genuinely does not cluster into 2+ themed groups, demote to a single `hack-skills-misc` bucket. Note in the merge log which case applied.
       - Every misfit MUST end up in some catch-all bucket per D-06 + D-07 — none are dropped from marketplace.json.

    6. **Identify borderline skills** (D-09 close calls): walk through every assigned skill and flag any where the description is genuinely ambiguous between two buckets (NOT every skill with tangential overlap — per CONTEXT.md SPECIFICS bullet 5, `also_relevant_to` should be rare). For each borderline, record the current assignment AND the candidate alternate bucket. These become the `borderline list` surfaced at the D-16 checkpoint and the `also_relevant_to` metadata in Plan 02-02's final JSON per D-10.

    7. **Apply D-07 invariant check**: assert that every one of the 102 skills appears in exactly one bucket across primary topical buckets + themed catch-alls + single misc fallback (if used). Zero duplicates. Zero omissions. If the check fails, fix the assignment and re-check.

    Append all of this to `.first-pass-classification.md` (do NOT overwrite the Task 1 corpus section). New sections to add:

    - `## First-Pass Buckets` — one subsection per bucket with: bucket name, working description (one-line, gerund/noun-phrase tone per PATTERNS.md `02-CLASSIFICATION.json` analog of marketplace.json description-field tone), member list (skill names only — full verbatim descriptions stay in the corpus section), member count, and a sizing-status flag (`within 8-15`, `catch-all per D-06`, or `sized-outside-range with reason`).
    - `## Merge/Split Moves` — flat list of every resize action: `(origin → destination, reason)`. Empty list is valid if no resize was needed.
    - `## Emergent Buckets (D-03)` — flat list of any new buckets spawned beyond the 8 starters, with the 8+ cluster justification. Empty list is valid if no emergent buckets were needed.
    - `## Catch-all Shape (D-06)` — explicit statement of which D-06 case applied (`multiple themed` vs. `single misc`), the catch-all bucket name(s), and the per-member reason tag (`standalone` or `redundant` per D-05) per misfit.
    - `## Borderline List (D-09)` — table of close-call skills with columns: `skill-name`, `assigned-bucket`, `candidate-alternate`, `reason-for-choice` (one-line referencing D-09 signal — the skill's own description framing).
    - `## D-07 Invariant Check` — `total assigned: 102 | duplicates: 0 | unassigned: 0` with a one-line note on how this was verified (e.g., a small awk or python script counting bucket-membership entries).

    Update the frontmatter `status` field from `corpus_extracted` to `first_pass_complete` and add a `bucket_count: <N>` field reflecting the final bucket count (after merges + splits + emergent + catch-alls).
  </action>
  <verify>
    <automated>
      # Status flag advanced to first_pass_complete:
      grep -E '^status: first_pass_complete$' .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md
      # MUST match exactly one line.

      # bucket_count field present and is a positive integer:
      grep -E '^bucket_count: [0-9]+$' .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md
      # MUST match exactly one line.

      # Required sections all present:
      for section in "## First-Pass Buckets" "## Merge/Split Moves" "## Emergent Buckets" "## Catch-all Shape" "## Borderline List" "## D-07 Invariant Check"; do
        grep -F "$section" .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md > /dev/null \
          && echo "FOUND: $section" \
          || { echo "MISSING: $section"; exit 1; }
      done

      # D-07 invariant check section claims 102 / 0 / 0:
      grep -E 'total assigned:[[:space:]]*102' .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md
      grep -E 'duplicates:[[:space:]]*0' .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md
      grep -E 'unassigned:[[:space:]]*0' .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md
      # All three MUST match.

      # Independent count verification — extract bucket-member listings and confirm union covers all 102 skills exactly once.
      # (Implementation: a small inline awk/python script that parses the "## First-Pass Buckets" section,
      # collects all skill-names listed across all bucket subsections, asserts:
      #   sort -u skills_extracted_from_buckets == sort `ls /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/`
      # AND assert no skill appears in 2+ buckets.)
      # This is the load-bearing audit for ROADMAP success criterion #5 (D-07).
      # The exact script form is executor's choice — what matters is the assertion holds.

      # Source tree still untouched:
      find /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/ -newer .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md -type f 2>/dev/null | wc -l
      # SHOULD be 0 (nothing in source tree modified after the intermediate was written).
    </automated>
  </verify>
  <acceptance_criteria>
    - `.first-pass-classification.md` frontmatter status advanced from `corpus_extracted` to `first_pass_complete`, with a `bucket_count: <N>` field added.
    - All 6 required sections present in the file: `## First-Pass Buckets`, `## Merge/Split Moves`, `## Emergent Buckets`, `## Catch-all Shape`, `## Borderline List`, `## D-07 Invariant Check`.
    - D-07 invariant explicitly stated and verified: `total assigned: 102 | duplicates: 0 | unassigned: 0`, with an independent count check (union of bucket memberships set-equal to `ls hack-skills/skills/`).
    - Every starter bucket from D-01 either appears in the final bucket set OR has its absorption/split recorded in the merge/split log.
    - Every misfit skill is routed to a catch-all bucket per D-06; misfits are NOT dropped.
    - Borderline list is a focused table (not exhaustive — `also_relevant_to` rare per CONTEXT.md SPECIFICS bullet 5).
    - Source tree at `/Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/` is unchanged (immutability invariant holds).
  </acceptance_criteria>
  <done>
    `.first-pass-classification.md` has `status: first_pass_complete` and a positive integer `bucket_count`. All six required sections (`First-Pass Buckets`, `Merge/Split Moves`, `Emergent Buckets`, `Catch-all Shape`, `Borderline List`, `D-07 Invariant Check`) exist and are populated. The D-07 invariant is verified independently — the union of bucket memberships exactly equals the 102 skill directories in the source tree, with zero duplicates. Every D-XX referenced in the action above has a corresponding decision artifact in the file (resize moves, emergent buckets list, catch-all shape declaration, borderline table). No file under `hack-skills/skills/` was modified.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mid-phase user-review checkpoint (D-16) — BLOCKING</name>
  <files>
    .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md
  </files>
  <read_first>
    - .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md (the complete first-pass state produced by Task 2 — bucket map, merge/split moves, catch-all shape, borderline list, D-07 invariant check)
    - .planning/phases/02-skill-classification-taxonomy/02-CONTEXT.md D-16 (mid-phase user-review checkpoint requirement — MANDATORY, non-skippable)
    - .planning/phases/02-skill-classification-taxonomy/02-CONTEXT.md SPECIFICS bullet 4 (checkpoint format: "compact — bucket name + member count + any merge/split notes + the borderline list with alternates. Not the full member-by-member dump.")
    - .planning/phases/02-skill-classification-taxonomy/02-PATTERNS.md "Mid-phase user-review checkpoint" section (the only Phase-2-specific procedural pattern; structural requirements for how the checkpoint is surfaced + how user approval is captured in writing)
  </read_first>
  <action>
    THIS IS A BLOCKING HUMAN-VERIFY CHECKPOINT per CONTEXT.md D-16. The executor pauses here and surfaces a compact taxonomy summary directly in the assistant message — the executor does NOT proceed to Plan 02-02 until the user explicitly responds with approval. The reason this step requires a human and cannot be automated: bucket assignments are inherently judgment-shaped; a user-review gate before artifact-write is the documented mechanism for catching bad calls before they harden into the committed CLASSIFICATION.md / CLASSIFICATION.json and propagate to Phase 3's marketplace.json plugin entries.

    Context that has been produced for the user's review (Tasks 1 and 2 already completed):
    - `02-extract-descriptions.sh` has produced 102 verbatim descriptions — committed in `.first-pass-classification.md` Extracted Corpus section.
    - First-pass bucket assignment has applied D-01 starter scaffold + D-02 resize + D-03 emergent + D-06 catch-all routing — captured in the `## First-Pass Buckets`, `## Merge/Split Moves`, `## Emergent Buckets`, `## Catch-all Shape`, `## Borderline List` sections of `.first-pass-classification.md`.
    - D-07 invariant (every-skill-exactly-once) is verified — captured in the `## D-07 Invariant Check` section.

    Executor steps to perform IN THE ASSISTANT MESSAGE before pausing:

    1. Compose a compact checkpoint message containing exactly these 5 elements in order, sourced directly from `.first-pass-classification.md` (do NOT ask the user to open the file — surface the summary in the message itself):

       a. **Bucket overview table** with columns: `bucket-name | member-count | sizing-status | description`. One row per bucket including all primary topical buckets, all themed catch-alls, and `hack-skills-misc` if it exists. `sizing-status` is one of: `within 8-15`, `catch-all per D-06`, or `sized-with-reason: <reason>`. Final row reads `TOTAL: 102 across N buckets`.

       b. **Merge/Split moves** — bulleted list, one bullet per resize action from the `## Merge/Split Moves` section of the intermediate. Format: `- merged <bucket-A> (<N> skills) into <bucket-B> because <reason>` or `- split <bucket-A> (<N> skills) into <bucket-B> + <bucket-C> along <axis>`. Empty list is a valid outcome and MUST be stated explicitly: `- No resize moves needed — all 8 starter buckets landed within 8-15.`

       c. **Emergent buckets (D-03)** — bulleted list, one bullet per new bucket beyond the 8 starters from the `## Emergent Buckets` section. Format: `- <new-bucket-name>: <N> skills clustering around <coherent topic>; justified per D-03 8+ threshold`. Empty list is valid and MUST be stated explicitly: `- No emergent buckets — all 102 skills fit within the 8 starters + catch-alls.`

       d. **Catch-all shape (D-06)** — single sentence summarizing the `## Catch-all Shape` section: either `Multiple themed catch-alls applied: <bucket-name-1>, <bucket-name-2>, ...` or `Single hack-skills-misc fallback applied (misfits did not cluster into 2+ themed groups).`

       e. **Borderline list (D-09)** — markdown table of close-call skills only from the `## Borderline List` section. Columns: `skill-name | assigned-bucket | candidate-alternate | one-line-reason`. Empty list is valid and MUST be stated explicitly: `No borderline cases — every assignment had an unambiguous primary signal per D-09.`

    2. End the message with this exact prompt block:

       ```
       Reviewing this first-pass taxonomy. Respond with one of:

       - **approved** — proceed to Plan 02-02 (write final CLASSIFICATION.md + CLASSIFICATION.json from this state).
       - **approved with edits: <specific changes>** — list the exact bucket-name renames, skill moves, or split/merge revisions you want. I will apply them, re-run the D-07 invariant, and re-surface the updated summary for re-approval before Plan 02-02 begins.
       - **needs rework: <reasons>** — explain what about the first-pass shape is wrong (e.g., bucket lens is off, catch-all strategy mismatches, missed clustering). I will re-open Task 2 and re-iterate.
       ```

    3. PAUSE. Do NOT proceed to write `02-CLASSIFICATION.md`, `02-CLASSIFICATION.json`, or any Plan 02-02 file. Do NOT advance `.first-pass-classification.md` status. Wait for the user's response in the next message.

    4. On receiving the user's response:
       - If **approved**: Append a `## Checkpoint Approval` section to `.first-pass-classification.md` containing the user's verbatim response message + the ISO timestamp. Update the frontmatter `status` field from `first_pass_complete` to `checkpoint_approved`. This task then completes; the orchestrator can proceed to Plan 02-02.
       - If **approved with edits**: Apply the requested edits to the bucket map within `.first-pass-classification.md` (move skills between buckets, rename buckets, adjust merges/splits as specified). Re-run the D-07 invariant check after edits — assert union(buckets) still equals 102 unique skills, zero duplicates. Re-surface the updated bucket overview table + merge/split moves + emergent buckets + catch-all shape + borderline list in a new assistant message. Re-pause for re-approval. Do NOT advance `status` to `checkpoint_approved` until the user gives unqualified approval on the edited state.
       - If **needs rework**: Re-open Task 2 with the user's feedback (e.g., if user says "the lens is wrong, group by attack surface not skill topic," that conflicts with locked D-04 and requires escalation — surface the conflict between the user's feedback and the locked decision rather than silently overriding D-04). After re-iteration, re-surface the updated summary, re-pause for approval.

    5. Once `status: checkpoint_approved` is set and the `## Checkpoint Approval` section is recorded, this task is done. Plan 02-02's `depends_on: ['02-01']` chain structurally ensures it cannot begin until this state is reached.

    Do NOT skip the pause. Do NOT proceed to Plan 02-02 with `status: first_pass_complete` — that state means "first-pass done, user has NOT yet reviewed." The dotfile intermediate `.first-pass-classification.md` is the load-bearing precondition for Plan 02-02 Task 1.
  </action>
  <verify>
    <human-check>
      User has reviewed the checkpoint summary message produced by the executor (containing all 5 required elements above), responded with one of `approved` / `approved with edits: ...` / `needs rework: ...`, and (on final approval, after any iteration) confirms that the taxonomy in `.first-pass-classification.md` reflects their intent. Verifies that the user response message is captured verbatim in the `## Checkpoint Approval` section.
    </human-check>
    <automated>
      # Frontmatter status MUST advance to checkpoint_approved only after explicit user approval was captured
      grep -E '^status: checkpoint_approved$' .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md
      # MUST match exactly one line.

      # Checkpoint Approval section MUST exist with the user's verbatim response
      grep -E '^## Checkpoint Approval$' .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md
      # MUST match.

      # If user approved with edits, the D-07 invariant MUST still hold after edits.
      # Independent re-verification: extract the final bucket-member lists from `## First-Pass Buckets`,
      # confirm union equals `ls /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/` and total count == 102.
      # (Executor runs this check inline after applying any edits, before re-surfacing the summary for re-approval.)

      # Source tree still untouched throughout the iteration loop
      find /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/ -newer .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md -type f 2>/dev/null | wc -l
      # SHOULD be 0.
    </automated>
  </verify>
  <acceptance_criteria>
    - The checkpoint summary message presented to the user contains all 5 required elements (bucket overview table, merge/split moves, emergent buckets, catch-all shape, borderline list) in compact form (no full per-skill member dump — that lives in the master table written by Plan 02-02).
    - The user's response (approved / approved-with-edits / needs-rework) is captured verbatim in `.first-pass-classification.md` `## Checkpoint Approval` section.
    - `.first-pass-classification.md` frontmatter advances to `status: checkpoint_approved` ONLY after the user explicitly approves the (possibly edited) first-pass state. Bare `first_pass_complete` is NOT sufficient.
    - If user responded "approved with edits": all requested edits are applied to the bucket map, the D-07 invariant is re-verified after edits, AND the updated summary is re-surfaced for re-approval before this task completes.
    - If user responded "needs rework": this task does NOT complete until a subsequent approval is captured after re-iteration. Plan 02-02 MUST NOT begin until `status: checkpoint_approved` is recorded.
    - Source tree at `/Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/` is untouched throughout (source-immutability invariant).
  </acceptance_criteria>
  <done>
    `.first-pass-classification.md` has `status: checkpoint_approved` (set ONLY after explicit user approval) and a `## Checkpoint Approval` section containing the user's verbatim response. If any edit iterations occurred, the D-07 invariant was re-verified after each edit cycle. The dotfile intermediate is the load-bearing input for Plan 02-02 Task 1 — Plan 02-02's `depends_on: ['02-01']` chain is the structural enforcement that no artifact-write begins until this task completes.
  </done>
  <resume-signal>Type "approved", "approved with edits: ...", or "needs rework: ..." — Plan 02-02 will not begin without an explicit approval recorded in writing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source repo (`hack-skills/skills/`) → extraction script | Read-only boundary. Source is third-party content treated as untrusted text input (per PROJECT.md source-immutability), but the consumed surface is YAML frontmatter which is metadata-only and never executed. |
| Extraction script stdout → `.first-pass-classification.md` | Internal-to-this-phase write boundary. The script writes only to phase-local files under `.planning/phases/02-skill-classification-taxonomy/`. No file outside this directory or `/tmp/` is modified. |
| First-pass bucket map → user checkpoint message | Trust-elevation boundary: the user sees a summary derived from the script output + Claude's bucket assignment reasoning, and grants approval to proceed. No automation can bypass this gate per D-16. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `02-extract-descriptions.sh` accidental write into source tree | mitigate | Script uses ONLY stdout for output; redirection target is `/tmp/` or `.planning/phases/02-.../`. Test command in Task 1 verify block asserts `find /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/ -name "*.tsv" -o -name "*.json" -o -name "*.classification*" | wc -l == 0`. PROJECT.md source-immutability is the upstream control. |
| T-02-02 | Information disclosure | Verbatim descriptions leak content that should be paraphrased or redacted | accept | Skill descriptions are already public on `github.com/yaklang/hack-skills`. Verbatim reproduction in `.planning/` (a private working tree until Phase 4) is no exposure increase. CONTEXT.md D-14 actively requires verbatim. |
| T-02-03 | Repudiation | Mid-phase checkpoint approval not durably recorded | mitigate | D-16 + this plan's Task 3 acceptance criteria require the approval to be captured in writing in `.first-pass-classification.md` `## Checkpoint Approval` section. Plan 02-02 cannot begin without it — the `depends_on: [02-01]` chain enforces structurally, plus the `status: checkpoint_approved` frontmatter flag is Plan 02-02 Task 1's precondition check. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | NO new package-manager installs in this phase. Extraction tooling uses Bash + `awk` (POSIX-standard, already on the system). Python (if used as a fallback for folded-scalar edge cases) uses the stdlib `yaml` module if `pyyaml` is already installed, or stays in pure Bash otherwise. No third-party packages introduced. Package Legitimacy Gate not applicable. |

Phase 2's threat surface is genuinely minimal: it produces only `.planning/`-tree planning artifacts, runs no code beyond a single Bash script over read-only local files, introduces no new dependencies, exposes no network surface, and the only user-facing interaction is the D-16 checkpoint message (no input is parsed beyond approval signal text). The register above documents the surface honestly rather than fabricating threats.
</threat_model>

<verification>
After all three tasks complete:

1. `02-extract-descriptions.sh` is committed, executable, and emits exactly 102 TSV rows.
2. `.first-pass-classification.md` is committed with `status: checkpoint_approved`, all six required sections populated, and the D-07 invariant (every skill assigned exactly once) verified.
3. The source tree at `/Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/` is unchanged (PROJECT.md source-immutability invariant).
4. User approval of the first-pass taxonomy is captured in writing as a `## Checkpoint Approval` section in `.first-pass-classification.md`. Plan 02-02 may consume this approval as its precondition.
</verification>

<success_criteria>
This plan completes the first half of Phase 2's GROUP-01 / GROUP-02 / GROUP-03 / GROUP-04 requirements:

- GROUP-01: 102 verbatim descriptions extracted (corpus complete; embedded in `.first-pass-classification.md` and re-derivable from `02-extract-descriptions.sh`).
- GROUP-02: Bucket taxonomy proposed with rationale (per-bucket descriptions in `## First-Pass Buckets`; merge/split rationale in `## Merge/Split Moves`).
- GROUP-03: 8-15 sizing applied via D-02 resize moves; exceptions explicitly flagged with reasons in the sizing-status column.
- GROUP-04: Misfit skills routed to themed catch-all(s) or a single `hack-skills-misc` per D-06; reasons captured per D-05 (`standalone` vs `redundant`).

These requirements are NOT fully closed until Plan 02-02 writes the final `02-CLASSIFICATION.md` and `02-CLASSIFICATION.json`. This plan produces the verified, user-approved input that Plan 02-02 finalizes.
</success_criteria>

<output>
Create `.planning/phases/02-skill-classification-taxonomy/02-01-SUMMARY.md` when this plan completes. The SUMMARY should record:
- Whether the user approved the first-pass taxonomy on the first checkpoint, or required edits / rework iterations (and how many).
- Final bucket count + names + member counts after any user-requested edits.
- The location of the checkpoint approval (`.first-pass-classification.md` `## Checkpoint Approval` section reference) — Plan 02-02 frontmatter `checkpoint_approval_signal` field references this exact location.
- A pointer to `.first-pass-classification.md` as the canonical first-pass state for Plan 02-02 to consume.
</output>
</content>
</invoke>