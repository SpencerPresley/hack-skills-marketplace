---
phase: 02-skill-classification-taxonomy
plan: 02
type: execute
wave: 2
depends_on: ['02-01']
files_modified:
  - .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md
  - .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json
autonomous: true
requirements: [GROUP-01, GROUP-02, GROUP-03, GROUP-04]

must_haves:
  truths:
    - "A human-readable classification artifact `02-CLASSIFICATION.md` exists listing all 102 skills with verbatim one-line summaries sourced from each SKILL.md's `description` field"
    - "A machine-readable classification artifact `02-CLASSIFICATION.json` exists with bucket-keyed top-level structure consumable by Phase 3 without ambiguity"
    - "Each proposed group has a documented scope (rationale paragraph in CLASSIFICATION.md, `description` field in CLASSIFICATION.json) explaining what unifies its skills"
    - "Each proposed group's membership is within 8-15 OR is explicitly flagged as a catch-all bucket (D-06) OR is sized outside range with documented reason (D-02), with the flag visible in both files"
    - "Misfit skills are listed in a catch-all bucket (themed per D-06 preferred, or single `hack-skills-misc` fallback) with per-member reason tags (`standalone` or `redundant` per D-05) — D-06 reframes 'excluded from groups' as 'assigned to catch-all bucket'"
    - "Every one of the 102 skills appears in the classification artifact pair exactly once — verified by D-07 invariant check committed inside both files"
    - "Skill paths in `02-CLASSIFICATION.json` use the root-level `./<skill-name>` form locked by Phase 1 D-03-REVISED — drop-in ready for Phase 3 to lift verbatim into marketplace.json plugin `skills` arrays"
    - "Master 102-row table at the bottom of `02-CLASSIFICATION.md` provides ctrl-F lookup across all skills with columns name | summary | assigned_bucket | also_relevant_to"
    - "The 5 ROADMAP Phase 2 success criteria are explicitly checked off in a closing section of `02-CLASSIFICATION.md`, mirroring the precedent set by 01-VERIFICATION.md lines 181-191"
  artifacts:
    - path: ".planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md"
      provides: "Human-readable classification artifact — per-bucket sections, catch-all section, master 102-row table, decision log, Roadmap success criteria check"
      contains: "YAML frontmatter (status, phase, classified date, requirements, total_skills, bucket_count, catch_all_shape, sized_within_8_15, mid_phase_checkpoint, checkpoint_approval_signal); Summary block at top; one `##` section per bucket with verbatim member descriptions; Catch-all section per D-06; Master 102-row table; Decision log per D-12; Roadmap Success Criteria check per PATTERNS analog"
      min_lines: 200
    - path: ".planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json"
      provides: "Machine-readable classification artifact consumed by Phase 3's executor to author marketplace.json plugin entries"
      contains: "bucket-keyed top-level object; each bucket has `description` (one-line, gerund/noun-phrase tone), `skills` (array of root-level `./<skill-name>` paths), `notes` (sizing/catch-all/merge-origin metadata, empty string if none); top-level `excluded: []` for schema completeness per D-13; top-level `skill_metadata` map with per-skill `also_relevant_to` cross-references per D-10; optional top-level `_meta` block with date, total skill count, bucket count, checkpoint approval marker"
  key_links:
    - from: ".first-pass-classification.md (Plan 02-01 user-approved state)"
      to: "02-CLASSIFICATION.md per-bucket sections + master table + decision log"
      via: "Per-bucket section per bucket from approved state; member list rendered with verbatim descriptions joined from the corpus section; master table is one row per skill from the corpus, columns from the assignment state"
      pattern: "no Claude-authored one-liners — every per-skill `summary` is from the corpus per D-14"
    - from: ".first-pass-classification.md (Plan 02-01 user-approved state)"
      to: "02-CLASSIFICATION.json bucket-keyed structure"
      via: "Each bucket from approved state becomes a top-level key; `description` derived from the bucket's working description in the approved state; `skills` array uses `./<skill-name>` root-level form per Phase 1 D-03-REVISED; `notes` field populated from sizing-status flag and merge/split origin"
      pattern: "JSON shape per CONTEXT.md D-13 — `excluded: []` reserved at top level for schema completeness even if empty under D-06; `skill_metadata` as separate top-level map per PATTERNS.md Override section recommendation"
    - from: "02-CLASSIFICATION.json `skills` arrays"
      to: "Phase 3 marketplace.json plugin entries' `skills` arrays"
      via: "Drop-in copy — root-level `./<skill-name>` paths match marketplace.json schema requirement locked by Phase 1 D-03-REVISED"
      pattern: "every path leads with `./`; no `./skills/` prefix; matches upstream directory name verbatim — verified by `ls /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/` membership"
---

<objective>
After the user has approved the first-pass taxonomy at Plan 02-01's mid-phase checkpoint (D-16), translate the approved state in `.first-pass-classification.md` into the two committed final artifacts: `02-CLASSIFICATION.md` (human-readable per CONTEXT.md D-12 layout) and `02-CLASSIFICATION.json` (machine-readable per D-13 bucket-keyed shape). Phase 3's executor consumes the JSON directly to author marketplace.json plugin entries.

Purpose: This plan is the artifact-finalization half of Phase 2. By depending on Plan 02-01 (which gated on user approval), it inherits the checkpoint's structural guarantee — no premature artifact write before the user reviewed the taxonomy. The two-plan split keeps the user-approval gate visible in the GSD dependency graph (`depends_on: ['02-01']`).

Output:
- `.planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md` — committed human-readable classification with per-bucket sections, catch-all section, master 102-row table, decision log, Roadmap success criteria check.
- `.planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json` — committed machine-readable classification, bucket-keyed, ready for Phase 3 to consume.
</objective>

<execution_context>
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase 2 source-of-truth context — load-bearing
@.planning/phases/02-skill-classification-taxonomy/02-CONTEXT.md
@.planning/phases/02-skill-classification-taxonomy/02-PATTERNS.md

# Plan 02-01 output (precondition — user approval must already be captured)
@.planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md
@.planning/phases/02-skill-classification-taxonomy/02-01-SUMMARY.md

# Structural analogs to borrow from
@.planning/phases/01-schema-verification/01-VERIFICATION.md
@.claude-plugin/marketplace.json

<interfaces>
<!-- CLASSIFICATION.md analog: 01-VERIFICATION.md (see PATTERNS.md for full borrow/override analysis) -->
<!-- Borrow: YAML frontmatter at top, Summary block, verbatim-evidence convention, Roadmap success criteria check at bottom -->
<!-- Override (per CONTEXT.md D-12): body axes — per-bucket sections + catch-all + master table + decision log (NOT VERIFY-NN per-question sections) -->

CLASSIFICATION.md frontmatter shape (adapted from 01-VERIFICATION.md):
```yaml
---
status: passed
phase: 02-skill-classification-taxonomy
classified: <ISO date>
requirements: GROUP-01, GROUP-02, GROUP-03, GROUP-04
total_skills: 102
bucket_count: <N from approved state>
catch_all_shape: multiple_themed | single_misc   # per D-06 outcome
sized_within_8_15: <true | "true with documented exceptions in <bucket-name(s)> per D-02">
mid_phase_checkpoint: completed   # per D-16
checkpoint_approval_signal: <reference to where user approval was recorded — file:section OR discussion-log ID>
---
```

<!-- CLASSIFICATION.json analog: .claude-plugin/marketplace.json (current Phase 1 output) -->
<!-- Borrow verbatim: skill-path convention (root-level `./<skill-name>` per Phase 1 D-03-REVISED), description-field tone (one-line, gerund/noun-phrase, ~3-6 words, no period) -->
<!-- Override (per CONTEXT.md D-13): outer container — bucket-keyed object (NOT flat `plugins: [...]` array) -->

CLASSIFICATION.json shape (per CONTEXT.md D-13 + PATTERNS.md Override):
```json
{
  "_meta": {
    "phase": "02-skill-classification-taxonomy",
    "classified": "<ISO date>",
    "total_skills": 102,
    "bucket_count": <N>,
    "checkpoint_approval_signal": "<reference matching CLASSIFICATION.md frontmatter>"
  },
  "<bucket-name-1>": {
    "description": "<one-line gerund/noun-phrase scope>",
    "skills": ["./<skill-name-1>", "./<skill-name-2>", ...],
    "notes": "<sizing exception, catch-all flag, merge/split origin — empty string if none>"
  },
  "<bucket-name-2>": { ... },
  "excluded": [],
  "skill_metadata": {
    "<skill-name>": { "also_relevant_to": "<other-bucket-name>" }
  }
}
```

Skill-path convention (locked by Phase 1 D-03-REVISED, verified in marketplace.json lines 15, 27-28):
```json
"skills": ["./api-recon-and-docs"]
"skills": ["./401-403-bypass-techniques", "./api-auth-and-jwt-abuse"]
```
Every path: leading `./`, no `./skills/` prefix, matches upstream directory name verbatim. Non-negotiable.

Description-field tone (verified in marketplace.json lines 14, 25):
```json
"description": "Reconnaissance and information gathering"
"description": "Authentication and authorization bypass"
```
One-line capability summary, gerund/noun-phrase form, ~3-6 words, no period.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Plan 02-01 precondition and synthesize final bucket map</name>
  <files>
    .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md
  </files>
  <read_first>
    - .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md (the user-approved state from Plan 02-01 Task 3 — MUST have `status: checkpoint_approved` and a `## Checkpoint Approval` section OR the equivalent reference)
    - .planning/phases/02-skill-classification-taxonomy/02-01-SUMMARY.md (records the checkpoint approval location and any user-requested edits applied during Plan 02-01)
    - .planning/phases/02-skill-classification-taxonomy/02-CONTEXT.md (D-07 every-skill-exactly-once, D-08 hard assignment, D-10 also_relevant_to metadata, D-13 JSON shape, D-14 verbatim summaries, D-15 phase-local artifact location)
    - .planning/phases/02-skill-classification-taxonomy/02-PATTERNS.md (the entire file — Plan 02-01 produced the input, this plan TRANSLATES it into the analog-derived final artifacts; PATTERNS is the structural map)
  </read_first>
  <action>
    Verify Plan 02-01's precondition is satisfied before any artifact write:

    1. Read `.first-pass-classification.md` frontmatter. The `status` field MUST equal `checkpoint_approved`. If it equals `corpus_extracted` or `first_pass_complete` (i.e., Plan 02-01 did not complete its Task 3 checkpoint), HALT this plan with a clear error message ("Plan 02-01 checkpoint not yet approved — cannot proceed with artifact finalization") and surface it as a planning gap requiring re-execution of Plan 02-01.

    2. Confirm the approval is recorded in writing per the D-16 acceptance criteria. Either:
       (a) `.first-pass-classification.md` contains a `## Checkpoint Approval` section with the user's response verbatim, OR
       (b) `02-01-SUMMARY.md` references the discussion-log entry where approval was captured.
       Capture the approval reference text — it becomes the `checkpoint_approval_signal` frontmatter value in both final artifacts.

    3. Re-run the D-07 invariant against the approved state: extract bucket-membership lists from the `## First-Pass Buckets` section, flatten them, and assert that:
       (a) `sort -u` of all assigned skill-names equals `ls /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/ | sort` (102 entries, set-equal).
       (b) Total assignments (counting duplicates) equals 102 — i.e., no skill is in two buckets.
       If either assertion fails, HALT and surface the gap. The invariant must hold in the approved state OR be fixable with documented edits before this plan continues.

    4. Build an in-memory bucket map from the approved state with this structure (use whatever data representation is most convenient — JSON-like dict, YAML, or simply a structured markdown plan-of-record under a working header in `.first-pass-classification.md`):
       - For each bucket: bucket-name, description (one-line gerund/noun-phrase per PATTERNS.md), skills (alphabetical-by-name list of `./<skill-name>` strings using Phase 1 D-03-REVISED root-level form), notes (sizing-status text — empty if within 8-15, "catch-all per D-06" if a catch-all bucket, "originally `<bucket>` (<N> skills), merged because <reason>" or "split from `<bucket>` along <axis>" if a resize move applies).
       - Borderline-skill metadata: for each entry in `## Borderline List`, record `{skill-name: {also_relevant_to: <candidate-alternate>}}`.

    5. Build the master 102-row data: for each skill, lookup its verbatim description from the `## Extracted Corpus` section of `.first-pass-classification.md`, its assigned bucket from the bucket map, and its `also_relevant_to` value (or empty) from the borderline metadata. This is the source data for both the master table in CLASSIFICATION.md AND a sanity-check pass for CLASSIFICATION.json's `skills` arrays.

    NO new files are created in this task — the work is verification + synthesis to set up Tasks 2 and 3. Append a `## Final-Artifact Synthesis State` section to `.first-pass-classification.md` recording: the verified precondition status, the bucket map summary (counts only — full bucket data is rendered by Tasks 2/3 from the corpus + approved state), the borderline metadata count, and a `synthesis_ready: true` marker.
  </action>
  <verify>
    <automated>
      # Precondition: status must be checkpoint_approved
      grep -E '^status: checkpoint_approved$' .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md

      # Approval reference captured (either in-file section OR summary reference)
      grep -E '^## Checkpoint Approval$|^checkpoint_approval_signal:' .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md \
        || grep -E 'checkpoint.*approval' .planning/phases/02-skill-classification-taxonomy/02-01-SUMMARY.md \
        || { echo "MISSING: approval reference"; exit 1; }

      # D-07 invariant re-check — extract bucket memberships, compare to source skill list
      # The exact form is executor's choice; the assertion that matters is:
      #   union(buckets) == ls /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/
      #   sum(|bucket|) == 102 (no duplicates)
      # A passing check produces synthesis_ready: true in the file.
      grep -E '^## Final-Artifact Synthesis State$' .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md
      grep -E 'synthesis_ready:[[:space:]]*true' .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md
    </automated>
  </verify>
  <done>
    `.first-pass-classification.md` has `status: checkpoint_approved` (precondition verified), the user-approval reference is locatable (in-file `## Checkpoint Approval` section OR `02-01-SUMMARY.md` reference), and the D-07 invariant is re-verified against the approved state. A `## Final-Artifact Synthesis State` section is appended with `synthesis_ready: true`. No artifact files have been written yet — Tasks 2 and 3 do the actual writes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write `02-CLASSIFICATION.md` per CONTEXT.md D-12 layout</name>
  <files>
    .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md
  </files>
  <read_first>
    - .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md (the approved bucket map + verbatim corpus + borderline list + checkpoint approval reference — this task RENDERS that data into the analog-derived final markdown)
    - .planning/phases/02-skill-classification-taxonomy/02-PATTERNS.md sections: "Pattern Assignments → `02-CLASSIFICATION.md`" (frontmatter pattern, top-of-file summary pattern, verbatim-evidence convention, Roadmap-success-criteria-check pattern, "Override from CONTEXT.md D-12" body sections, "What to NOT borrow" exclusions)
    - .planning/phases/02-skill-classification-taxonomy/02-CONTEXT.md (D-12 markdown sections, D-14 verbatim source, D-15 phase-local location)
    - .planning/phases/01-schema-verification/01-VERIFICATION.md lines 1-26 (frontmatter shape + Summary block to mirror) and lines 181-191 (Roadmap Success Criteria check format to mirror)
  </read_first>
  <action>
    Author `.planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md` rendering the approved state from `.first-pass-classification.md` into the D-12-prescribed structure. The file MUST contain, in this order:

    **1. YAML frontmatter** matching the shape PATTERNS.md gives in its "Adapt the frontmatter axes" example (CLASSIFICATION.md frontmatter shape in this plan's `<interfaces>` block). Required fields:
    - `status: passed` (or `gaps_found` / `human_needed` if Task 1's invariant re-check surfaced issues — should be `passed` in the happy path)
    - `phase: 02-skill-classification-taxonomy`
    - `classified: <today's ISO date>`
    - `requirements: GROUP-01, GROUP-02, GROUP-03, GROUP-04`
    - `total_skills: 102`
    - `bucket_count: <N>` (matches the count from approved state)
    - `catch_all_shape: multiple_themed` OR `single_misc` per D-06 outcome
    - `sized_within_8_15: true` OR `"true with documented exceptions in <bucket-name(s)> per D-02"`
    - `mid_phase_checkpoint: completed`
    - `checkpoint_approval_signal: <verbatim reference from Task 1>`

    **2. Title and Summary block** (mirrors 01-VERIFICATION.md lines 14-26 structure):
    - `# Phase 2 Classification: Skill Taxonomy`
    - `## Summary` — 1-2 sentence rollup: "All 102 skills classified into N buckets; X catch-all bucket(s) per D-06 (<themed | single misc>); Y `also_relevant_to` close-call entries logged in Decision Log below."
    - Compact bucket-overview table with columns: `bucket | member_count | sized_within_8_15 | notes`. One row per bucket (including catch-alls). Final row: `TOTAL: 102 across N buckets`.
    - Explicit D-07 invariant statement: "Every one of the 102 skills appears exactly once across all buckets above. Verified by [method — set equality against `ls /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/`]."

    **3. Per-bucket sections** (D-12 item 1; PATTERNS.md "Override from CONTEXT.md D-12" — the load-bearing structural override). One `##` section per primary topical bucket. Required fields per section:
    - `## <bucket-name>` (kebab-case per PATTERNS.md Bucket-naming convention, matches the JSON key in CLASSIFICATION.json)
    - **Scope paragraph** — one paragraph stating what unifies the members (this text is lifted into CLASSIFICATION.json's `description` field downstream and into marketplace.json's plugin `description` in Phase 3 per CONTEXT.md D-13 + Deferred-Ideas note on bucket descriptions).
    - **Member list** — alphabetical by skill-name. Format per item: `` - `<skill-name>`: <verbatim description from upstream SKILL.md frontmatter> `` (PATTERNS.md verbatim-evidence convention; descriptions are short enough — ~100 chars/skill — that markdown list is right form, fenced blocks would be overkill).
    - **Member count** — `**Count:** <N> skills`.
    - **Sizing note** — only present for special cases per D-02: catch-all flag, sized-outside-8-15 with reason, merge/split origin (e.g., "Originally `crypto` (4 skills), merged into this bucket because skills are payload-shaped per D-02 < 5 resize policy."). Omit for cleanly-sized buckets within 8-15.

    **4. Catch-all section** (D-12 item 2; D-06):
    - `## Catch-all bucket(s)` header.
    - Lead sentence stating which D-06 case applied: "Multiple themed catch-alls applied: `<names>`." OR "Single `hack-skills-misc` fallback applied (misfits did not cluster into 2+ themed groups)."
    - For each catch-all bucket, render with the SAME field set as a per-bucket section above (bucket-name as `###` sub-header, scope paragraph, member list, member count, sizing note flagging it as a catch-all).
    - Per-member reason tag per D-05: append `(standalone)` or `(redundant)` after each skill's verbatim description (e.g., `` - `skill-x`: <verbatim description> (standalone) ``).

    **5. Master 102-row table** (D-12 item 3; PATTERNS.md verbatim-evidence convention):
    - `## Master Table` header.
    - Single markdown table with columns: `name | summary | assigned_bucket | also_relevant_to`.
    - One row per skill, alphabetical by `name`. The `name` column matches the upstream directory name. The `summary` column uses the verbatim description from the corpus. The `assigned_bucket` column matches a bucket name from a per-bucket or catch-all section. The `also_relevant_to` column is empty for routine assignments and populated per D-10 for borderline skills only.
    - **Row count MUST equal 102** — load-bearing audit invariant for ROADMAP Phase 2 Success Criterion #5 (D-07).

    **6. Decision log** (D-12 item 4):
    - `## Decision Log` header.
    - One entry per non-obvious close call only (skip routine assignments per CONTEXT.md D-12 + SPECIFICS bullet on `also_relevant_to`).
    - Format per entry: `` - `<skill-name>`: assigned to `<bucket>` over `<other>`; reason: <skill's own description framing emphasized X> [+ optional cross-ref to RELATED ROUTING block in skill body if used as secondary signal per D-09]. ``
    - Empty log is valid and should be stated explicitly: "No non-obvious close calls — all 102 assignments were routine per D-09's primary-signal heuristic."

    **7. Roadmap Success Criteria check** (mirrors 01-VERIFICATION.md lines 181-191 — PATTERNS.md "Roadmap-success-criteria-check pattern"):
    - `## Roadmap Success Criteria (Phase 2)` header.
    - Lead line: "Checking against the 5 success criteria listed in ROADMAP.md §Phase 2:"
    - Numbered list, 5 items, each with a `✓` checkmark, the criterion paraphrase, and the in-file evidence reference. Exact form per the PATTERNS.md template:
      1. ✓ Classification artifact exists listing all 102 skills with one-line summaries — see Master Table above.
      2. ✓ Topical group taxonomy documented with scope + rationale — see Per-bucket sections above.
      3. ✓ Each proposed group's membership within 8-15 — see Summary bucket-overview table; exceptions noted under Catch-all section if any.
      4. ✓ Misfit skills explicitly listed with reason — see Catch-all section (D-06 reframe: assigned to catch-all bucket rather than absent from marketplace.json).
      5. ✓ Every skill appears exactly once — D-07 invariant verified; total count matches `ls /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/ | wc -l = 102`.
    - Closing line: **"All 5 success criteria met. Phase 2 unlocks Phase 3."**

    **Pattern exclusions** (per PATTERNS.md "What to NOT borrow from 01-VERIFICATION.md"):
    - Do NOT include `## VERIFY-NN` per-section structure — wrong axes for Phase 2.
    - Do NOT include `## Pivot Policy` section — VERIFY-specific, no analog.
    - Do NOT include `### VERIFY-NN corrective note` subsections — Phase 2's mid-phase checkpoint is planned (D-16), not reactive; any pre-checkpoint vs. post-checkpoint changes are documented in the Decision Log section or the `mid_phase_checkpoint: completed` frontmatter, not as standalone sections.
  </action>
  <verify>
    <automated>
      # File exists with the expected frontmatter status
      grep -E '^status: passed$' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md
      grep -E '^phase: 02-skill-classification-taxonomy$' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md
      grep -E '^total_skills: 102$' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md
      grep -E '^bucket_count: [0-9]+$' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md
      grep -E '^catch_all_shape: (multiple_themed|single_misc)$' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md
      grep -E '^mid_phase_checkpoint: completed$' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md
      grep -E '^checkpoint_approval_signal: ' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md

      # All five required body sections present
      for section in '^## Summary$' '^## Catch-all bucket' '^## Master Table$' '^## Decision Log$' '^## Roadmap Success Criteria \(Phase 2\)$'; do
        grep -E "$section" .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md > /dev/null \
          && echo "FOUND: $section" \
          || { echo "MISSING: $section"; exit 1; }
      done

      # Per-bucket sections — at least one `##` heading that is NOT one of the meta sections above
      # Count of bucket sections SHOULD equal bucket_count from frontmatter
      EXPECTED=$(grep -oE '^bucket_count: [0-9]+$' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md | awk '{print $2}')
      # Bucket sections are H2 headings that are not in the meta set {Summary, Catch-all bucket(s), Master Table, Decision Log, Roadmap Success Criteria (Phase 2)}
      # The exact assertion: total H2 count - 5 meta sections == EXPECTED primary buckets
      # (Catch-all buckets are H3 under "## Catch-all bucket(s)"; if D-06 multiple_themed expands that, adjust accordingly)

      # Master table has exactly 102 data rows (header row + 102 data rows + separator row = 104 lines starting with `|`)
      # Extract the master table section, count rows starting with `|` that look like data (not header or separator)
      awk '/^## Master Table$/,/^## /{print}' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md \
        | grep -E '^\| [a-z0-9-]+ +\|' | wc -l
      # MUST output: 102

      # D-07 invariant — every skill in master table column 1 matches a directory under hack-skills/skills/
      awk '/^## Master Table$/,/^## /{print}' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md \
        | grep -E '^\| [a-z0-9-]+ +\|' \
        | awk -F'|' '{gsub(/^ +| +$/, "", $2); print $2}' \
        | sort > /tmp/classified-skills.txt
      ls /Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/ | sort > /tmp/source-skills.txt
      diff /tmp/classified-skills.txt /tmp/source-skills.txt
      # MUST exit 0 (no diff) — proves D-07 invariant.

      # Roadmap criteria closing line present verbatim
      grep -F 'All 5 success criteria met. Phase 2 unlocks Phase 3.' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md

      # PATTERN EXCLUSION checks — verify we did NOT borrow the wrong structure
      ! grep -E '^## VERIFY-[0-9]+' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md \
        && echo "OK: no VERIFY-NN sections" \
        || { echo "FAIL: VERIFY-NN structure should not appear in Phase 2"; exit 1; }
      ! grep -E '^## Pivot Policy' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md \
        && echo "OK: no Pivot Policy section" \
        || { echo "FAIL: Pivot Policy is VERIFY-specific, should not appear"; exit 1; }
    </automated>
  </verify>
  <done>
    `02-CLASSIFICATION.md` exists at the canonical phase-local path with: full frontmatter matching the PATTERNS-prescribed shape; Summary block with bucket-overview table and D-07 invariant statement; one `##` per-bucket section per primary bucket with scope paragraph + verbatim member list + count; Catch-all section per D-06 with per-member reason tags; Master Table with exactly 102 data rows whose `name` column set-equals `ls hack-skills/skills/`; Decision Log section (entries OR explicit empty statement); Roadmap Success Criteria check with all 5 items ✓-marked and the verbatim closing line. No VERIFY-NN structure, no Pivot Policy section (D-12 axes, not D-06-Phase-1 axes).
  </done>
</task>

<task type="auto">
  <name>Task 3: Write `02-CLASSIFICATION.json` per CONTEXT.md D-13 bucket-keyed shape</name>
  <files>
    .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json
  </files>
  <read_first>
    - .planning/phases/02-skill-classification-taxonomy/.first-pass-classification.md (approved bucket map + skill paths in root-level form — translate to JSON; this is the data source)
    - .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md (the markdown artifact just written — bucket names and per-bucket descriptions MUST cross-reference cleanly between the two files; the master table's `assigned_bucket` column is the cross-check)
    - .planning/phases/02-skill-classification-taxonomy/02-PATTERNS.md sections: "Pattern Assignments → `02-CLASSIFICATION.json`" (skill-path convention to borrow verbatim, description-field tone to borrow, "Override from CONTEXT.md D-13" for the bucket-keyed shape, "What to NOT borrow from marketplace.json")
    - .planning/phases/02-skill-classification-taxonomy/02-CONTEXT.md (D-13 JSON shape requirements, D-08 hard assignment invariant for `skills` arrays, D-10 also_relevant_to metadata)
    - .claude-plugin/marketplace.json (current Phase 1 output — lines 15, 27-28 verify the root-level `./<skill-name>` path convention to mirror; lines 14, 25 verify the description-field tone)
    - .planning/phases/01-schema-verification/01-CONTEXT.md (D-03-REVISED — the source descriptor + root-level path form is locked; CLASSIFICATION.json `skills` arrays MUST use root-level form)
  </read_first>
  <action>
    Author `.planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json` as valid JSON with the bucket-keyed top-level shape per CONTEXT.md D-13 + PATTERNS.md "Override from CONTEXT.md D-13" — see the JSON shape in this plan's `<interfaces>` block.

    Structure requirements:

    **1. Top-level `_meta` block** (PATTERNS.md "Optional but recommended" Claude's Discretion bullet — included here as load-bearing for Phase 3 sanity-checking):
    - `phase`: `"02-skill-classification-taxonomy"`
    - `classified`: today's ISO date (matches CLASSIFICATION.md frontmatter `classified` field — cross-reference invariant)
    - `total_skills`: `102` (integer literal — Phase 3 will assert this before iterating)
    - `bucket_count`: `<N>` (integer; matches CLASSIFICATION.md frontmatter `bucket_count`)
    - `checkpoint_approval_signal`: `"<reference text matching CLASSIFICATION.md frontmatter>"`

    **2. Bucket-keyed top-level entries** — one key per bucket (primary topical buckets first, alphabetical within each tier; then catch-all bucket(s) afterward, also alphabetical). PATTERNS.md "Optional but recommended" suggests alphabetical key ordering for diff stability — apply this. Each bucket entry MUST contain:
    - `description`: one-line scope, gerund/noun-phrase tone, no period — lifted from the per-bucket section's scope-paragraph distillation in CLASSIFICATION.md. PATTERNS.md gives the marketplace.json description tone as the model: `"Reconnaissance and information gathering"`, `"Authentication and authorization bypass"`. Phase 3's executor LIFTS this field verbatim into marketplace.json's plugin `description`.
    - `skills`: JSON array of root-level `./<skill-name>` strings, alphabetical by skill-name. Phase 1 D-03-REVISED locks the root-level form — NO `./skills/` prefix. Phase 3's executor DROPS this array as-is into the marketplace.json plugin entry.
    - `notes`: string field. Empty string `""` if no special-case metadata. Populated with sizing-exception text, catch-all flag, merge/split origin per D-02/D-06 outcome (e.g., `"catch-all per D-06"`, `"originally crypto (4 skills), merged here per D-02 < 5 policy"`, `"split from injection along web-vs-protocol axis per D-02 > 15 policy"`).

    **3. Top-level `excluded` array** — `"excluded": []` reserved at top level per CONTEXT.md D-13 + PATTERNS.md "Override" note ("likely empty under D-06's misfits-go-into-catch-alls rule — but it exists for schema completeness so Phase 3's executor doesn't need a conditional `if 'excluded' in data`"). Under D-06, this should be an empty array in the happy path.

    **4. Top-level `skill_metadata` map** — separate top-level object per PATTERNS.md "Override from CONTEXT.md D-13" recommendation ("a separate top-level map so the per-bucket `skills: []` arrays stay clean and Phase 3 can paste them directly into marketplace.json without needing to strip metadata fields"). Keys are skill-names that have borderline `also_relevant_to` entries; values are `{"also_relevant_to": "<other-bucket-name>"}`. Skills with NO borderline metadata are absent from this map (it is sparse, not exhaustive — per D-10, populated only for genuine close calls).

    **Pattern exclusions** (per PATTERNS.md "What to NOT borrow from marketplace.json"):
    - Do NOT include a flat `plugins: [...]` outer container — wrong shape; CLASSIFICATION.json is bucket-keyed.
    - Do NOT include a `source` descriptor — that's Phase 3's job to add per plugin entry when translating buckets to marketplace.json.
    - Do NOT include a `strict: false` field — also Phase 3's responsibility.
    - Do NOT include marketplace-level `name`, `owner`, top-level `description` fields — those are marketplace-config-level, not classification-level.
    - Do NOT include a `$schema` field — no published schema exists for CLASSIFICATION.json, a bogus URL would mislead readers (PATTERNS.md "Procedural Patterns NOT Borrowed" section).

    The JSON MUST parse cleanly (valid syntax, no trailing commas, proper quoting). Use `jq` to format with 2-space indent for diff readability.
  </action>
  <verify>
    <automated>
      # File exists and is valid JSON
      jq empty .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json
      # MUST exit 0 (valid JSON syntax)

      # _meta block present with expected fields
      jq -e '._meta | has("phase") and has("classified") and has("total_skills") and has("bucket_count") and has("checkpoint_approval_signal")' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json
      jq -e '._meta.total_skills == 102' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json
      jq -e '._meta.phase == "02-skill-classification-taxonomy"' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json

      # excluded array reserved at top level (empty under D-06 happy path)
      jq -e 'has("excluded") and (.excluded | type == "array")' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json

      # skill_metadata top-level map present (may be empty if no borderlines)
      jq -e 'has("skill_metadata") and (.skill_metadata | type == "object")' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json

      # Bucket-keyed: every key OTHER than _meta/excluded/skill_metadata is a bucket with the required shape
      jq -e '[keys[] | select(. != "_meta" and . != "excluded" and . != "skill_metadata")] | length > 0' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json

      # Every bucket has description + skills + notes
      jq -e '[to_entries[] | select(.key != "_meta" and .key != "excluded" and .key != "skill_metadata")] | all(.value | has("description") and has("skills") and has("notes"))' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json

      # Bucket count matches _meta.bucket_count
      META_COUNT=$(jq -r '._meta.bucket_count' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json)
      ACTUAL_COUNT=$(jq -r '[keys[] | select(. != "_meta" and . != "excluded" and . != "skill_metadata")] | length' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json)
      [ "$META_COUNT" = "$ACTUAL_COUNT" ] && echo "OK bucket_count == $META_COUNT" || { echo "MISMATCH: _meta.bucket_count=$META_COUNT actual=$ACTUAL_COUNT"; exit 1; }

      # D-07 invariant in JSON form: sum of |bucket.skills| across all buckets == 102, no duplicates
      TOTAL=$(jq -r '[to_entries[] | select(.key != "_meta" and .key != "excluded" and .key != "skill_metadata") | .value.skills | length] | add' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json)
      [ "$TOTAL" = "102" ] && echo "OK total skills == 102" || { echo "FAIL: total skills = $TOTAL, expected 102"; exit 1; }

      UNIQUE=$(jq -r '[to_entries[] | select(.key != "_meta" and .key != "excluded" and .key != "skill_metadata") | .value.skills[]] | unique | length' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json)
      [ "$UNIQUE" = "102" ] && echo "OK unique skills == 102 (D-08 hard assignment holds)" || { echo "FAIL: unique skills = $UNIQUE, expected 102"; exit 1; }

      # D-03-REVISED skill-path form: every path starts with "./" and has NO "./skills/" prefix
      jq -r '[to_entries[] | select(.key != "_meta" and .key != "excluded" and .key != "skill_metadata") | .value.skills[]]' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json \
        | grep -E '^\s*"\./skills/' && { echo "FAIL: paths must NOT use ./skills/ prefix per Phase 1 D-03-REVISED"; exit 1; } \
        || echo "OK no nested ./skills/ paths"

      jq -r '[to_entries[] | select(.key != "_meta" and .key != "excluded" and .key != "skill_metadata") | .value.skills[]] | .[]' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json \
        | grep -vE '^\./[a-z0-9-]+$' | head -5 \
        | { read line; [ -z "$line" ] && echo "OK all paths are root-level ./<skill-name> form" || { echo "FAIL: non-conforming path: $line"; exit 1; }; }

      # Cross-file invariant: every skill in JSON appears in master table of CLASSIFICATION.md
      jq -r '[to_entries[] | select(.key != "_meta" and .key != "excluded" and .key != "skill_metadata") | .value.skills[]] | .[] | sub("^\\./"; "")' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json | sort > /tmp/json-skills.txt
      awk '/^## Master Table$/,/^## /{print}' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.md \
        | grep -E '^\| [a-z0-9-]+ +\|' | awk -F'|' '{gsub(/^ +| +$/, "", $2); print $2}' | sort > /tmp/md-skills.txt
      diff /tmp/json-skills.txt /tmp/md-skills.txt
      # MUST exit 0 (no diff) — JSON `skills` arrays and Markdown master table reference the same 102 skills.

      # Cross-file invariant: bucket names in JSON match per-bucket `##` headings in Markdown (set equality, modulo Catch-all sub-headings)
      jq -r '[keys[] | select(. != "_meta" and . != "excluded" and . != "skill_metadata")] | .[]' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json | sort > /tmp/json-buckets.txt
      # Bucket names in MD are `##` headings minus the meta-section set {Summary, Catch-all bucket(s), Master Table, Decision Log, Roadmap Success Criteria (Phase 2)}
      # AND `###` sub-headings under "## Catch-all bucket(s)" if multiple_themed
      # The exact extraction is executor's choice; the assertion that matters:
      #   set(json_buckets) == set(md_buckets - meta_sections)
      # If equal, both files cross-reference cleanly; Phase 3 can iterate by bucket name and find every bucket in both files.

      # Pattern exclusion check: no plugins[] array, no source/strict fields, no $schema
      jq -e 'has("plugins") | not' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json && echo "OK no plugins[]"
      jq -e '[to_entries[] | select(.key != "_meta" and .key != "excluded" and .key != "skill_metadata")] | all(.value | (has("source") | not) and (has("strict") | not))' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json && echo "OK no source/strict per bucket"
      jq -e 'has("$schema") | not' .planning/phases/02-skill-classification-taxonomy/02-CLASSIFICATION.json && echo "OK no \$schema"
    </automated>
  </verify>
  <done>
    `02-CLASSIFICATION.json` exists, parses as valid JSON, has the `_meta` + bucket-keyed-entries + `excluded` + `skill_metadata` shape per D-13. Every bucket has `description` + `skills` + `notes`. Total skills across buckets == 102, unique skills across buckets == 102 (D-07 + D-08 invariants hold). Every skill path is root-level `./<skill-name>` form (Phase 1 D-03-REVISED). Cross-file invariants hold: master table in CLASSIFICATION.md references the same 102 skills, and bucket names cross-reference cleanly between MD and JSON. No `plugins[]` flat array, no `source`/`strict` fields per bucket, no `$schema` (PATTERNS exclusions).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.first-pass-classification.md` (Plan 02-01 output) → this plan's artifact synthesis | Read boundary. The intermediate file is the trusted-input source — its content was already user-approved at the D-16 checkpoint. This plan does not re-validate user intent; it renders the approved state into the analog-derived final shapes. |
| Final artifacts → Phase 3 executor | Cross-phase contract boundary. Phase 3 reads `02-CLASSIFICATION.json` (NOT `02-CLASSIFICATION.md`) to author marketplace.json. The `skills` arrays in the JSON are dropped in verbatim — any path corruption here cascades into marketplace.json. The cross-file invariant check (JSON paths == Markdown master table names == upstream directory names) is the structural defense. |
| Verbatim descriptions from corpus → Markdown member lists + master table | Documentation-fidelity boundary. CONTEXT.md D-14 mandates verbatim — any Claude-authored paraphrasing here breaks the convention and Phase 3 downstream behaviors that may match against the upstream descriptions in marketplace.json. The verify block's verbatim-equality test against the source SKILL.md (carried from Plan 02-01 Task 1) is the structural defense. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Skill paths in `02-CLASSIFICATION.json` use wrong form (e.g., `./skills/<name>` instead of `./<name>`) | mitigate | Phase 1 D-03-REVISED locks the root-level form. Task 3 verify block asserts no `./skills/` prefix appears AND every path matches `^\./[a-z0-9-]+$`. A failure here causes Phase 3's marketplace.json to inherit the broken paths, which Phase 1 already established BREAKS the curated `skills` array behavior (~10,503 always-on tokens vs ~205 expected). Auto-blocked by the verify gate. |
| T-02-05 | Tampering | Cross-file drift between CLASSIFICATION.md master table and CLASSIFICATION.json skill arrays | mitigate | Task 3 verify block diffs the two sources of skill membership (JSON paths vs Markdown master-table `name` column). Diff MUST be empty. Drift surfaces as a hard fail before commit. |
| T-02-06 | Information disclosure | Verbatim descriptions reproduced in committed artifacts may carry unintended content | accept | All 102 SKILL.md files are public on `github.com/yaklang/hack-skills`. Verbatim reproduction in `.planning/` (private working tree until Phase 4) is no exposure increase. CONTEXT.md D-14 actively requires verbatim. Same disposition as T-02-02 from Plan 02-01. |
| T-02-07 | Repudiation | Loss of traceability from CLASSIFICATION → checkpoint approval | mitigate | Both final artifacts carry `checkpoint_approval_signal` (MD frontmatter field, JSON `_meta.checkpoint_approval_signal` field). The signal text references the location of the user's approval in `.first-pass-classification.md` `## Checkpoint Approval` section or the 02-01-SUMMARY.md discussion-log entry. Traceability is auditable post-hoc by following the signal back to the approval record. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | NO new package-manager installs in this plan. Only uses `jq` (already on the system per the verify-block assumption — `command -v jq` is a non-failing-soft prereq). If `jq` is unavailable, fall back to a small Python `json` stdlib script. No third-party packages introduced. Package Legitimacy Gate not applicable. |

Phase 2's threat surface remains minimal (planning-tree artifacts only, no code/runtime/API). The boundaries above are documented honestly. The two highest-risk paths (skill-path form drift, cross-file drift) are gated by automated verify-block assertions before commit, so a hand-authored mistake during artifact synthesis fails closed.
</threat_model>

<verification>
After all three tasks complete:

1. `.first-pass-classification.md` precondition verified (`status: checkpoint_approved`); D-07 invariant re-run; `synthesis_ready: true` recorded.
2. `02-CLASSIFICATION.md` exists with full frontmatter, Summary block, per-bucket sections, catch-all section, master 102-row table, decision log, and Roadmap success criteria check. No VERIFY-NN structure, no Pivot Policy section (correct D-12 axes).
3. `02-CLASSIFICATION.json` exists as valid JSON with `_meta` + bucket-keyed entries + `excluded` + `skill_metadata`. Every bucket has `description` + `skills` + `notes`. All paths root-level. Cross-file invariant holds against CLASSIFICATION.md master table.
4. ROADMAP Phase 2 Success Criteria 1-5 all satisfied per the explicit checkoff section in CLASSIFICATION.md.
5. Source tree at `/Users/spencerpresley/code/hack-skills-wip/hack-skills/skills/` remains untouched.
</verification>

<success_criteria>
This plan closes out Phase 2's GROUP-01 / GROUP-02 / GROUP-03 / GROUP-04 requirements:

- GROUP-01: All 102 skills appear in `02-CLASSIFICATION.md`'s master table with verbatim one-line summaries from upstream SKILL.md frontmatter. ✓
- GROUP-02: Final topical group taxonomy is documented — per-bucket sections in CLASSIFICATION.md state scope + rationale; `description` + `notes` fields in CLASSIFICATION.json encode the same for Phase 3. ✓
- GROUP-03: Each proposed group's membership is within 8-15 OR explicitly flagged as a catch-all per D-06 OR documented as sized-outside-range with reason per D-02. ✓
- GROUP-04: Misfit skills appear in the catch-all bucket(s) per D-06 reframe (assigned to a catch-all bucket rather than absent from marketplace.json) with per-member reason tags (`standalone` or `redundant` per D-05). ✓

ROADMAP Phase 2's 5 success criteria are explicitly checked off in CLASSIFICATION.md's closing section, mirroring 01-VERIFICATION.md's precedent. Phase 2 unlocks Phase 3.
</success_criteria>

<output>
Create `.planning/phases/02-skill-classification-taxonomy/02-02-SUMMARY.md` when this plan completes. The SUMMARY should record:
- Final bucket count, names, and member counts.
- Catch-all shape outcome (multiple themed vs. single misc) per D-06.
- Count of `also_relevant_to` borderline entries logged in Decision Log + `skill_metadata`.
- Any sizing exceptions to the 8-15 range with documented reasons.
- Confirmation that all 5 ROADMAP Phase 2 success criteria are met.
- Pointer to `02-CLASSIFICATION.json` as the Phase 3 input file (with the exact absolute path Phase 3's executor reads).
</output>
</content>
</invoke>