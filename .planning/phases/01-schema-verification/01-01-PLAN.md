---
phase: 01-schema-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/marketplace.json
autonomous: true
requirements:
  - VERIFY-01
  - VERIFY-02
  - VERIFY-03
tags:
  - claude-code-plugin-marketplace
  - marketplace-json-authoring
  - strict-false

must_haves:
  truths:
    - "A 2-group test `.claude-plugin/marketplace.json` exists pointing at `yaklang/hack-skills` (D-01, D-03)"
    - "The `hack-skills-recon` plugin entry lists exactly one skill path: `./skills/api-recon-and-docs` (D-01)"
    - "The `hack-skills-auth-bypass` plugin entry lists exactly two skill paths: `./skills/401-403-bypass-techniques` and `./skills/api-auth-and-jwt-abuse` (D-01, D-02)"
    - "Both plugin entries set `strict: false` (D-03)"
    - "Both plugin entries set `source` to `{ \"source\": \"github\", \"repo\": \"yaklang/hack-skills\" }` (D-03)"
    - "Every skill path uses the leading `./` prefix required by the schema (RESEARCH §Anti-Patterns line 299)"
    - "`claude plugin validate` on this marketplace exits cleanly (no duplicate names, no path traversal, valid JSON)"
  artifacts:
    - path: ".claude-plugin/marketplace.json"
      provides: "Test marketplace declaration with two plugin entries per D-01"
      contains: "hack-skills-recon, hack-skills-auth-bypass, strict, skills"
  key_links:
    - from: ".claude-plugin/marketplace.json plugin entry hack-skills-recon"
      to: "yaklang/hack-skills upstream"
      via: "source: { source: github, repo: yaklang/hack-skills }"
      pattern: "\"repo\":\\s*\"yaklang/hack-skills\""
    - from: ".claude-plugin/marketplace.json plugin entry hack-skills-auth-bypass"
      to: "yaklang/hack-skills upstream"
      via: "source: { source: github, repo: yaklang/hack-skills }"
      pattern: "\"repo\":\\s*\"yaklang/hack-skills\""
---

<objective>
Author the test marketplace.json that exercises all three VERIFY questions. This is the only authored artifact in Phase 1; everything downstream (install, observe, evidence collection) reads this file.

Purpose: Provide a known-good 2-group test marketplace whose shape is the literal target shape documented in RESEARCH.md Example 1 (lines 401–431), so that downstream install/observe steps have something concrete to install. The composition (D-01) is intentionally minimal — two real candidate buckets from PROJECT.md, three individual skill paths total — chosen so VERIFY-01 (individual-skill addressing) is directly exercised by the auth-bypass plugin (2 paths) and VERIFY-02 isolation is easy to eyeball (the two groups are topically distant).

Output: `.claude-plugin/marketplace.json` written to the literal content of RESEARCH.md Example 1, validated by `claude plugin validate`.
</objective>

<execution_context>
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.planning/PROJECT.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.planning/ROADMAP.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.planning/STATE.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.planning/REQUIREMENTS.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.planning/phases/01-schema-verification/01-CONTEXT.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.planning/phases/01-schema-verification/01-RESEARCH.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.planning/phases/01-schema-verification/01-PATTERNS.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/docs/PLAN.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/CLAUDE.md

<interfaces>
<!-- Marketplace schema shape lifted from authoritative sources. -->
<!-- Executor uses RESEARCH.md Example 1 verbatim (lines 401–431); this block enumerates the constraints those lines encode. -->

Required top-level fields (per code.claude.com/docs/en/plugin-marketplaces#marketplace-schema, captured in RESEARCH.md):
  - "name": string — marketplace identifier — for this phase: "hack-skills-marketplace"
  - "owner": object with "name" — for this phase: { "name": "Spencer Presley" }
  - "description": string — for this phase: "Curated topical groups of yaklang/hack-skills"
  - "plugins": array of plugin entry objects — exactly 2 entries for this phase per D-01

Required per-plugin fields:
  - "name": string — for this phase: "hack-skills-recon" (entry 1) and "hack-skills-auth-bypass" (entry 2)
  - "source": object — for this phase: { "source": "github", "repo": "yaklang/hack-skills" } verbatim per D-03
  - "strict": boolean — must be false for both per D-03
  - "description": string — entry 1: "Reconnaissance and information gathering"; entry 2: "Authentication and authorization bypass"
  - "skills": array of strings — leading "./" required per Anti-Pattern at RESEARCH.md line 299

Skill paths (D-01, D-02 — these are the load-bearing strings):
  - hack-skills-recon.skills = [ "./skills/api-recon-and-docs" ]
  - hack-skills-auth-bypass.skills = [ "./skills/401-403-bypass-techniques", "./skills/api-auth-and-jwt-abuse" ]

Paths are relative to the PLUGIN'S SOURCE ROOT (the cloned yaklang/hack-skills repo), NOT to this marketplace repo. See RESEARCH.md line 250 and Pattern Map line 86.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author .claude-plugin/marketplace.json</name>
  <files>.claude-plugin/marketplace.json</files>
  <read_first>
    - .claude-plugin/marketplace.json (current state: zero-byte placeholder per Pattern Map line 12; confirm before overwriting)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 401–431 (Example 1 — the literal target content)
    - .planning/phases/01-schema-verification/01-CONTEXT.md D-01, D-02, D-03 (composition and source-descriptor decisions)
    - .planning/phases/01-schema-verification/01-PATTERNS.md lines 30–93 (Pattern Assignments → marketplace.json — copy-source for outer shape from wondelai; intentional divergences in `source` descriptor)
    - /Users/spencerpresley/code/hack-skills-wip/reference/cc-docs-plugin-marketplace-page-content.md (authoritative marketplace schema)
  </read_first>
  <action>
    Overwrite `.claude-plugin/marketplace.json` (currently a zero-byte placeholder) with the literal content of RESEARCH.md Example 1 (lines 401–431). Use the Write tool, not heredoc. Strip the `// Source:` and `// Verified:` comment lines from the example before writing — `.claude-plugin/marketplace.json` must be valid pure JSON, not JSONC.

    The resulting file must contain exactly these top-level keys: `name` ("hack-skills-marketplace"), `owner` ({ "name": "Spencer Presley" }), `description` ("Curated topical groups of yaklang/hack-skills"), `plugins` (array of length 2). The two plugin entries are, in order:

    Entry 1 — name "hack-skills-recon", source { source: "github", repo: "yaklang/hack-skills" }, strict false, description "Reconnaissance and information gathering", skills [ "./skills/api-recon-and-docs" ] (per D-01).

    Entry 2 — name "hack-skills-auth-bypass", source { source: "github", repo: "yaklang/hack-skills" }, strict false, description "Authentication and authorization bypass", skills [ "./skills/401-403-bypass-techniques", "./skills/api-auth-and-jwt-abuse" ] (per D-01, D-02).

    Constraints (per D-03, PROJECT.md, and RESEARCH.md Anti-Patterns):
    - Both `skills` arrays MUST use the leading `./` prefix on every path (RESEARCH.md line 299 — paths without `./` are invalid syntax).
    - No `version` field, no `plugin.json` reference, no `@branch` anywhere (PROJECT.md install-command-shape constraint).
    - Do NOT add a `$schema` line (Pattern Map line 82 calls it harmless; CONTEXT.md does not require it; keep authored file minimal).
    - This file MUST be the only file modified in this plan. Do not touch any upstream `yaklang/hack-skills` content; source immutability is a hard constraint (CLAUDE.md line 12).
  </action>
  <verify>
    <automated>python3 -c "import json,sys; d=json.load(open('.claude-plugin/marketplace.json')); assert d['name']=='hack-skills-marketplace', 'name mismatch'; assert d['owner']['name']=='Spencer Presley', 'owner mismatch'; assert len(d['plugins'])==2, 'expected 2 plugins'; p1,p2=d['plugins']; assert p1['name']=='hack-skills-recon' and p2['name']=='hack-skills-auth-bypass', 'plugin name order'; assert p1['strict'] is False and p2['strict'] is False, 'strict must be false'; assert p1['source']=={'source':'github','repo':'yaklang/hack-skills'} and p2['source']==p1['source'], 'source descriptor'; assert p1['skills']==['./skills/api-recon-and-docs'], 'recon skills'; assert p2['skills']==['./skills/401-403-bypass-techniques','./skills/api-auth-and-jwt-abuse'], 'auth-bypass skills'; print('JSON shape OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.claude-plugin/marketplace.json` exists and is valid JSON (verified by python3 json.load).
    - Top-level `name` equals `hack-skills-marketplace` (exact string).
    - Top-level `owner.name` equals `Spencer Presley` (exact string).
    - Top-level `plugins` is an array of length 2.
    - `plugins[0].name` equals `hack-skills-recon`; `plugins[1].name` equals `hack-skills-auth-bypass`.
    - Both plugins have `strict: false` (boolean false, not the string "false").
    - Both plugins have `source` equal to `{ "source": "github", "repo": "yaklang/hack-skills" }` (object equality).
    - `plugins[0].skills` equals `["./skills/api-recon-and-docs"]` (exact array).
    - `plugins[1].skills` equals `["./skills/401-403-bypass-techniques", "./skills/api-auth-and-jwt-abuse"]` (exact array, in this order).
    - Every entry in every `skills` array starts with `./` (no bare `skills/...` paths).
    - No `version` key anywhere in the file. No `$schema` key.
  </acceptance_criteria>
  <done>The file exists at `.claude-plugin/marketplace.json`, the python3 shape-assertion command above prints `JSON shape OK` and exits 0, and `wc -l < .claude-plugin/marketplace.json` is > 1 (file is non-empty pretty-printed JSON).</done>
</task>

<task type="auto">
  <name>Task 2: Validate marketplace.json with claude plugin validate</name>
  <files></files>
  <read_first>
    - .claude-plugin/marketplace.json (just-authored file from Task 1)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 433–442 (Example 2 — the validate command and expected output shape)
    - .planning/phases/01-schema-verification/01-RESEARCH.md Common Pitfalls Pitfall 5 (line 376) — distinguishes "what got cached" (all 102 skills present on disk) from "what got exposed" (curated subset); validation may not yet have access to the source repo to confirm individual skill paths exist, so validator passing on JSON schema alone is sufficient at this stage
  </read_first>
  <action>
    Run `claude plugin validate /Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace` (absolute path required per the official validator invocation shape — see RESEARCH.md line 437). Capture stdout and exit code.

    Expected pass shape (per RESEARCH.md line 438): silent or a success indicator (e.g., "All checks passed"). Failure shapes that constitute a Phase 1 blocker (per RESEARCH.md line 440 and Security Domain table at line 622–625): duplicate plugin name, path traversal (`..` in source paths), JSON syntax error.

    Failure shape that is NOT a blocker at this stage: a "skill path not found in source" error tied to the github source not yet being cloned — that is expected pre-install behavior. If the validator surfaces this specific class of warning, document it in the executor's plan summary but do not treat it as a Task 2 failure.

    Validator output (the literal stdout text plus exit code) MUST be captured into a file at `.planning/phases/01-schema-verification/.validate-output.txt` (dot-prefixed, ignored by GSD artifact conventions, not committed) so Plan 02 has a copy of the pre-install validator result for the VERIFY-01 evidence trail.
  </action>
  <verify>
    <automated>claude plugin validate /Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace 2>&1 | tee .planning/phases/01-schema-verification/.validate-output.txt; test "${PIPESTATUS[0]}" -eq 0 || (echo "validator failed; inspect .validate-output.txt" && grep -E "Duplicate plugin name|path traversal|JSON syntax" .planning/phases/01-schema-verification/.validate-output.txt && exit 1; exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-schema-verification/.validate-output.txt` exists and contains the verbatim stdout of `claude plugin validate /Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace`.
    - The captured output does NOT contain any of the literal substrings: `Duplicate plugin name`, `path traversal`, `JSON syntax`. (These are the validator's blocker categories per RESEARCH.md Security Domain table.)
    - If `claude plugin validate` exits non-zero AND the captured output contains none of the above blocker substrings, the failure is treated as a non-blocker (likely a pre-install skill-path-existence warning); record the exact text and proceed.
    - If `claude plugin validate` exits non-zero AND the captured output contains any blocker substring, this task FAILS and Plan 01 cannot mark complete — the marketplace.json must be fixed.
  </acceptance_criteria>
  <done>The validator output file exists at `.planning/phases/01-schema-verification/.validate-output.txt`, contains validator stdout/stderr verbatim, and contains none of the blocker substrings listed above. If `claude plugin validate` itself is not available on this machine for any reason, document that in the file (single line `VALIDATOR_UNAVAILABLE: <reason>`) and proceed — Plan 02's `claude plugin install` will surface any JSON-shape errors as a fallback check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → marketplace.json schema validator | The authored JSON crosses into Claude Code's parser. Untrusted only in the sense that authoring errors here propagate to install state on this machine. |
| Marketplace declaration → yaklang/hack-skills upstream (NOT crossed by this plan) | The `source` descriptor names the upstream, but Plan 01 does not trigger a clone. The boundary crossing happens in Plan 02 (first `install`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `.claude-plugin/marketplace.json` plugin entries | mitigate | Task 2 runs `claude plugin validate` which rejects duplicate plugin names and path traversal in source paths (RESEARCH.md Security Domain). The two plugin names are distinct by construction (hack-skills-recon, hack-skills-auth-bypass per D-01) — duplicate-name impossible if D-01 is followed verbatim. |
| T-01-02 | Tampering | `skills` array path values | mitigate | Schema requires `./`-prefixed relative paths; validator rejects `..` traversal. Plan 01's `<acceptance_criteria>` asserts every skill path begins with `./` (no bare paths, no parent-relative `../`). |
| T-01-03 | Information disclosure | `owner` block contents | accept | `owner.name` is "Spencer Presley" — already public on PROJECT.md and the github repo. No PII beyond what is already part of the public repo identity. No email, no token. |
| T-01-04 | Tampering | yaklang/hack-skills upstream content (between Plan 01 author-time and Plan 02 install-time) | accept | Plan 01 does not fetch upstream content. The descriptor names the repo but the actual clone happens in Plan 02. Reproducibility of upstream HEAD between plans is addressed in Plan 02's threat model; for Plan 01 this is out of scope (no upstream content is touched here). |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages — no `npm install`, no `pip install`, no `cargo install`. Only authors a JSON file and runs the already-installed `claude` CLI. RESEARCH.md Package Legitimacy Audit at line 134 explicitly marks the audit as not-applicable for this phase. The blocking-human checkpoint for `[ASSUMED]`/`[SUS]` packages is therefore moot. |
</threat_model>

<verification>
- `python3 -c "import json; json.load(open('.claude-plugin/marketplace.json'))"` exits 0 (valid JSON).
- The full python3 shape-assertion command in Task 1's `<automated>` block prints `JSON shape OK` and exits 0.
- `.planning/phases/01-schema-verification/.validate-output.txt` exists and contains no blocker substring (per Task 2's acceptance criteria).
- `grep -c '"./skills/' .claude-plugin/marketplace.json` returns exactly 3 (one for the recon skill, two for the auth-bypass skills).
- `grep -c '"strict": false' .claude-plugin/marketplace.json` returns exactly 2 (one per plugin entry, lowercased boolean).
</verification>

<success_criteria>
Plan 01 succeeds when:
- `.claude-plugin/marketplace.json` is no longer a zero-byte placeholder.
- The file content matches the shape of RESEARCH.md Example 1 verbatim (modulo whitespace and the JSONC comments stripped to make it valid JSON).
- All three skill paths from D-01 (`./skills/api-recon-and-docs`, `./skills/401-403-bypass-techniques`, `./skills/api-auth-and-jwt-abuse`) appear in the correct plugin entries.
- `claude plugin validate` either exits cleanly OR exits non-zero only for a non-blocker reason (e.g., source-not-yet-cloned skill-path warning), with the verbatim output captured to `.planning/phases/01-schema-verification/.validate-output.txt`.
- No upstream `yaklang/hack-skills` file is modified (CLAUDE.md source-immutability).
- No git push happens (D-04 — local-path install only; publish is Phase 4).
</success_criteria>

<output>
Create `.planning/phases/01-schema-verification/01-01-SUMMARY.md` when done, including:
- The verbatim final content of `.claude-plugin/marketplace.json` (fenced JSON block).
- The verbatim stdout of `claude plugin validate` (fenced text block).
- One-line confirmation that all three D-01 skill paths are present and prefixed with `./`.
- Note on whether the validator output was clean, non-blocker-warning, or VALIDATOR_UNAVAILABLE.
</output>
</content>
</invoke>