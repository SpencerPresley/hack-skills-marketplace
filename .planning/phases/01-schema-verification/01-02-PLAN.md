---
phase: 01-schema-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .planning/phases/01-schema-verification/01-VERIFICATION.md
autonomous: false
requirements:
  - VERIFY-01
  - VERIFY-02
tags:
  - claude-code-plugin-marketplace
  - system-reminder-capture
  - context-isolation

must_haves:
  truths:
    - "After installing `hack-skills-auth-bypass` alone (per D-04 local-path install), `claude plugin details hack-skills-auth-bypass@hack-skills-marketplace` reports `Skills (2)` listing exactly `401-403-bypass-techniques` and `api-auth-and-jwt-abuse` (VERIFY-01 primary evidence)"
    - "After installing one of the test plugins with the test marketplace, the session's system-reminder available-skills block lists ONLY that plugin's curated skills — no other skills from yaklang/hack-skills' 102 appear (VERIFY-02 primary evidence per D-07)"
    - "`claude plugin details` on the installed plugin shows the same curated `Skills (N)` line as a sanity check on enabled state (VERIFY-02 secondary evidence per D-07)"
    - "Hygiene per D-10: between each VERIFY sub-test, the previous test plugin is uninstalled and the test marketplace is removed before the next install — except where the VERIFY-03 concurrent-install exception applies (Plan 03)"
    - "01-VERIFICATION.md exists at the D-05 phase-local location with sections for VERIFY-01 and VERIFY-02 per D-06 (yes/no answer, install command used, verbatim evidence, one-line conclusion)"
    - "All CLI output evidence is captured verbatim per CONTEXT.md §Specifics line 104 — exact tokens (SHA, paths) preserved, no paraphrasing"
  artifacts:
    - path: ".planning/phases/01-schema-verification/01-VERIFICATION.md"
      provides: "VERIFY-01 + VERIFY-02 sections with verbatim evidence per D-06"
      contains: "## VERIFY-01, ## VERIFY-02, Skills (2), available-skills"
  key_links:
    - from: ".claude-plugin/marketplace.json (Plan 01 output)"
      to: "~/.claude/plugins/installed_plugins.json"
      via: "claude plugin install --scope user"
      pattern: "hack-skills-(recon|auth-bypass)@hack-skills-marketplace"
    - from: "~/.claude/plugins/installed_plugins.json"
      to: "Claude Code session system-reminder"
      via: "interactive `claude` session enumerates enabled plugin skills"
      pattern: "- hack-skills-(recon|auth-bypass):"
---

<objective>
Install one (or both, sequentially per D-10 hygiene) test plugin(s) from Plan 01's marketplace.json via local-path, observe the runtime behavior of `strict: false` + curated `skills` array, and capture the evidence required to answer VERIFY-01 (individual-skill addressing) and VERIFY-02 (context isolation) into `01-VERIFICATION.md`.

Purpose: This is the actual experiment. RESEARCH.md assumptions A5 and A6 (the very claims under test) are MEDIUM-confidence — they are exactly what this plan resolves to HIGH or to a documented "no". Both VERIFY-01 and VERIFY-02 land in this plan because their evidence is observed in the same install/enable cycle: VERIFY-01's evidence is `claude plugin details`' "Skills (N)" line, which is captured immediately post-install; VERIFY-02's evidence is the in-session system-reminder excerpt, captured by launching `claude` interactively against the same install state. Running them as separate plans would require two install cycles for one experiment — wasteful, and D-10 hygiene state would be harder to reason about.

Output: `.planning/phases/01-schema-verification/01-VERIFICATION.md` with VERIFY-01 and VERIFY-02 sections fully populated per D-06's per-question structure (yes/no answer, install command, verbatim evidence, one-line conclusion). VERIFY-02 includes BOTH the system-reminder excerpt (primary, per D-07) AND the `claude plugin details` Skills line (secondary, per D-07).
</objective>

<execution_context>
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.planning/PROJECT.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.planning/REQUIREMENTS.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.planning/phases/01-schema-verification/01-CONTEXT.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.planning/phases/01-schema-verification/01-RESEARCH.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.planning/phases/01-schema-verification/01-PATTERNS.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/templates/verification-report.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.planning/phases/01-schema-verification/01-01-SUMMARY.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/CLAUDE.md

<interfaces>
<!-- CLI surface and expected output shapes for VERIFY-01 + VERIFY-02. -->
<!-- Verbatim copy-targets in RESEARCH.md Pattern 2 (lines 254–285), Example 3 (lines 444–477), Example 4 (lines 479–508). -->

Install/observe/uninstall procedure for ONE plugin at a time (D-10 hygiene applies between sub-tests):
  Pre-flight:
    claude --version                                                 # confirm 2.1.148 still installed (RESEARCH.md line 130)
  Step 1 — register marketplace:
    claude plugin marketplace add /Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace --scope user
  Step 2 — install single plugin (auth-bypass is the load-bearing one for VERIFY-01 because it has 2 skill paths):
    claude plugin install hack-skills-auth-bypass@hack-skills-marketplace --scope user
    claude plugin enable hack-skills-auth-bypass@hack-skills-marketplace
  Step 3 — capture details (VERIFY-01 primary, VERIFY-02 secondary):
    claude plugin details hack-skills-auth-bypass@hack-skills-marketplace
  Step 4 — capture installed_plugins.json snippet (VERIFY-01 secondary):
    cat ~/.claude/plugins/installed_plugins.json | jq '.plugins["hack-skills-auth-bypass@hack-skills-marketplace"]'
  Step 5 — system-reminder capture (VERIFY-02 primary):
    Launch interactive `claude` session.
    Inside the session, prompt: "Dump verbatim the section of your system reminder that lists available skills."
    Paste the model's verbatim response into 01-VERIFICATION.md as a fenced ``` code block.
  Step 6 — D-10 teardown before moving to next sub-test:
    claude plugin disable hack-skills-auth-bypass@hack-skills-marketplace
    claude plugin uninstall hack-skills-auth-bypass@hack-skills-marketplace --scope user -y
    claude plugin marketplace remove hack-skills-marketplace
  (Repeat Steps 1–6 for hack-skills-recon if needed to corroborate single-skill case; recon's `Skills (1)` is a useful sanity check on the auth-bypass `Skills (2)` result but is OPTIONAL — auth-bypass alone is what directly exercises VERIFY-01.)

Expected `claude plugin details` output shape (RESEARCH.md lines 455–467):
    hack-skills-auth-bypass
      Authentication and authorization bypass
      Source: hack-skills-auth-bypass@hack-skills-marketplace
    Component inventory
      Skills (2)  401-403-bypass-techniques, api-auth-and-jwt-abuse
      Agents (0)
      Hooks (0)
      MCP servers (0)
      LSP servers (0)

Expected system-reminder shape (RESEARCH.md line 491, citing issue #57515):
    # available-skills (heading may vary — capture verbatim, see Open Question 2 line 579)
    - hack-skills-auth-bypass:401-403-bypass-techniques: <description text>
    - hack-skills-auth-bypass:api-auth-and-jwt-abuse: <description text>

Pass/fail criteria (RESEARCH.md lines 469–471 for VERIFY-01, lines 495–497 for VERIFY-02):
  VERIFY-01 PASSES iff details' Skills (N) line lists exactly the two curated names and no others. FAILS if Skills (102) or any other count appears.
  VERIFY-02 PASSES iff exactly the two curated skill entries appear in the system reminder, no other yaklang/hack-skills skills. FAILS if any of the other 100 skills appear.

Known landmines (must surface in evidence section):
  - Pitfall 1 (RESEARCH.md line 327): description-text dropout in system reminder is NOT a VERIFY-02 failure. The load-bearing evidence is count + name list. Document any dropout, count as PASS.
  - Pitfall 5 (RESEARCH.md line 377): the on-disk cache contains all 102 skill subdirs (`strict: false` does not prune the clone). VERIFY-01 evidence is what `details` reports, NOT what `ls cache/` shows. Include an explicit note in 01-VERIFICATION.md.
  - Pitfall 4 (RESEARCH.md line 367): never uninstall via in-session TUI; always use `claude plugin uninstall ... -y` from a shell.

01-VERIFICATION.md frontmatter and section structure (CONTEXT.md D-05, D-06; Pattern Map lines 96–158):
  ---
  phase: 01-schema-verification
  verified: <ISO-8601 timestamp at completion>
  status: passed | gaps_found | human_needed
  score: <N>/3 VERIFY questions resolved
  ---
  # Phase 1: Schema Verification Report
  **Phase Goal:** Prove that `strict: false` + individual-skill paths in the `skills` array actually deliver curated, context-isolated, per-plugin-cached groups — before spending effort on classification and full build-out.
  **Verified:** <timestamp>
  **Status:** <enum>

  ## VERIFY-01: Individual Skill Path Addressing
  (Section body per D-06; see Plan content below for exact shape.)

  ## VERIFY-02: Context Isolation
  (Section body per D-06 with DUAL evidence per D-07.)

  (VERIFY-03 and Pivot Policy come in later plans.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install hack-skills-auth-bypass and capture VERIFY-01 + VERIFY-02-secondary evidence</name>
  <files>.planning/phases/01-schema-verification/.task01-evidence.txt</files>
  <read_first>
    - .planning/phases/01-schema-verification/01-01-SUMMARY.md (confirm marketplace.json was authored successfully and validator was clean)
    - .claude-plugin/marketplace.json (the file under test)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 254–285 (Pattern 2 — install/observe/uninstall cycle)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 444–477 (Example 3 — VERIFY-01 evidence collection, expected output shape)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 376–386 (Pitfall 5 — distinguish cache contents from runtime exposure)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 366–373 (Pitfall 4 — never use TUI for uninstall)
  </read_first>
  <action>
    Run the following commands IN ORDER and capture every command's exit code + verbatim stdout/stderr into `.planning/phases/01-schema-verification/.task01-evidence.txt` (dot-prefixed temp file; will be consumed by Task 3 and discarded after the verification artifact is finalized in Plan 04).

    1. `claude --version` — confirm CLI is 2.1.148 (RESEARCH.md line 130). If a different version, record it; do not abort unless the version is < 2.1.x.

    2. `claude plugin marketplace add /Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace --scope user` — register the test marketplace from local path per D-04. The absolute path is REQUIRED; do not use `./` or `.` (RESEARCH.md line 260 uses absolute path).

    3. `claude plugin install hack-skills-auth-bypass@hack-skills-marketplace --scope user` — install the 2-skill plugin (the one that directly exercises VERIFY-01).

    4. `claude plugin enable hack-skills-auth-bypass@hack-skills-marketplace` — enable it (Pitfall 2 at RESEARCH.md line 342: disabled plugins still register skills, so enable explicitly to be unambiguous).

    5. `claude plugin details hack-skills-auth-bypass@hack-skills-marketplace` — capture the "Component inventory" output. This is the PRIMARY VERIFY-01 evidence (RESEARCH.md line 454) and the SECONDARY VERIFY-02 evidence (RESEARCH.md line 506).

    6. `cat ~/.claude/plugins/installed_plugins.json` and pipe through `jq '.plugins["hack-skills-auth-bypass@hack-skills-marketplace"]'` if `jq` is available; otherwise capture the raw file. This is the SECONDARY VERIFY-01 evidence — the install metadata (installPath, version, gitCommitSha, scope) for the install-state trail. Per RESEARCH.md Assumption A2 (line 566): record the actual `version` token observed; do NOT predict it.

    7. `claude plugin list --json | jq '.[] | select(.id == "hack-skills-auth-bypass@hack-skills-marketplace")'` (or plain `claude plugin list` if `jq` is unavailable) — sanity-check enabled state.

    Do NOT yet uninstall — Task 2 (human-verify checkpoint) needs the install to remain live for system-reminder capture. The uninstall + teardown happens at the end of Task 3.

    Important hygiene: if `~/.claude/plugins/cache/hack-skills-marketplace/` already exists from a prior run, delete it with `rm -rf ~/.claude/plugins/cache/hack-skills-marketplace/` BEFORE Step 2 — Pitfall 1 (RESEARCH.md line 332) cites stale cache dirs as a cause of system-reminder description dropouts and Pitfall 3 confirms the cache is grace-period-orphaned after prior uninstalls.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-schema-verification/.task01-evidence.txt && grep -q "Skills (2)" .planning/phases/01-schema-verification/.task01-evidence.txt && grep -q "401-403-bypass-techniques" .planning/phases/01-schema-verification/.task01-evidence.txt && grep -q "api-auth-and-jwt-abuse" .planning/phases/01-schema-verification/.task01-evidence.txt && echo "VERIFY-01 evidence present"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-schema-verification/.task01-evidence.txt` exists with the verbatim stdout/stderr of every command 1–7 above (each section labeled with the command that produced it).
    - The captured `claude plugin details` output contains the literal substring `Skills (2)` (case-sensitive, exact match — RESEARCH.md line 463).
    - The captured `claude plugin details` output lists EXACTLY these two skill names and no others: `401-403-bypass-techniques`, `api-auth-and-jwt-abuse` (RESEARCH.md line 469 — pass criterion).
    - The captured `claude plugin list` or `installed_plugins.json` snippet shows the plugin with `scope: "user"` and `enabled: true`.
    - If `claude plugin details` shows `Skills (102)` or any count other than 2: the evidence file still gets written verbatim, but Task 1 acceptance FAILS and VERIFY-01 is answered "no" — escalate to the user via checkpoint per the pivot policy in CONTEXT.md D-09. Do not silently proceed.
    - The actual `version` token in `installed_plugins.json` is recorded verbatim (whatever string Claude Code resolves — likely the github SHA `c6f732be...` per RESEARCH.md A2, or `"unknown"`, or another token). Do not assume; record what is observed.
  </acceptance_criteria>
  <done>Evidence file exists, contains all 7 command sections verbatim, and the grep assertions above pass. If VERIFY-01 evidence is "Skills (2) 401-403-bypass-techniques, api-auth-and-jwt-abuse", Task 1 is complete. If VERIFY-01 evidence shows a different shape (Skills count != 2, or extra/missing skill names), Task 1 is FAILED — record the actual evidence verbatim and report to the user before continuing to Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human captures system-reminder block from a fresh `claude` interactive session</name>
  <files>.planning/phases/01-schema-verification/.task02-system-reminder.txt</files>
  <read_first>
    - .planning/phases/01-schema-verification/.task01-evidence.txt (Task 1 output; confirms `hack-skills-auth-bypass` is currently installed and enabled at scope user)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 287–291 (Pattern 3 — system-reminder capture procedure)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 482–504 (Example 4 — PRIMARY evidence shape for VERIFY-02)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 326–339 (Pitfall 1 — description dropout is NOT a VERIFY-02 failure)
    - .planning/phases/01-schema-verification/01-CONTEXT.md D-07 (dual-evidence requirement — system-reminder excerpt is the PRIMARY ground truth)
  </read_first>
  <action>
    THIS IS A HUMAN-VERIFY CHECKPOINT. The executor pauses here and waits for the human (Spencer) to perform the steps below; the executor does NOT execute them itself. The reason this step requires a human and cannot be automated: the system-reminder block is only visible inside a fresh interactive `claude` session that the human launches in a separate terminal — Claude Code does not expose that block via any CLI or API surface. The model inside that fresh session is the only observer; the human's job is to relay the model's verbatim output back into the planning artifact.

    Context that has been automated for the human (Task 1 already completed):
    - `claude plugin marketplace add /Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace --scope user` — DONE
    - `claude plugin install hack-skills-auth-bypass@hack-skills-marketplace --scope user` — DONE
    - `claude plugin enable hack-skills-auth-bypass@hack-skills-marketplace` — DONE
    The plugin is live on this machine RIGHT NOW. Do not re-run those commands.

    Human steps to perform (in order; relay the output back to the executor):

    1. Open a FRESH terminal window. Do not reuse the executor's terminal — start clean to avoid env contamination.

    2. Run `claude` (bare invocation, no flags, no file argument) to launch an interactive Claude Code session.

    3. Once the session is responsive, type EXACTLY this prompt as a single message (no surrounding quotes; just the literal text):
       Dump verbatim the section of your system reminder that lists available skills, as a fenced code block. Do not summarize. Do not rephrase. Show exactly what your context contains under that section.

    4. The model will reply with a fenced code block. Copy the ENTIRE fenced block including the heading line above the list (likely `# available-skills` per CONTEXT.md D-07, or a close variant per RESEARCH.md Open Question 2 at line 579 — capture whatever heading the model produces, do not normalize).

    5. Paste the verbatim copy into `.planning/phases/01-schema-verification/.task02-system-reminder.txt` as a single fenced ``` code block. Preserve every character including any description-dropout patterns or formatting oddities — verbatim per CONTEXT.md §Specifics line 104.

    6. Visually verify (BEFORE issuing the resume signal):
       a. The block contains exactly two skill entries of the form `- hack-skills-auth-bypass:<skill-name>` (with or without trailing description text per Pitfall 1).
       b. The skill names are EXACTLY `401-403-bypass-techniques` and `api-auth-and-jwt-abuse` and no others from yaklang/hack-skills (no `crypto-...`, no `active-directory-...`, no `mobile-...`, none of the other 100 upstream skills).
       c. If a description dropout is observed (entry renders as bare `- hack-skills-auth-bypass:<skill-name>` with no trailing `: <description>`), append a single-line note inline at the top of `.task02-system-reminder.txt` BEFORE the fenced block: `# Note: description dropout observed for <skill-name> — per Pitfall 1 this is NOT a VERIFY-02 failure; counts + names are the load-bearing evidence.`

    7. Exit the interactive `claude` session via `/exit` or Ctrl+D. Do NOT attempt to `/plugin uninstall` from inside the TUI (Pitfall 4 — RESEARCH.md line 367). Teardown happens in Task 3 via the CLI.

    8. Resume the executor by typing one of these resume signals back to it:
       - `approved` — the verbatim system-reminder block lists exactly the two curated auth-bypass skills and no others; VERIFY-02 PASS.
       - `dropout-only: <details>` — same as approved BUT one or both entries had a description dropout; still VERIFY-02 PASS per Pitfall 1; describe the dropout pattern.
       - `gaps: <details>` — the block contains skills beyond the curated two (any of the other 100 yaklang skills are visible); VERIFY-02 FAIL; describe which extra skills appeared.

    The executor must wait for one of the three resume signals before proceeding to Task 3. On `gaps:`, Task 3 must record VERIFY-02 = NO and Plan 04 will apply D-09's VERIFY-02 pivot stance.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-schema-verification/.task02-system-reminder.txt && test -s .planning/phases/01-schema-verification/.task02-system-reminder.txt && grep -q "hack-skills-auth-bypass:401-403-bypass-techniques" .planning/phases/01-schema-verification/.task02-system-reminder.txt && grep -q "hack-skills-auth-bypass:api-auth-and-jwt-abuse" .planning/phases/01-schema-verification/.task02-system-reminder.txt && echo "system-reminder captured (Task 3 will check for extra-skill leakage)"</automated>
    <human-check>Human has issued one of `approved`, `dropout-only: <details>`, or `gaps: <details>` as the resume signal. The signal text is the source of truth for the VERIFY-02 YES/NO answer Task 3 will record.</human-check>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-schema-verification/.task02-system-reminder.txt` exists and is non-empty.
    - The file contains the verbatim fenced code block from a fresh `claude` interactive session's system-reminder excerpt.
    - The file contains both literal substrings `hack-skills-auth-bypass:401-403-bypass-techniques` and `hack-skills-auth-bypass:api-auth-and-jwt-abuse` (the two curated skills' system-reminder entries).
    - The human has issued exactly one of the three resume signals: `approved`, `dropout-only: <details>`, or `gaps: <details>`. The signal is recorded in the executor's conversation state for Task 3 to consume.
    - If the resume signal is `gaps: <details>`, the `<details>` content identifies which extra skill(s) from yaklang/hack-skills appeared in the system reminder beyond the curated two — this is the failure-mode evidence Plan 04 will key off.
    - The executor did NOT attempt to capture the system reminder programmatically (this is impossible — Claude Code does not expose the system reminder via any CLI/API).
  </acceptance_criteria>
  <resume-signal>approved | dropout-only: &lt;details&gt; | gaps: &lt;details&gt;</resume-signal>
  <done>The verbatim system-reminder excerpt file exists with both expected skill entries present. The human has issued one of the three resume signals and the executor has recorded it. Task 3 can now derive VERIFY-02 = YES (approved or dropout-only) or NO (gaps) from the recorded signal.</done>
</task>


<task type="auto">
  <name>Task 3: Write VERIFY-01 and VERIFY-02 sections of 01-VERIFICATION.md and tear down install state</name>
  <files>.planning/phases/01-schema-verification/01-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/01-schema-verification/.task01-evidence.txt (Task 1 output: claude plugin details + installed_plugins.json + claude plugin list)
    - .planning/phases/01-schema-verification/.task02-system-reminder.txt (Task 2 output: verbatim system-reminder block)
    - .planning/phases/01-schema-verification/01-CONTEXT.md D-05, D-06, D-07 (artifact location, per-question section structure, dual-evidence requirement for VERIFY-02)
    - .planning/phases/01-schema-verification/01-PATTERNS.md lines 158–179 (suggested per-VERIFY section shape sketch)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 326–339 (Pitfall 1 — description dropout caveat to call out in VERIFY-02)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 376–386 (Pitfall 5 — explicit cache-vs-exposure note to add to VERIFY-01)
    - .claude/get-shit-done/templates/verification-report.md lines 9–22 (frontmatter shape — borrow as-is per Pattern Map line 105)
  </read_first>
  <action>
    Write `.planning/phases/01-schema-verification/01-VERIFICATION.md` (this is a CREATE — file does not yet exist; Plans 03 and 04 will APPEND to it). Use the Write tool, not heredoc.

    File structure (exact section order; use this shape):

    1. Frontmatter per the GSD template (Pattern Map lines 104–118):
       - `phase: 01-schema-verification`
       - `verified: <current ISO-8601 UTC timestamp>` — fill in via `date -u +%Y-%m-%dT%H:%M:%SZ`
       - `status: in_progress` (will be flipped to `passed`/`gaps_found`/`human_needed` by Plan 04 when the artifact is finalized)
       - `score: 2/3 VERIFY questions resolved` (VERIFY-03 lands in Plan 03)

    2. H1 title: `# Phase 1: Schema Verification Report`

    3. Header paragraph stating phase goal (verbatim from ROADMAP.md §Phase 1 line 23: "Prove that `strict: false` + individual-skill paths in the `skills` array actually deliver curated, context-isolated, per-plugin-cached groups — before spending effort on classification and full build-out"), verification timestamp, and current status.

    4. `## VERIFY-01: Individual Skill Path Addressing` section per D-06. Required subsections:
       - **Answer:** YES or NO (derived from Task 1 acceptance — if `Skills (2)` matched, answer is YES).
       - **Plugin under test:** `hack-skills-auth-bypass@hack-skills-marketplace`
       - **Install command:** fenced bash block containing the verbatim command from Task 1 Step 3.
       - **Primary evidence:** fenced text block containing the verbatim `claude plugin details ...` output from `.task01-evidence.txt` (the "Component inventory" block including the `Skills (2)` line).
       - **Secondary evidence:** fenced text block containing the verbatim `installed_plugins.json` entry for this plugin (showing the resolved `version` token, `gitCommitSha`, `scope`, `installPath`).
       - **Cache-vs-exposure note** (per Pitfall 5): a paragraph stating that the on-disk cache at `~/.claude/plugins/cache/hack-skills-marketplace/hack-skills-auth-bypass/<version-token>/skills/` contains all 102 upstream skill subdirectories — this is expected and does not constitute a VERIFY-01 failure; VERIFY-01 evidence is the `Skills (N)` line from `details`, not the cache file count.
       - **Conclusion:** one sentence linking the evidence to the yes/no.

    5. `## VERIFY-02: Context Isolation` section per D-06 and D-07. Required subsections:
       - **Answer:** YES or NO (derived from Task 2 resume signal — `approved` or `dropout-only` → YES; `gaps` → NO).
       - **Plugin under test:** `hack-skills-auth-bypass@hack-skills-marketplace`
       - **Install command:** same as VERIFY-01 (same install state).
       - **Primary evidence (system-reminder excerpt, D-07 ground truth):** verbatim contents of `.task02-system-reminder.txt` as a fenced ``` code block. Do NOT paraphrase. If a heading variant was captured (e.g., not literally `# available-skills`), keep whatever heading the model produced.
       - **Secondary evidence (`claude plugin details` sanity check, D-07):** the same `Skills (2)` block reused from VERIFY-01's primary evidence — paste it again here in a fenced block so VERIFY-02 is self-contained.
       - **Description dropout note** (per Pitfall 1): if Task 2 reported `dropout-only`, include a paragraph noting the dropout, citing issue #57515, and stating that per the pre-decided pass criterion (count + names match), VERIFY-02 still passes. If no dropout was observed, include a one-line note that the system reminder rendered descriptions cleanly.
       - **Conclusion:** one sentence linking the evidence to the yes/no.

    6. Placeholder section: `## VERIFY-03: Cache Behavior` with a single line `_(populated by Plan 03)_` — Plans 03 will replace this placeholder with the full VERIFY-03 section.

    7. Placeholder section: `## Pivot Policy` with a single line `_(populated by Plan 04)_` — Plan 04 will replace this placeholder.

    After writing 01-VERIFICATION.md, run the D-10 teardown commands (per the locked decision; this prepares clean state for Plan 03's "both installed concurrently" exception):

    8. `claude plugin disable hack-skills-auth-bypass@hack-skills-marketplace`
    9. `claude plugin uninstall hack-skills-auth-bypass@hack-skills-marketplace --scope user -y` (the `-y` is required — Pitfall 4 prohibits the TUI; RESEARCH.md line 268 shows the canonical form).
    10. `claude plugin marketplace remove hack-skills-marketplace`
    11. `claude plugin list --json | jq '[.[] | select(.id | startswith("hack-skills-"))]'` (or `claude plugin list` without jq) — confirm no `hack-skills-*` entries remain.
    12. `claude plugin marketplace list` — confirm `hack-skills-marketplace` is not listed.

    Capture stdout of commands 8–12 to `.planning/phases/01-schema-verification/.task03-teardown.txt` (so Plan 03 has proof that hygiene state is clean before it starts).

    Note on orphaned cache (per Pitfall 3, RESEARCH.md line 354): `~/.claude/plugins/cache/hack-skills-marketplace/hack-skills-auth-bypass/<version>/` will persist on disk for ~7 days after uninstall. This is intentional Claude Code behavior, not a Plan 02 failure. Plan 03 needs to delete this cache subdir BEFORE its install cycle to keep VERIFY-03 evidence clean (Pitfall 1). Document this fact in `.task03-teardown.txt` as a note line — Plan 03 will key off it.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-schema-verification/01-VERIFICATION.md && grep -c "^## VERIFY-01" .planning/phases/01-schema-verification/01-VERIFICATION.md | grep -q "^1$" && grep -c "^## VERIFY-02" .planning/phases/01-schema-verification/01-VERIFICATION.md | grep -q "^1$" && grep -q "Skills (2)" .planning/phases/01-schema-verification/01-VERIFICATION.md && grep -q "hack-skills-auth-bypass:401-403-bypass-techniques" .planning/phases/01-schema-verification/01-VERIFICATION.md && grep -q "hack-skills-auth-bypass:api-auth-and-jwt-abuse" .planning/phases/01-schema-verification/01-VERIFICATION.md && test -f .planning/phases/01-schema-verification/.task03-teardown.txt && ! (claude plugin list 2>/dev/null | grep -q "^hack-skills-") && echo "VERIFY-01/02 documented, teardown clean"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-schema-verification/01-VERIFICATION.md` exists with valid YAML frontmatter (phase, verified, status, score keys present).
    - The file contains exactly one `## VERIFY-01` section and exactly one `## VERIFY-02` section (grep `-c` on the heading prefix returns 1 for each).
    - VERIFY-01 section contains the verbatim substring `Skills (2)` and both skill names `401-403-bypass-techniques` and `api-auth-and-jwt-abuse`.
    - VERIFY-02 section contains the verbatim substrings `hack-skills-auth-bypass:401-403-bypass-techniques` and `hack-skills-auth-bypass:api-auth-and-jwt-abuse` (the system-reminder skill-entry shape from RESEARCH.md line 491).
    - VERIFY-02 section explicitly references D-07 dual evidence — both the system-reminder excerpt (PRIMARY) and the `claude plugin details` Skills line (SECONDARY) are present as fenced blocks.
    - The file contains placeholder sections `## VERIFY-03: Cache Behavior` and `## Pivot Policy` whose bodies are the literal string `_(populated by Plan 03)_` and `_(populated by Plan 04)_` respectively.
    - The pitfall-5 cache-vs-exposure note appears in VERIFY-01.
    - The pitfall-1 description-dropout note appears in VERIFY-02 (either documenting an observed dropout or stating none was observed).
    - `claude plugin list` (after teardown) shows no `hack-skills-*` entries.
    - `claude plugin marketplace list` shows no `hack-skills-marketplace` entry.
    - `.task03-teardown.txt` exists and includes the orphaned-cache note line for Plan 03.
  </acceptance_criteria>
  <done>01-VERIFICATION.md contains complete VERIFY-01 and VERIFY-02 sections with verbatim evidence per D-06/D-07. VERIFY-03 and Pivot Policy are placeholders. The install state is fully torn down (no plugins listed, marketplace removed). The orphaned-cache note for Plan 03 is recorded in .task03-teardown.txt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local marketplace.json → `~/.claude/plugins/known_marketplaces.json` | `claude plugin marketplace add` writes user state on this machine. Trusted boundary (user invokes intentionally). |
| Local marketplace declaration → yaklang/hack-skills upstream | First `claude plugin install` clones `github.com/yaklang/hack-skills` into the local cache. Upstream is trusted per PROJECT.md but the content crossing the boundary is markdown — not executed at install. |
| Claude Code session runtime → system-reminder injection | The model surfaces enabled-plugin skill metadata as the in-session system reminder. This is the surface VERIFY-02 measures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | yaklang/hack-skills upstream content between Plan 01 (author-time) and Plan 02 (install-time, FIRST upstream clone) | mitigate | Per RESEARCH.md A2 (line 566): record the actual `gitCommitSha` resolved by Claude Code into `installed_plugins.json` verbatim in 01-VERIFICATION.md. Reproducibility of the VERIFY-01/02 result is anchored to that SHA, not to "HEAD at some point" — the artifact captures the exact commit verified. If the upstream HEAD has advanced since Plan 01, the verification still binds to the specific SHA cloned. |
| T-02-02 | Tampering | Upstream skill `SKILL.md` content malicious between verification runs | accept | yaklang/hack-skills is documented trusted upstream (PROJECT.md "trusted upstream per PROJECT.md"). The verification artifact records the exact SHA verified. Subsequent phases (and future re-runs) will surface SHA drift via re-validation. ASVS V2 lightly applies; standard control: rely on PROJECT.md trust assertion + git's content-addressed SHAs. No active scanning of skill content in scope for Phase 1. |
| T-02-03 | Information disclosure | system-reminder excerpt pasted into 01-VERIFICATION.md | accept | Skill descriptions are public upstream content. No secrets or PII surface in this evidence. The verbatim paste is required (CONTEXT.md §Specifics line 104) and acceptable. |
| T-02-04 | Tampering | Test hygiene state leakage (orphaned cache from prior runs polluting VERIFY-02 measurement) | mitigate | Pitfall 1 (RESEARCH.md line 326) explicitly cites stale cache dirs as a description-dropout cause. Task 1 action requires `rm -rf ~/.claude/plugins/cache/hack-skills-marketplace/` before Step 2 if a prior cache exists. Task 3 documents the post-uninstall orphaned-cache state for Plan 03's pre-test cleanup. |
| T-02-05 | Spoofing | Local-path marketplace registration accepting a path that is not this repo | mitigate | The `marketplace add` command uses the absolute path `/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace`. Relative paths (`./`) are rejected by the design of this plan to eliminate working-directory ambiguity. |
| T-02-06 | Tampering | TUI uninstall path being used (Pitfall 4, RESEARCH.md line 367) | mitigate | All uninstall commands explicitly use `claude plugin uninstall ... --scope user -y` from a shell. The plan never invokes the in-session `/plugin uninstall` TUI. Task 3 verifies post-teardown state with `claude plugin list --json`. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. `claude plugin install` is a declarative skill-loader (PROJECT.md "skill-only curations"); no hooks scripts run at install. RESEARCH.md Package Legitimacy Audit (line 134) explicitly marks the audit as not-applicable for this phase. |
</threat_model>

<verification>
- After Task 3: `grep -c "^## VERIFY-0[12]" .planning/phases/01-schema-verification/01-VERIFICATION.md` returns 2.
- `grep -E "^status:" .planning/phases/01-schema-verification/01-VERIFICATION.md` returns `status: in_progress`.
- `claude plugin list 2>&1 | grep -c "^hack-skills-"` returns 0 (post-teardown clean).
- `claude plugin marketplace list 2>&1 | grep -c "^hack-skills-marketplace"` returns 0.
- `.planning/phases/01-schema-verification/.task01-evidence.txt`, `.task02-system-reminder.txt`, and `.task03-teardown.txt` all exist.
</verification>

<success_criteria>
Plan 02 succeeds when:
- VERIFY-01 has a documented yes/no answer with verbatim `claude plugin details` evidence in 01-VERIFICATION.md.
- VERIFY-02 has a documented yes/no answer with BOTH the verbatim system-reminder excerpt (primary, per D-07) AND the verbatim `claude plugin details` Skills line (secondary, per D-07) in 01-VERIFICATION.md.
- The Pitfall 5 cache-vs-exposure caveat appears in the VERIFY-01 section.
- The Pitfall 1 description-dropout note appears in the VERIFY-02 section (with or without an observed dropout — the note is unconditional).
- Install state is fully cleaned: `claude plugin list` shows no `hack-skills-*` entries; `claude plugin marketplace list` shows no `hack-skills-marketplace`.
- The orphaned-cache state for Plan 03 is documented in `.task03-teardown.txt`.
- If VERIFY-01 or VERIFY-02 evidence DOES NOT match the pass criteria (Skills count != 2; or skills from outside the curated set appear in the system reminder): the artifact records the verbatim evidence anyway, sets `status: gaps_found` in the frontmatter (overriding the default `in_progress`), and Plan 04 will apply the D-09 pre-decided pivot stance for that specific VERIFY question.
</success_criteria>

<output>
Create `.planning/phases/01-schema-verification/01-02-SUMMARY.md` when done, including:
- VERIFY-01 answer (YES | NO) and a one-line reason.
- VERIFY-02 answer (YES | NO) and a one-line reason.
- The exact `version` / `gitCommitSha` token observed in `installed_plugins.json` for the auth-bypass plugin (record verbatim per RESEARCH.md A2).
- Whether a description dropout was observed (yes / no) and which skills were affected.
- Confirmation that teardown is clean (`claude plugin list` and `claude plugin marketplace list` outputs both empty of `hack-skills-*`).
- A pointer line for Plan 03: "Orphaned cache state at `~/.claude/plugins/cache/hack-skills-marketplace/hack-skills-auth-bypass/<version>/` — Plan 03 must `rm -rf` this before its install cycle per Pitfall 1."
</output>
</content>
</invoke>