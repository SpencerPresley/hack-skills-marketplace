---
phase: 01-schema-verification
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .planning/phases/01-schema-verification/01-VERIFICATION.md
autonomous: true
requirements:
  - VERIFY-03
tags:
  - claude-code-plugin-marketplace
  - per-plugin-cache
  - concurrent-install

must_haves:
  truths:
    - "Both `hack-skills-recon` and `hack-skills-auth-bypass` install concurrently from the same `yaklang/hack-skills` source without error (D-10 concurrent-install exception)"
    - "After both installs, `~/.claude/plugins/cache/hack-skills-marketplace/` contains TWO separate per-plugin subdirectories: `hack-skills-recon/<version>/` and `hack-skills-auth-bypass/<version>/` (VERIFY-03 pass criterion per RESEARCH.md line 532)"
    - "The strict-pass assertion holds: `find ~/.claude/plugins/cache/hack-skills-marketplace -mindepth 2 -maxdepth 2 -type d | wc -l` equals exactly 2 (RESEARCH.md line 538)"
    - "Both plugins report enabled state via `claude plugin list --json` (sanity check on concurrent enable)"
    - "Cache size per plugin is recorded via `du -sh` per CONTEXT.md §Specifics line 105 — confirms the 'small for text-only repo' assumption"
    - "01-VERIFICATION.md gets its VERIFY-03 section appended per D-06 (yes/no answer, install commands, verbatim cache tree, one-line conclusion)"
    - "Final D-10 teardown clears install state but documents the orphaned-cache grace-period behavior (Pitfall 3) so the residual cache dirs are not misread as a Plan 03 failure"
  artifacts:
    - path: ".planning/phases/01-schema-verification/01-VERIFICATION.md"
      provides: "VERIFY-03 section appended with verbatim cache-tree evidence per D-06"
      contains: "## VERIFY-03: Cache Behavior, find ~/.claude/plugins/cache, mindepth 2 -maxdepth 2"
  key_links:
    - from: ".claude-plugin/marketplace.json plugin entries (Plan 01 output)"
      to: "~/.claude/plugins/cache/hack-skills-marketplace/<plugin-name>/<version>/"
      via: "claude plugin install (one per plugin, no teardown between)"
      pattern: "hack-skills-marketplace/(hack-skills-recon|hack-skills-auth-bypass)/"
    - from: "Plan 02 teardown state"
      to: "Plan 03 pre-test state"
      via: "rm -rf orphaned cache subdir before fresh install"
      pattern: "rm -rf ~/.claude/plugins/cache/hack-skills-marketplace"
---

<objective>
Install both test plugins from Plan 01's marketplace.json concurrently (D-10's explicit exception — no teardown between installs) and capture the cache-layout evidence required to answer VERIFY-03: do multiple plugin entries sharing the same `yaklang/hack-skills` source produce separate cache entries without collision?

Purpose: This is the second distinct experiment of the phase. Unlike VERIFY-01 and VERIFY-02 (which share an install cycle and live in Plan 02), VERIFY-03 requires BOTH plugins installed simultaneously — which inverts the D-10 hygiene model. The evidence is purely filesystem-level (cache tree shape), so no human-verify checkpoint is needed; this plan is autonomous. The risk surfaced by RESEARCH.md Pitfall 6 (line 388) — confusing "matching version slots from same source" with "cache collision" — is handled by the strict-pass assertion (RESEARCH.md line 538: `find ... -mindepth 2 -maxdepth 2 -type d | wc -l` must equal 2). The pre-test cleanup of Plan 02's orphaned cache (Pitfall 1, Pitfall 3) is required to keep the measurement clean.

Output: VERIFY-03 section appended to `.planning/phases/01-schema-verification/01-VERIFICATION.md` per D-06 (yes/no answer, install command, verbatim cache-tree output, `du -sh` sizes, one-line conclusion). Final teardown leaves the machine clean.
</objective>

<execution_context>
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.planning/REQUIREMENTS.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.planning/phases/01-schema-verification/01-CONTEXT.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.planning/phases/01-schema-verification/01-RESEARCH.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.planning/phases/01-schema-verification/01-PATTERNS.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.planning/phases/01-schema-verification/01-02-SUMMARY.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/CLAUDE.md

<interfaces>
<!-- Cache-tree inspection surface for VERIFY-03. -->
<!-- Verbatim copy-target in RESEARCH.md Pattern 2 lines 271–284 (concurrent install sequence) and Example 5 lines 510–548 (cache-tree assertion, du size, list sanity). -->

VERIFY-03 install/observe/uninstall procedure (concurrent installs — D-10 exception applies; no teardown between the two installs):

  Pre-cleanup (Pitfall 1 + Pitfall 3 mitigation — required because Plan 02's uninstall orphaned the cache):
    rm -rf ~/.claude/plugins/cache/hack-skills-marketplace
    (Safe: no other plugins share that subdir tree per RESEARCH.md line 361.)

  Concurrent install (RESEARCH.md lines 273–276):
    claude plugin marketplace add /Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace --scope user
    claude plugin install hack-skills-recon@hack-skills-marketplace --scope user
    claude plugin install hack-skills-auth-bypass@hack-skills-marketplace --scope user
    claude plugin enable hack-skills-recon@hack-skills-marketplace
    claude plugin enable hack-skills-auth-bypass@hack-skills-marketplace

  Cache-tree capture (RESEARCH.md lines 277–278; the PRIMARY VERIFY-03 evidence):
    find ~/.claude/plugins/cache/hack-skills-marketplace -maxdepth 3 -type d
    du -sh ~/.claude/plugins/cache/hack-skills-marketplace/*/* 2>/dev/null

  Strict-pass assertion (RESEARCH.md lines 538–540 — the load-bearing pass/fail check):
    [ "$(find ~/.claude/plugins/cache/hack-skills-marketplace -mindepth 2 -maxdepth 2 -type d | wc -l)" -eq 2 ]
    Expected outcome: exit 0 (== 2 subdirs found, one per plugin name).
    Failure outcome: exit 1 (anything other than 2 — collision, partial install, or missing subdir).

  Enabled-state sanity check (RESEARCH.md line 546):
    claude plugin list --json | jq '.[] | select(.id | startswith("hack-skills-")) | {id, version, scope, enabled}'

  Final teardown (RESEARCH.md lines 280–284 — D-10 reapplies after evidence is captured):
    claude plugin disable hack-skills-recon@hack-skills-marketplace
    claude plugin disable hack-skills-auth-bypass@hack-skills-marketplace
    claude plugin uninstall hack-skills-recon@hack-skills-marketplace --scope user -y
    claude plugin uninstall hack-skills-auth-bypass@hack-skills-marketplace --scope user -y
    claude plugin marketplace remove hack-skills-marketplace

Expected cache-tree output shape (RESEARCH.md lines 523–530 — version-token is observed, not predicted, per A2):
  /Users/spencerpresley/.claude/plugins/cache/hack-skills-marketplace
  /Users/spencerpresley/.claude/plugins/cache/hack-skills-marketplace/hack-skills-recon
  /Users/spencerpresley/.claude/plugins/cache/hack-skills-marketplace/hack-skills-recon/<version-token>
  /Users/spencerpresley/.claude/plugins/cache/hack-skills-marketplace/hack-skills-auth-bypass
  /Users/spencerpresley/.claude/plugins/cache/hack-skills-marketplace/hack-skills-auth-bypass/<version-token>

Pitfall 6 (RESEARCH.md line 388 — version-slot-vs-collision confusion):
  Both plugins source from the same `yaklang/hack-skills@HEAD` so the `<version-token>` is likely IDENTICAL between the two subdirs (e.g., both `c6f732be...`). This is NOT a collision — the partitioning is by `<plugin-name>` directory level, NOT by version token. The strict-pass assertion above counts subdirs at depth 2 (the `<plugin-name>` level), which is the correct level.

01-VERIFICATION.md section append (CONTEXT.md D-06):
  Plan 02 created a placeholder `## VERIFY-03: Cache Behavior` section with body `_(populated by Plan 03)_`. Plan 03 REPLACES that placeholder with the full populated section.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-cleanup orphaned cache and install both plugins concurrently</name>
  <files>.planning/phases/01-schema-verification/.task03-verify03-evidence.txt</files>
  <read_first>
    - .planning/phases/01-schema-verification/01-02-SUMMARY.md (confirm Plan 02 teardown was clean and read the orphaned-cache path note)
    - .planning/phases/01-schema-verification/.task03-teardown.txt (Plan 02 output documenting orphaned-cache state at `~/.claude/plugins/cache/hack-skills-marketplace/hack-skills-auth-bypass/<version>/`)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 271–284 (Pattern 2 — concurrent install sequence)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 510–548 (Example 5 — cache-tree assertion)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 326–339 (Pitfall 1 — stale cache → description dropout)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 352–363 (Pitfall 3 — uninstall leaves orphaned cache)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 388–397 (Pitfall 6 — version-slot-vs-collision)
  </read_first>
  <action>
    Run the following commands IN ORDER and capture every command's exit code + verbatim stdout/stderr into `.planning/phases/01-schema-verification/.task03-verify03-evidence.txt`. Label each command section with the command that produced it.

    1. **Pre-cleanup (Pitfall 1 + Pitfall 3 mitigation):**
       `rm -rf ~/.claude/plugins/cache/hack-skills-marketplace` — clears the orphaned cache subdir left behind by Plan 02's uninstall. Safe per RESEARCH.md line 361. Then `test ! -d ~/.claude/plugins/cache/hack-skills-marketplace && echo "cache cleared" || echo "cache still present — investigate"` to confirm.

    2. **Confirm Plan 02 teardown still holds (no leftover plugin entries):**
       `claude plugin list --json 2>&1 | grep -c hack-skills- || true` — record output; expected 0. If non-zero, run the canonical teardown commands from RESEARCH.md lines 280–284 first to clean up before proceeding.
       `claude plugin marketplace list 2>&1 | grep -c hack-skills-marketplace || true` — record output; expected 0.

    3. **Concurrent install (D-10 exception applies — no teardown between the two installs; this is the only place in Phase 1 where both are installed simultaneously):**
       `claude plugin marketplace add /Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace --scope user`
       `claude plugin install hack-skills-recon@hack-skills-marketplace --scope user`
       `claude plugin install hack-skills-auth-bypass@hack-skills-marketplace --scope user`
       `claude plugin enable hack-skills-recon@hack-skills-marketplace`
       `claude plugin enable hack-skills-auth-bypass@hack-skills-marketplace`

    4. **Capture cache tree (PRIMARY VERIFY-03 evidence):**
       `find ~/.claude/plugins/cache/hack-skills-marketplace -maxdepth 3 -type d` — captures the verbatim tree shape. Expected to show 5 paths total per RESEARCH.md lines 523–530 (root + two `<plugin-name>` dirs + two `<plugin-name>/<version-token>` dirs).

    5. **Run strict-pass assertion (the load-bearing check):**
       `find ~/.claude/plugins/cache/hack-skills-marketplace -mindepth 2 -maxdepth 2 -type d | wc -l` — capture stdout and exit code. The strict pass condition is: this command outputs the integer `2`. Anything else is a VERIFY-03 fail.

    6. **Capture per-plugin cache sizes (CONTEXT.md §Specifics line 105 — confirms 'small for text-only repo' assumption):**
       `du -sh ~/.claude/plugins/cache/hack-skills-marketplace/*/* 2>/dev/null` — record verbatim. Two lines expected, one per plugin.

    7. **Enabled-state sanity (RESEARCH.md line 546):**
       `claude plugin list --json` (and if `jq` is available also pipe through `jq '.[] | select(.id | startswith("hack-skills-")) | {id, version, scope, enabled}'`) — confirm both plugins show `scope: "user"` and `enabled: true`.

    Important: do NOT yet uninstall — Task 2 needs the cache state observable while writing the VERIFY-03 section. Teardown happens in Task 3.

    On strict-pass-assertion FAILURE: the evidence file still gets written verbatim (it is the evidence record). Task 1's acceptance criteria flag the failure, and Plan 04 will key off the `gaps_found` status to apply D-09's VERIFY-03 pivot stance.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-schema-verification/.task03-verify03-evidence.txt && grep -q "hack-skills-recon" .planning/phases/01-schema-verification/.task03-verify03-evidence.txt && grep -q "hack-skills-auth-bypass" .planning/phases/01-schema-verification/.task03-verify03-evidence.txt && SUBDIR_COUNT=$(find ~/.claude/plugins/cache/hack-skills-marketplace -mindepth 2 -maxdepth 2 -type d 2>/dev/null | wc -l | tr -d ' ') && test "$SUBDIR_COUNT" = "2" && echo "VERIFY-03 cache evidence: 2 parallel subdirs"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-schema-verification/.task03-verify03-evidence.txt` exists with the verbatim stdout/stderr of every command 1–7 above, each section labeled.
    - The evidence file's pre-cleanup section contains the literal string `cache cleared` OR equivalent confirmation that the orphaned cache subdir was removed before the install cycle.
    - The evidence file's cache-tree section (command 4) contains BOTH literal substrings: `hack-skills-recon` and `hack-skills-auth-bypass` (the two plugin-name dirs under the marketplace cache root).
    - Command 5's stdout is the literal `2` (followed by a newline). If it is anything else, Task 1 STILL completes (evidence is captured) but the VERIFY-03 answer is "no" and Task 2 writes the section accordingly.
    - Command 6's stdout shows exactly two `du -sh` lines, one ending in `.../hack-skills-recon/<version-token>` and one ending in `.../hack-skills-auth-bypass/<version-token>` (size column varies — capture verbatim).
    - Command 7's output shows both `hack-skills-recon@hack-skills-marketplace` and `hack-skills-auth-bypass@hack-skills-marketplace` with `scope: "user"` and `enabled: true` (per `claude plugin list --json`).
    - If `claude plugin install` fails for either plugin (e.g., network failure cloning yaklang/hack-skills): evidence file still records the failure verbatim, Task 1 FAILS, and the orchestrator escalates per RESEARCH.md Environment Availability table (internet connectivity required at install time, no fallback).
  </acceptance_criteria>
  <done>Evidence file exists, contains all 7 command sections verbatim, the strict-pass assertion outputs `2`, both plugins show enabled in `claude plugin list`. If the strict-pass assertion outputs anything other than `2`, Task 1 completes with VERIFY-03 documented as "no" and the actual subdir count recorded — proceed to Task 2 to write the section.</done>
</task>

<task type="auto">
  <name>Task 2: Append VERIFY-03 section to 01-VERIFICATION.md</name>
  <files>.planning/phases/01-schema-verification/01-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/01-schema-verification/01-VERIFICATION.md (current state from Plan 02 — has VERIFY-01, VERIFY-02 sections plus `## VERIFY-03: Cache Behavior` placeholder line `_(populated by Plan 03)_`)
    - .planning/phases/01-schema-verification/.task03-verify03-evidence.txt (Task 1 output: full evidence trail)
    - .planning/phases/01-schema-verification/01-CONTEXT.md D-06 (per-question section structure)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 388–397 (Pitfall 6 — version-slot-vs-collision; must be called out in VERIFY-03 section)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 352–363 (Pitfall 3 — orphaned-cache caveat)
  </read_first>
  <action>
    Edit `.planning/phases/01-schema-verification/01-VERIFICATION.md` to REPLACE the placeholder line `_(populated by Plan 03)_` under the `## VERIFY-03: Cache Behavior` heading with a full populated section per D-06. Do NOT touch the VERIFY-01 or VERIFY-02 sections (those are Plan 02's output). Do NOT touch the `## Pivot Policy` placeholder (that is Plan 04's).

    VERIFY-03 section body (D-06 per-question structure):

    - **Answer:** YES or NO. Derive from Task 1 strict-pass-assertion result: if command 5 output `2` → YES; anything else → NO. Cite the exact subdir count observed.

    - **Plugins under test:** `hack-skills-recon@hack-skills-marketplace` and `hack-skills-auth-bypass@hack-skills-marketplace` (BOTH installed concurrently per D-10 exception).

    - **Pre-cleanup command:** fenced bash block containing the `rm -rf ~/.claude/plugins/cache/hack-skills-marketplace` line from Task 1 step 1, plus the one-line note that this was required per Pitfall 1 + Pitfall 3 to clear the orphaned cache from Plan 02's uninstall.

    - **Install command sequence:** fenced bash block containing the five install/enable commands from Task 1 step 3 (verbatim, in order).

    - **Primary evidence — cache tree (`find` output):** fenced text block containing the verbatim stdout of `find ~/.claude/plugins/cache/hack-skills-marketplace -maxdepth 3 -type d` from `.task03-verify03-evidence.txt`. Preserve the full absolute paths including the resolved `<version-token>`.

    - **Strict-pass assertion result:** one-line statement of the form `find ... -mindepth 2 -maxdepth 2 -type d | wc -l = <observed-integer>` showing the actual count.

    - **Secondary evidence — cache sizes (`du -sh` output):** fenced text block with the verbatim `du -sh` output from Task 1 step 6 (two lines, one per plugin). Per CONTEXT.md §Specifics line 105, this confirms the "small for text-only repo" assumption with a real byte count.

    - **Tertiary evidence — install metadata (`claude plugin list --json` output):** fenced text block with the verbatim relevant entries showing both plugins enabled at scope user.

    - **Pitfall 6 note (version-slot-vs-collision):** a paragraph stating that if both plugins resolved to the same `<version-token>` (likely the github commit SHA — RESEARCH.md A2), this is NOT a collision because the per-plugin partitioning happens at the `<plugin-name>` directory level (one level above `<version-token>`). Cite the actual observed tokens.

    - **Pitfall 3 note (orphaned-cache caveat):** a paragraph noting that after the final teardown in Task 3, the cache subdirs at `~/.claude/plugins/cache/hack-skills-marketplace/<plugin-name>/<version>/` will persist for ~7 days (Claude Code grace period). Future re-runs of Phase 1 must re-apply the Task 1 pre-cleanup. This is a documented landmine, not a VERIFY-03 failure.

    - **Conclusion:** one sentence linking the strict-pass assertion to the yes/no.

    After replacing the placeholder, update the frontmatter's `score:` field from `2/3 VERIFY questions resolved` to `3/3 VERIFY questions resolved`. Leave `status:` as `in_progress` — Plan 04 will set the final terminal status when the Pivot Policy section is populated.
  </action>
  <verify>
    <automated>grep -c "^## VERIFY-03: Cache Behavior$" .planning/phases/01-schema-verification/01-VERIFICATION.md | grep -q "^1$" && ! grep -q "_(populated by Plan 03)_" .planning/phases/01-schema-verification/01-VERIFICATION.md && grep -q "hack-skills-recon" .planning/phases/01-schema-verification/01-VERIFICATION.md && grep -q "hack-skills-auth-bypass" .planning/phases/01-schema-verification/01-VERIFICATION.md && grep -q "mindepth 2 -maxdepth 2" .planning/phases/01-schema-verification/01-VERIFICATION.md && grep -q "Pitfall 6" .planning/phases/01-schema-verification/01-VERIFICATION.md && grep -E "^score:" .planning/phases/01-schema-verification/01-VERIFICATION.md | grep -q "3/3" && echo "VERIFY-03 documented"</automated>
  </verify>
  <acceptance_criteria>
    - 01-VERIFICATION.md contains exactly one `## VERIFY-03: Cache Behavior` heading.
    - The placeholder line `_(populated by Plan 03)_` no longer appears in the file (grep returns 0).
    - The VERIFY-03 section contains a fenced block whose body includes the verbatim `find ~/.claude/plugins/cache/hack-skills-marketplace -maxdepth 3 -type d` output from `.task03-verify03-evidence.txt`.
    - The VERIFY-03 section contains the literal substring `mindepth 2 -maxdepth 2` (the strict-pass assertion is documented).
    - The VERIFY-03 section contains a `du -sh` size block with two non-empty lines (one per plugin).
    - The VERIFY-03 section contains a paragraph that explicitly cites `Pitfall 6` (version-slot-vs-collision) AND a paragraph that explicitly cites `Pitfall 3` (orphaned-cache grace period).
    - The frontmatter `score:` field reads `3/3 VERIFY questions resolved`.
    - The frontmatter `status:` field is still `in_progress` (Plan 04 finalizes it).
    - The `## VERIFY-01`, `## VERIFY-02`, and `## Pivot Policy` headings are still present and their bodies are unchanged (VERIFY-01/02 still populated by Plan 02; Pivot Policy still the Plan 04 placeholder).
  </acceptance_criteria>
  <done>01-VERIFICATION.md has a fully populated VERIFY-03 section with all D-06 subsections present, both Pitfall callouts included, score updated to 3/3, and prior sections untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Final D-10 teardown and confirm clean state</name>
  <files>.planning/phases/01-schema-verification/.task03-final-teardown.txt</files>
  <read_first>
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 280–284 (final teardown commands)
    - .planning/phases/01-schema-verification/01-RESEARCH.md lines 352–363 (Pitfall 3 — orphaned cache after final teardown is expected, not a failure)
    - .planning/phases/01-schema-verification/01-CONTEXT.md D-10 (test hygiene)
  </read_first>
  <action>
    Run the final D-10 teardown commands and capture verbatim stdout/stderr to `.planning/phases/01-schema-verification/.task03-final-teardown.txt`:

    1. `claude plugin disable hack-skills-recon@hack-skills-marketplace`
    2. `claude plugin disable hack-skills-auth-bypass@hack-skills-marketplace`
    3. `claude plugin uninstall hack-skills-recon@hack-skills-marketplace --scope user -y`
    4. `claude plugin uninstall hack-skills-auth-bypass@hack-skills-marketplace --scope user -y`
    5. `claude plugin marketplace remove hack-skills-marketplace`

    Confirm clean state (paste output verbatim into the file):

    6. `claude plugin list --json | jq '[.[] | select(.id | startswith("hack-skills-"))]'` (or plain `claude plugin list | grep -c hack-skills- || true` if `jq` unavailable) — expected: empty array `[]` or count `0`.
    7. `claude plugin marketplace list` — expected: no `hack-skills-marketplace` row.

    Document the expected orphaned-cache residue (per Pitfall 3):

    8. `find ~/.claude/plugins/cache/hack-skills-marketplace -maxdepth 3 -type d 2>/dev/null || echo "cache already pruned"` — record verbatim. The cache subdirs ARE expected to persist for ~7 days after uninstall; this is documented Claude Code behavior. Append a single-line note to the file: `# Note: cache residue above is expected per Pitfall 3 (grace period). Not a Plan 03 failure.`

    Do NOT run `rm -rf ~/.claude/plugins/cache/hack-skills-marketplace` after teardown. Reasoning: keeping the residual cache visible is part of the verification trail for the orphaned-cache caveat documented in VERIFY-03 (Pitfall 3 note). If a future re-run of Phase 1 needs a clean baseline, the Plan 03 Task 1 pre-cleanup step handles it then.

    Per CONTEXT.md "Claude's Discretion" — decide whether the test `.claude-plugin/marketplace.json` should be preserved for Phase 3 or scrubbed:
    - **Decision: PRESERVE.** Plan 04 will document this decision. Reasoning: the test marketplace.json (D-01 two-plugin shape) is the literal starting point of Phase 3's full build-out — Phase 3 ADDS plugins to it, it does not start from scratch. Preserving it eliminates rework and keeps Phase 1's authored artifact as the seed for Phase 3. Phase 3 will reorganize / rename / expand as needed; Phase 1's content remains an internally-consistent verified shape.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-schema-verification/.task03-final-teardown.txt && PLUGIN_COUNT=$(claude plugin list 2>/dev/null | grep -c "^hack-skills-" || echo 0) && test "$PLUGIN_COUNT" = "0" && MKT_COUNT=$(claude plugin marketplace list 2>/dev/null | grep -c "^hack-skills-marketplace" || echo 0) && test "$MKT_COUNT" = "0" && echo "teardown clean"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-schema-verification/.task03-final-teardown.txt` exists with verbatim output of commands 1–8.
    - `claude plugin list` shows no `hack-skills-*` entries (grep count is 0).
    - `claude plugin marketplace list` shows no `hack-skills-marketplace` (grep count is 0).
    - The orphaned-cache note (`# Note: cache residue above is expected per Pitfall 3...`) appears in the teardown file.
    - The test `.claude-plugin/marketplace.json` is unchanged (Plan 03 must not modify it — only Plan 01 touches that file).
  </acceptance_criteria>
  <done>Final teardown commands executed verbatim, install state is clean (no plugins, no marketplace entry), the orphaned-cache caveat is documented in the teardown file, and `.claude-plugin/marketplace.json` is preserved for Phase 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 02 teardown state → Plan 03 install state | The orphaned cache from Plan 02's uninstall (Pitfall 3) sits between these plans. Pre-cleanup in Task 1 step 1 closes this boundary by rm-rf-ing the cache subdir before re-installing. |
| Local marketplace declaration → yaklang/hack-skills upstream (SECOND clone in this phase) | `claude plugin install` for each plugin triggers a clone of `github.com/yaklang/hack-skills`. The second clone (in Plan 03) may resolve to a DIFFERENT `gitCommitSha` than Plan 02's clone if upstream HEAD has advanced between Plans 02 and 03. |
| Filesystem inspection (`find`, `du`, `rm -rf`) → user's `~/.claude/plugins/cache/` tree | These commands READ + DELETE from user home. The rm is scoped to `~/.claude/plugins/cache/hack-skills-marketplace/` — no other plugin's cache is touched per RESEARCH.md line 361. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | yaklang/hack-skills upstream content changing between Plan 02's clone and Plan 03's clone | mitigate | Both Plan 02 and Plan 03 record the actual `gitCommitSha` resolved per `installed_plugins.json` verbatim. If the SHAs differ between plans, the artifact's evidence trail makes that visible. The VERIFY-03 question is structural (do per-plugin subdirs partition?) — even with SHA drift the partitioning is by plugin name, so the strict-pass assertion holds regardless of SHA. Reproducibility note appears in Plan 04's summary. |
| T-03-02 | Tampering | `rm -rf ~/.claude/plugins/cache/hack-skills-marketplace` scoping error | mitigate | The path is hardcoded and absolute: `~/.claude/plugins/cache/hack-skills-marketplace`. No globs, no variable substitution, no relative paths. RESEARCH.md line 361 explicitly affirms safety because no other plugin shares this subdir tree. The Task 1 verify-cleared check (`test ! -d ...`) confirms the deletion targeted the intended directory. |
| T-03-03 | Tampering | Test hygiene state leakage across phases (Plan 03 cache residue confusing Phase 2 or 3) | mitigate | Per the "PRESERVE marketplace.json" decision documented in Task 3, the on-disk `.claude-plugin/marketplace.json` remains as a seed for Phase 3 — but the install state (`installed_plugins.json`, `known_marketplaces.json`) is fully torn down by Task 3 commands 1–5. Phase 2 (classification) does not touch install state. Phase 3 will re-author marketplace.json from scratch on top of Phase 1's verified shape. |
| T-03-04 | Denial of Service | Concurrent install of both plugins consuming unbounded cache space | accept | RESEARCH.md PROJECT.md key decisions describe `yaklang/hack-skills` as a "text-only repo" with "storage cost is real but small". Task 1 step 6 captures `du -sh` output to confirm this empirically. Even with both plugins installed, total cache delta is bounded by 2× the repo size of yaklang/hack-skills (which on this machine is observable on the sibling clone at `~/code/hack-skills-wip/hack-skills/`). |
| T-03-05 | Information disclosure | Verbatim `installed_plugins.json` excerpt pasted into VERIFY-03 evidence revealing `installPath` containing user home directory | accept | The installPath contains `/Users/spencerpresley/...` which is already disclosed elsewhere in PROJECT.md and the planning artifacts. No secrets, no tokens. The verbatim paste requirement (CONTEXT.md §Specifics line 104) is the dominant constraint here. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. `claude plugin install` triggers a github clone of yaklang/hack-skills, which is markdown-only skill content (no executable code, no install scripts). RESEARCH.md Package Legitimacy Audit (line 134) marks the audit as not-applicable. |
</threat_model>

<verification>
- After Task 2: `grep -c "^## VERIFY-03: Cache Behavior$" .planning/phases/01-schema-verification/01-VERIFICATION.md` returns 1.
- After Task 2: `grep -c "_(populated by Plan 03)_" .planning/phases/01-schema-verification/01-VERIFICATION.md` returns 0.
- After Task 2: `grep -E "^score:" .planning/phases/01-schema-verification/01-VERIFICATION.md` contains `3/3`.
- After Task 3: `claude plugin list 2>&1 | grep -c "^hack-skills-"` returns 0.
- After Task 3: `claude plugin marketplace list 2>&1 | grep -c "^hack-skills-marketplace"` returns 0.
- After Task 3: `.claude-plugin/marketplace.json` content is unchanged from Plan 01's output (no diff against the Plan 01 SUMMARY's recorded content).
</verification>

<success_criteria>
Plan 03 succeeds when:
- Both `hack-skills-recon` and `hack-skills-auth-bypass` installed and enabled concurrently against the same `yaklang/hack-skills` source (D-10 concurrent-install exception fully exercised).
- The `find ... -mindepth 2 -maxdepth 2 -type d | wc -l` strict-pass assertion outputs `2` (cache tree shows two separate per-plugin subdirs at depth 2).
- VERIFY-03 section of 01-VERIFICATION.md is fully populated per D-06 with verbatim cache-tree output, `du -sh` size block, strict-pass-assertion result statement, Pitfall 6 callout, Pitfall 3 callout, and one-line conclusion.
- Frontmatter `score:` updated to `3/3 VERIFY questions resolved`; `status:` still `in_progress` (Plan 04 owns the final terminal status).
- Final teardown leaves `claude plugin list` and `claude plugin marketplace list` empty of `hack-skills-*` entries.
- The test `.claude-plugin/marketplace.json` is preserved on disk (Claude's Discretion decision: preserve as Phase 3 seed).
- VERIFY-01 and VERIFY-02 sections (Plan 02's output) and the Pivot Policy placeholder (Plan 04's target) are unchanged.
- If the strict-pass assertion outputs anything other than `2`: 01-VERIFICATION.md frontmatter `status:` is set to `gaps_found` overriding `in_progress`, the actual subdir count is recorded verbatim in the VERIFY-03 conclusion, and Plan 04 will apply D-09's pre-decided VERIFY-03 pivot stance.
</success_criteria>

<output>
Create `.planning/phases/01-schema-verification/01-03-SUMMARY.md` when done, including:
- VERIFY-03 answer (YES | NO) and a one-line reason citing the observed subdir count.
- The exact `<version-token>` observed for each plugin in the cache tree (verbatim — per RESEARCH.md A2 record what was seen, not what was predicted).
- The `du -sh` sizes captured (two lines, one per plugin) — confirms or refutes the "small for text-only repo" assumption.
- One-line confirmation that final teardown is clean.
- Decision record: "Preserved `.claude-plugin/marketplace.json` as Phase 3 seed per Claude's Discretion clause in CONTEXT.md."
- A pointer for Plan 04: the current `status:` field value, the current `score:` field value, and the three VERIFY answers ready to drive Pivot Policy synthesis.
</output>
</content>
</invoke>