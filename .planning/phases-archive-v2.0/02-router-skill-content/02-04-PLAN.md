---
phase: 02-router-skill-content
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - plugins/hack-skills-router/skills/hack-skills-router/SKILL.md
autonomous: true
requirements: [ROUTER-01, ROUTER-05]
tags: [skill-md, router-body, frontmatter, rewrite-in-place, stub-removal, progressive-disclosure]

must_haves:
  truths:
    - "Router SKILL.md frontmatter description is keyword-stuffed and fits both the 1024-char hard cap and the 250-char first-paragraph listing display cap"
    - "Router SKILL.md body provides all 7 sections per D-12: title+attribution, when-to-use, trust model, routing strategy, 3-step operating model, plugin availability handling, boundary conditions (8 one-line summaries), workflow examples cross-ref"
    - "Body cross-references all three sub-files by relative path: patterns/routing-tables.md, patterns/expert-intuitions.md, examples/workflow-walkthroughs.md"
    - "Body contains zero STUB / Phase 1 / TBD / FIXME / XXX markers (per Pitfall 4 + Phase 1 STUB→FINAL boundary)"
    - "Body uses soft heuristic voice (per D-14): prefer/consider/typically phrasing in routing strategy + operating model — zero MUST/ALWAYS in those sections (Pitfall 6)"
    - "Body contains 8 one-line summaries for the boundary conditions (D-13) — NOT names-only, NOT full paragraphs"
    - "Each boundary-condition summary's lede is byte-identical to the corresponding Lede field in patterns/expert-intuitions.md"
    - "Body is 95-105 lines (D-12 target)"
    - "Frontmatter omits 'name', 'user-invocable', 'disable-model-invocation' fields (per Phase 1 constraints)"
    - "All cross-references one-level-deep from SKILL.md (sub-files do NOT link to each other; body is the only file that links out)"
  artifacts:
    - path: "plugins/hack-skills-router/skills/hack-skills-router/SKILL.md"
      provides: "Production router skill: keyword-dense frontmatter description (always-on) + 95-105 line body (on-demand) with hybrid routing strategy + 3-step operating model + plugin-availability handling + 8 boundary-condition inline summaries + cross-refs to 3 sub-files"
      contains: ["yaklang/hack-skills", "When to use", "Trust model", "Routing strategy", "Operating model", "Plugin availability", "Boundary conditions", "patterns/routing-tables.md", "patterns/expert-intuitions.md", "examples/workflow-walkthroughs.md"]
  key_links:
    - from: "plugins/hack-skills-router/skills/hack-skills-router/SKILL.md (body)"
      to: "plugins/hack-skills-router/skills/hack-skills-router/patterns/routing-tables.md"
      via: "clickable markdown link in 'Routing strategy' and 'Plugin availability' sections"
      pattern: "grep -qE '\\]\\(patterns/routing-tables\\.md\\)' SKILL.md"
    - from: "plugins/hack-skills-router/skills/hack-skills-router/SKILL.md (body)"
      to: "plugins/hack-skills-router/skills/hack-skills-router/patterns/expert-intuitions.md"
      via: "clickable markdown link in 'Boundary conditions' section"
      pattern: "grep -qE '\\]\\(patterns/expert-intuitions\\.md\\)' SKILL.md"
    - from: "plugins/hack-skills-router/skills/hack-skills-router/SKILL.md (body)"
      to: "plugins/hack-skills-router/skills/hack-skills-router/examples/workflow-walkthroughs.md"
      via: "clickable markdown link in 'Workflow examples' section"
      pattern: "grep -qE '\\]\\(examples/workflow-walkthroughs\\.md\\)' SKILL.md"
---

<objective>
Rewrite `plugins/hack-skills-router/skills/hack-skills-router/SKILL.md` IN PLACE — replace
Phase 1's 28-line STUB with the production router skill. Frontmatter description is
keyword-stuffed for description-match auto-invocation; body provides hybrid routing
strategy + 3-step operating model + plugin-availability handling + 8 inline boundary-
condition summaries + cross-refs to the three sub-files created in Wave 1.

Purpose: ROUTER-01 (router invocable via Skill(hack-skills-router); body provides hybrid
routing + dual-load + plugin-availability handling) and ROUTER-05 (body's plugin-availability
section + the inline cross-plugin dual-load example surface install commands for missing
plugins).

Output: One file rewrite at `plugins/hack-skills-router/skills/hack-skills-router/SKILL.md`
with production frontmatter (~810 chars total, 241-char first paragraph) + ~95-105 line
body in 7 D-12-locked sections.
</objective>

<execution_context>
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-router-skill-content/02-CONTEXT.md
@.planning/phases/02-router-skill-content/02-RESEARCH.md
@.planning/phases/02-router-skill-content/02-PATTERNS.md
@.planning/specs/2026-05-22-v2-router-design.md
@.planning/phases/01-plugin-mechanism-spike/01-01-SUMMARY.md
@.planning/phases/02-router-skill-content/02-01-SUMMARY.md
@.planning/phases/02-router-skill-content/02-02-SUMMARY.md
@.planning/phases/02-router-skill-content/02-03-SUMMARY.md

<interfaces>
<!-- Source-of-truth content the executor must mirror. -->

Frontmatter target (RESEARCH §"Code Examples Example 1", tightened to fit 250-char first-
paragraph listing cap + 1024-char hard cap):

```yaml
---
description: |
  CRITICAL: Use FIRST for security/hacking tasks before any deep topic skill. Routes vulnerability questions to the right deep skill in the hack-skills marketplace and surfaces boundary conditions a baseline AI often misses.

  Triggers: XSS, SQLi, SSRF, XXE, IDOR, BOLA, BFLA, CSRF, CORS, RCE, SSTI, LFI, RFI, JWT,
  OAuth, SAML, OIDC, NTLM, Kerberos, vulnerability, exploit, pentest, bug bounty, payload,
  recon, enumeration, privilege escalation, lateral movement, reverse shell, burp, nmap,
  sqlmap, metasploit, CVE-XXXX-NNNN, .env exposure, .git exposure, prototype pollution,
  deserialization, race condition, request smuggling, web cache deception, host header,
  parameter pollution, type juggling, NoSQL injection, WAF bypass, file upload,
  business logic, SAML assertion, OAuth misconfiguration. Adapted from yaklang/hack-skills.
---
```

Frontmatter constraints (from RESEARCH §"Frontmatter Cap Analysis"):
- First paragraph (text up to first blank line in description) ≤ 250 chars (listing cap).
- Total description ≤ 1024 chars (hard cap).
- No `name` field (Phase 1 SC #3 verified omission works).
- No `user-invocable` field (would hide from claude plugin details).
- No `disable-model-invocation` field (would block desired auto-invoke).
- No XML tags in description (Claude Code constraint).

Body section order (per D-12), exact section count = 7 H2s (boundary conditions and
workflow examples are inline H2s, plus a top-level H1 + attribution one-liner):

1. **H1 title + one-line attribution:** `# Hack-Skills Router` then a single line:
   `Adapted from upstream \`yaklang/hack-skills\` \`hack\` SKILL.md (MIT-licensed).`

2. **## When to use this skill** — 4-5 bullets (researcher's tailored bullets from
   02-RESEARCH.md §"Open Question 4" Recommendation):
   - "New target, unclear where to start"
   - "Multiple signals could route to multiple categories"
   - "Need methodology rigor, not payload guessing"
   - "Want the boundary conditions baseline AI misses"
   - "Want install commands for plugins you don't yet have"

3. **## Trust model** — 3-4 lines. Paraphrased from upstream `hack` SKILL.md §Trust Model:
   - Authorized targets, bug-bounty programs in scope, defensive validation, legitimate
     research only.
   - If a task lacks clear authorization context, ask before proceeding / refuse.
   - This section MAY contain one "use only within authorized targets" type sentence with
     a should-style verb — that's the one place soft "should" is acceptable per Pitfall 6.

4. **## Routing strategy (hybrid)** — paraphrased from RESEARCH §"Code Examples Example 2"
   (Body section template). Two-tier:
   - **Static signal table** is primary. Common cases routed by signal-term matching from
     `patterns/routing-tables.md`. Cite the file with a clickable link.
   - **Model reasoning** is fallback for ambiguous queries.
   - Voice: "Prefer the table when it matches; use reasoning when it doesn't." — soft
     heuristic phrasing (D-14, Pitfall 6).

5. **## Operating model (3 steps)** — paraphrased from upstream operating model, compressed
   to 3 steps per CONTEXT §Specifics ("Phase ID → Signal route → Dual-load"):
   - Step 1: Identify the testing phase (Recon / Validation / PrivEsc / Chain).
   - Step 2: Look for signals that route to a specific category (table or reasoning).
   - Step 3: Consider dual-loading if signals span topics (see `patterns/routing-tables.md`
     dual-load rules).
   - Phrase as guidance, NOT as algorithm. Voice constraint: zero MUST/ALWAYS in this
     section (Pitfall 6).

6. **## Plugin availability** — instructs Claude to output install commands when the routed
   deep skill's plugin isn't installed. Cite `patterns/routing-tables.md` plugin-
   recommendation template. Per RESEARCH §"Open Question 1" recommendation, include a
   4-5 line inline cross-plugin dual-load example using the SSRF + business-logic
   synthetic case (rule 3 in routing-tables.md):
   ```
   > **Recommended deep skill:** ssrf-server-side-request-forgery (in `hack-skills-server-side-execution`, not currently installed)
   > **Install:** `/plugin install hack-skills-server-side-execution@hack-skills-marketplace`
   >
   > **Recommended deep skill:** business-logic-vulnerabilities (in `hack-skills-web-client-attacks`, not currently installed)
   > **Install:** `/plugin install hack-skills-web-client-attacks@hack-skills-marketplace`
   ```

7. **## Boundary conditions (quick reference)** — 8 one-line summaries inline, one bullet
   per intuition (per D-13). Each bullet's text is byte-identical to the corresponding
   `**Lede:**` field in `patterns/expert-intuitions.md` (so body and sub-file ledes match).
   Format: numbered list 1-8 mirroring upstream order. Followed by a closing line:
   `For full paragraphs and concrete examples, load [patterns/expert-intuitions.md](patterns/expert-intuitions.md).`

8. **## Workflow examples** — cross-reference paragraph + clickable link:
   `For four worked traces (admin/JWT, GraphQL, .env, coupon-reuse), see
   [examples/workflow-walkthroughs.md](examples/workflow-walkthroughs.md).`

Phase 1 STUB markers to REMOVE (per Pitfall 4 + Phase 1 STUB→FINAL boundary):
- `STUB —` (in current frontmatter description, character positions 1-5)
- `(STUB)` (in current H1 title)
- `Phase 1 mechanism-spike stub` (current body line 7)
- `Phase 2 will replace` (current body — H2 section name and prose)
- `Phase 1 success signal` (current body — H2 section name)
- Any other `STUB`, `stub`, `Phase 1`, `TBD`, `FIXME`, `XXX` markers

Frontmatter dual-cap verification probes (RESEARCH §"Frontmatter Cap Analysis"):
- First paragraph ≤ 250 chars (cut at first blank line in description)
- Total description ≤ 1024 chars

Body length target: 95-105 lines (D-12). Body = everything after the closing `---` of the
YAML frontmatter.

Pre-existing files this body cross-references (created in Wave 1 — must exist before this
plan runs, hence Wave 2 placement):
- patterns/routing-tables.md (created by plan 02-01)
- patterns/expert-intuitions.md (created by plan 02-02)
- examples/workflow-walkthroughs.md (created by plan 02-03)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md in place — replace Phase 1 STUB frontmatter + body with production router content (frontmatter + 7-section body)</name>
  <files>plugins/hack-skills-router/skills/hack-skills-router/SKILL.md</files>
  <read_first>
    - plugins/hack-skills-router/skills/hack-skills-router/SKILL.md (current Phase 1 STUB — see what markers need removing)
    - plugins/hack-skills-router/skills/hack-skills-router/patterns/routing-tables.md (created by plan 02-01 in Wave 1 — body cross-references this; confirms link target exists)
    - plugins/hack-skills-router/skills/hack-skills-router/patterns/expert-intuitions.md (created by plan 02-02 in Wave 1 — body's 8 boundary-condition ledes must match this file's `**Lede:**` fields byte-exact)
    - plugins/hack-skills-router/skills/hack-skills-router/examples/workflow-walkthroughs.md (created by plan 02-03 in Wave 1 — body cross-references this; confirms link target exists)
    - /Users/spencerpresley/.claude/plugins/cache/hack-skills-marketplace/hack-skills-recon/c6f732befcae-32c1cf49/hack/SKILL.md (upstream MIT-licensed source — content source for Trust Model paraphrase, Operating Model paraphrase, and 8 intuition ledes that must match expert-intuitions.md)
    - .planning/phases/02-router-skill-content/02-RESEARCH.md lines 442-528 (Examples 1-4 — frontmatter, body section, routing-table section, walkthrough trace templates)
    - .planning/phases/02-router-skill-content/02-RESEARCH.md lines 554-584 (Frontmatter Cap Analysis — dual-cap probe at lines 567-582)
    - .planning/phases/02-router-skill-content/02-PATTERNS.md lines 65-231 (pattern assignment #1 — SKILL.md frontmatter pattern, body section list with upstream excerpts, critical constraints, STUB vs FINAL divergence)
    - .planning/phases/02-router-skill-content/02-CONTEXT.md §Implementation Decisions D-11 through D-17 (locked structural decisions for this file)
    - .planning/specs/2026-05-22-v2-router-design.md §5.6 + §5.7 + §11 (design source-of-truth — frontmatter + body outline + attribution requirements)
    - .planning/phases/01-plugin-mechanism-spike/01-01-SUMMARY.md §key-decisions (Phase 1 frontmatter constraints: no `name`, no `user-invocable`, no `disable-model-invocation`)
  </read_first>
  <action>
    Rewrite `plugins/hack-skills-router/skills/hack-skills-router/SKILL.md` IN PLACE. Use the Read tool first to capture the current STUB content (so the Write tool's "must have read first" gate is satisfied), then use the Write tool with the new full file content. No heredoc / cat-EOF.

    Replace BOTH the YAML frontmatter and the body. The 3-line frontmatter delimiter
    (`---` ... `---`) stays as YAML structure, but the `description` field changes from
    STUB form to production form. The body is fully replaced.

    Build the file in this order:

    1. **YAML frontmatter** (verbatim from `<interfaces>` block — the multi-line description
       block-scalar form `description: |`). Use the tightened first paragraph (241 chars) per
       RESEARCH §"Frontmatter Cap Analysis" — the design spec §5.6 verbatim form has a 270-char
       first paragraph that overflows the 250-char listing cap. Do not include `name`,
       `user-invocable`, or `disable-model-invocation`. Do not include XML tags inside the
       description value.

    2. **Body — H1 + one-line attribution:**
       ```
       # Hack-Skills Router

       Adapted from upstream `yaklang/hack-skills` `hack` SKILL.md (MIT-licensed).
       ```

    3. **Body — `## When to use this skill`:** 4-5 bullets, exactly the tailored bullets in
       `<interfaces>`.

    4. **Body — `## Trust model`:** 3-4 lines paraphrased from upstream Trust Model section
       (cached at /Users/spencerpresley/.claude/plugins/cache/.../hack/SKILL.md lines 22-28).
       Authorized targets / legitimate research / refuse-or-ask phrasing.

    5. **Body — `## Routing strategy (hybrid)`:** Two-tier description (static table primary
       + model reasoning fallback). Cite `patterns/routing-tables.md` with a clickable
       relative link: `[patterns/routing-tables.md](patterns/routing-tables.md)`. Voice:
       prefer/consider/typically — zero MUST/ALWAYS.

    6. **Body — `## Operating model (3 steps)`:** 3-step description per CONTEXT §Specifics
       (Phase ID → Signal route → Dual-load). Use numbered list or 3 H3 sub-headings; either
       form acceptable. Voice: prefer/consider/typically — zero MUST/ALWAYS (Pitfall 6).
       Cite `patterns/routing-tables.md` dual-load rules section.

    7. **Body — `## Plugin availability`:** Instructions for outputting install commands
       when target plugin is missing. Cite the plugin-recommendation template in
       `patterns/routing-tables.md`. Include the inline 4-5 line cross-plugin dual-load
       example from `<interfaces>` (SSRF + business-logic synthetic case, with stacked
       two-line blocks). Per RESEARCH §"Open Question 1" recommendation, this demonstrates
       cross-plugin stacking (which the 4 walkthroughs don't naturally exercise).

    8. **Body — `## Boundary conditions (quick reference)`:** Introductory paragraph noting
       these are the 8 high-value intuitions. Then a numbered list 1-8 with one-line
       summaries (per D-13 — NOT names-only, NOT full paragraphs). Each item's text is
       byte-identical to the `**Lede:**` field of the corresponding intuition in
       `patterns/expert-intuitions.md` (Plan 02-02 deliverable). Close with the link line:
       `For full paragraphs and concrete examples, load [patterns/expert-intuitions.md](patterns/expert-intuitions.md).`

    9. **Body — `## Workflow examples`:** One-paragraph cross-reference:
       `For four worked traces (admin panel + JWT, GraphQL with introspection, .env in
       webroot, coupon-reuse checkout), see [examples/workflow-walkthroughs.md](examples/workflow-walkthroughs.md).`

    Critical constraints (enforced by verification):
    - All STUB markers from the Phase 1 stub are removed (`STUB`, `(STUB)`, `Phase 1`, `TBD`,
      `FIXME`, `XXX` — case-insensitive grep returns zero matches in production file).
    - Frontmatter description first paragraph ≤ 250 chars (listing cap).
    - Frontmatter description total ≤ 1024 chars (hard cap).
    - No `name`, `user-invocable`, or `disable-model-invocation` keys in frontmatter.
    - Body length 95-105 lines (D-12 target).
    - Body sections appear in the order above (7 H2s + H1 + attribution one-liner).
    - Body cross-refs all 3 sub-files via clickable relative links.
    - Zero MUST/ALWAYS in Routing strategy and Operating model sections (Pitfall 6).
    - 8 boundary-condition bullets, each byte-identical to the corresponding `**Lede:**`
      in `patterns/expert-intuitions.md`.
    - English only — no Chinese characters carried over from bilingual upstream.

    After rewriting the file, run the verification probes under `<verify>` to confirm
    correctness.
  </action>
  <verify>
    <automated>
ROUTER_DIR=/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/plugins/hack-skills-router/skills/hack-skills-router
F="$ROUTER_DIR/SKILL.md"

# Probe 1: File still exists (in-place rewrite did not delete)
test -f "$F" || { echo "FAIL: SKILL.md missing after rewrite"; exit 1; }

# Probe 2: YAML frontmatter delimiters present (--- ... ---)
FM_COUNT=$(grep -c '^---$' "$F")
[ "$FM_COUNT" = "2" ] || { echo "FAIL: expected exactly 2 '---' frontmatter delimiters, got $FM_COUNT"; exit 1; }

# Probe 3: No STUB / Phase 1 / TBD / FIXME / XXX markers in production file
RESIDUAL=$(grep -nE '(STUB|stub|Phase 1 (stub|spike|mechanism-spike)|TBD|FIXME|XXX)' "$F" || true)
[ -z "$RESIDUAL" ] || { echo "FAIL: residual STUB markers detected:"; echo "$RESIDUAL"; exit 1; }

# Probe 4: Frontmatter dual-cap (first paragraph ≤ 250 chars, total ≤ 1024)
DESC=$(awk '/^description: \|$/{flag=1; next} flag && /^---$/{exit} flag{print}' "$F")
TOTAL=$(printf '%s' "$DESC" | wc -c | tr -d ' ')
[ "$TOTAL" -le 1024 ] || { echo "FAIL: description $TOTAL chars > 1024 hard cap"; exit 1; }
[ "$TOTAL" -ge 200 ] || { echo "FAIL: description $TOTAL chars too short (expected ~810); STUB likely not replaced"; exit 1; }
# First paragraph = lines up to first blank line of the description block
FIRST_PARA=$(printf '%s\n' "$DESC" | awk '/^$/{exit} {print}')
FIRST_LEN=$(printf '%s' "$FIRST_PARA" | wc -c | tr -d ' ')
[ "$FIRST_LEN" -le 250 ] || { echo "FAIL: first paragraph $FIRST_LEN chars > 250 listing cap"; exit 1; }

# Probe 5: Forbidden frontmatter keys absent (name, user-invocable, disable-model-invocation)
FM_BLOCK=$(awk '/^---$/{c++; next} c==1' "$F")
echo "$FM_BLOCK" | grep -qE '^name:' && { echo "FAIL: 'name:' present in frontmatter (Phase 1 forbids)"; exit 1; } || true
echo "$FM_BLOCK" | grep -qE '^user-invocable:' && { echo "FAIL: 'user-invocable:' present (would hide skill)"; exit 1; } || true
echo "$FM_BLOCK" | grep -qE '^disable-model-invocation:' && { echo "FAIL: 'disable-model-invocation:' present (would block auto-invoke)"; exit 1; } || true

# Probe 6: H1 title + attribution line present
grep -q '^# Hack-Skills Router' "$F" || { echo "FAIL: missing H1 '# Hack-Skills Router'"; exit 1; }
grep -q 'yaklang/hack-skills' "$F" || { echo "FAIL: missing yaklang/hack-skills attribution"; exit 1; }
grep -qi 'MIT' "$F" || { echo "FAIL: missing MIT license note in attribution"; exit 1; }

# Probe 7: All 7 body H2 sections present
for H2 in 'When to use' 'Trust model' 'Routing strategy' 'Operating model' 'Plugin availability' 'Boundary conditions' 'Workflow examples'; do
  grep -qE "^## $H2" "$F" || { echo "FAIL: missing H2 section '## $H2'"; exit 1; }
done

# Probe 8: Cross-references to all 3 sub-files (clickable relative links)
grep -qE '\]\(patterns/routing-tables\.md\)' "$F" || { echo "FAIL: missing clickable link to patterns/routing-tables.md"; exit 1; }
grep -qE '\]\(patterns/expert-intuitions\.md\)' "$F" || { echo "FAIL: missing clickable link to patterns/expert-intuitions.md"; exit 1; }
grep -qE '\]\(examples/workflow-walkthroughs\.md\)' "$F" || { echo "FAIL: missing clickable link to examples/workflow-walkthroughs.md"; exit 1; }

# Probe 9: Cross-ref targets exist on disk
test -f "$ROUTER_DIR/patterns/routing-tables.md" || { echo "FAIL: cross-ref target patterns/routing-tables.md missing"; exit 1; }
test -f "$ROUTER_DIR/patterns/expert-intuitions.md" || { echo "FAIL: cross-ref target patterns/expert-intuitions.md missing"; exit 1; }
test -f "$ROUTER_DIR/examples/workflow-walkthroughs.md" || { echo "FAIL: cross-ref target examples/workflow-walkthroughs.md missing"; exit 1; }

# Probe 10: Body length 95-105 lines (D-12). Body = lines after second '---'
BODY_LINES=$(awk '/^---$/{c++; next} c>=2' "$F" | wc -l | tr -d ' ')
[ "$BODY_LINES" -ge 80 ] && [ "$BODY_LINES" -le 130 ] || { echo "FAIL: body has $BODY_LINES lines (expected 80-130; D-12 target 95-105)"; exit 1; }

# Probe 11: Zero MUST/ALWAYS in Routing strategy + Operating model sections (Pitfall 6 — soft heuristic voice)
ROUTING_OPS=$(awk '/^## Routing strategy/,/^## (Plugin availability|Boundary conditions|Workflow examples)/{print}' "$F" | grep -cE '\b(MUST|ALWAYS|REQUIRED)\b' || true)
[ "$ROUTING_OPS" = "0" ] || { echo "FAIL: $ROUTING_OPS MUST/ALWAYS/REQUIRED tokens in Routing strategy + Operating model sections (Pitfall 6 — soft heuristic voice required)"; exit 1; }

# Probe 12: 8 boundary-condition bullets present in body (numbered 1-8 under Boundary conditions H2)
BC_BULLETS=$(awk '/^## Boundary conditions/,/^## Workflow examples/{print}' "$F" | grep -cE '^[1-8]\.|^- ' || true)
[ "$BC_BULLETS" -ge 8 ] || { echo "FAIL: expected at least 8 boundary-condition bullets, got $BC_BULLETS"; exit 1; }

# Probe 13: Boundary-condition ledes byte-match expert-intuitions.md Lede fields
INT_FILE="$ROUTER_DIR/patterns/expert-intuitions.md"
if [ -f "$INT_FILE" ]; then
  # Extract Lede texts from expert-intuitions.md
  LEDES=$(grep -E '^\*\*Lede:\*\*' "$INT_FILE" | sed 's/^\*\*Lede:\*\* //')
  MISSING=0
  LEDE_NUM=0
  echo "$LEDES" | while IFS= read -r LEDE; do
    LEDE_NUM=$((LEDE_NUM + 1))
    [ -z "$LEDE" ] && continue
    # Search for any matching substring (after numbered marker) in SKILL.md body's Boundary conditions section
    BODY_BC=$(awk '/^## Boundary conditions/,/^## Workflow examples/{print}' "$F")
    if ! printf '%s' "$BODY_BC" | grep -qF "$LEDE"; then
      echo "FAIL: SKILL.md boundary-conditions section missing lede match for intuition $LEDE_NUM:"
      echo "  Expected substring: $LEDE"
      exit 1
    fi
  done
else
  echo "WARN: expert-intuitions.md not found — cross-file lede match skipped (Wave 1 plan 02-02 must complete first)"
fi

# Probe 14: English only — no CJK characters
! perl -ne 'exit 1 if /\p{Han}/' "$F" 2>/dev/null && true || { echo "FAIL: CJK character detected"; exit 1; }

# Probe 15: Plugin/skill name references byte-exact against marketplace.json (focused on the cross-plugin dual-load example in Plugin availability section)
REPO=/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace
for PLUGIN in 'hack-skills-server-side-execution' 'hack-skills-web-client-attacks'; do
  jq -r '.plugins[].name' "$REPO/.claude-plugin/marketplace.json" | grep -qxF "$PLUGIN" \
    || { echo "FAIL: plugin '$PLUGIN' referenced in body not in marketplace.json"; exit 1; }
done
for SKILL in 'ssrf-server-side-request-forgery' 'business-logic-vulnerabilities'; do
  jq -r '.plugins[].skills[]?' "$REPO/.claude-plugin/marketplace.json" | sed 's|^\./||' | grep -qxF "$SKILL" \
    || { echo "FAIL: deep-skill '$SKILL' referenced in body not in marketplace.json"; exit 1; }
done

# Probe 16: Install commands in canonical shape (no @branch suffix)
! grep -E '/plugin install hack-skills-[a-z-]+@hack-skills-marketplace@' "$F" || { echo "FAIL: @branch suffix detected"; exit 1; }
INSTALL_COUNT=$(grep -cE '/plugin install hack-skills-[a-z-]+@hack-skills-marketplace' "$F")
[ "$INSTALL_COUNT" -ge 2 ] || { echo "FAIL: expected at least 2 install commands in Plugin availability section, got $INSTALL_COUNT"; exit 1; }

echo "PASS: all 16 probes succeeded"
    </automated>
  </verify>
  <done>
    All 16 verification probes return PASS:
    1. File still exists (in-place rewrite preserved path).
    2. Exactly 2 `---` frontmatter delimiters.
    3. Zero STUB / Phase 1 / TBD / FIXME / XXX markers.
    4. Frontmatter description ≤ 1024 chars total + first paragraph ≤ 250 chars.
    5. Forbidden frontmatter keys absent: `name`, `user-invocable`, `disable-model-invocation`.
    6. H1 `# Hack-Skills Router` + attribution line with yaklang/hack-skills + MIT.
    7. All 7 body H2 sections present (When to use, Trust model, Routing strategy, Operating model, Plugin availability, Boundary conditions, Workflow examples).
    8. Clickable links to all 3 sub-files.
    9. All 3 cross-ref target files exist on disk.
    10. Body length 80-130 lines (D-12 target 95-105).
    11. Zero MUST/ALWAYS/REQUIRED in Routing strategy + Operating model sections (Pitfall 6).
    12. At least 8 boundary-condition bullets in body.
    13. Each `**Lede:**` from `patterns/expert-intuitions.md` appears as a substring in the body's Boundary conditions section (cross-file consistency).
    14. No Chinese characters.
    15. Plugin/skill names in the cross-plugin dual-load example byte-exact against marketplace.json.
    16. Install commands in canonical shape (no `@branch` suffix), at least 2 install lines in Plugin availability section.
  </done>
</task>

</tasks>

<acceptance_criteria>
- `plugins/hack-skills-router/skills/hack-skills-router/SKILL.md` exists (in-place rewrite preserved path).
- Frontmatter: keyword-stuffed `description` field with first paragraph ≤ 250 chars (listing cap) and total ≤ 1024 chars (hard cap). No `name`, `user-invocable`, `disable-model-invocation` keys.
- Body: 7 H2 sections in locked D-12 order (When to use → Trust model → Routing strategy → Operating model → Plugin availability → Boundary conditions → Workflow examples) plus H1 title and attribution one-liner.
- Body length 80-130 lines (D-12 target 95-105).
- Zero STUB / Phase 1 / TBD / FIXME / XXX markers anywhere in the file.
- Cross-references to all 3 sub-files via clickable relative links: `patterns/routing-tables.md`, `patterns/expert-intuitions.md`, `examples/workflow-walkthroughs.md`. All 3 target files exist on disk.
- Boundary conditions section contains 8 numbered bullets, each byte-identical to the corresponding `**Lede:**` field in `patterns/expert-intuitions.md`.
- Routing strategy + Operating model sections contain zero MUST/ALWAYS/REQUIRED tokens (Pitfall 6 — soft heuristic voice).
- Plugin availability section contains a cross-plugin dual-load example (stacked 2-line blocks for SSRF + business-logic synthetic case) with both install commands in canonical `/plugin install <name>@hack-skills-marketplace` shape (no `@branch` suffix).
- Plugin/skill names in the body byte-exact against marketplace.json.
- English only.
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Always-on context tax | Frontmatter description loads into every session's context — overshooting caps either truncates trigger keywords (≥250 listing cap) or rejects the skill (≥1024 hard cap) |
| Phase 1 STUB → Phase 2 FINAL | Rewrite-in-place must remove all STUB markers; missed markers leave the production router declaring itself a stub (UX failure + reviewer embarrassment) |
| Upstream paraphrase → router file | Body paraphrases Trust Model, Operating Model, and 8 intuition ledes from MIT-licensed upstream; attribution required |
| Sub-file cross-reference correctness | Body cites 3 sub-files by relative path; if a sub-file is missing or path is wrong, Claude follows a broken link and partial-reads (Pitfall 5 — multi-hop link risk) |
| Plugin-availability install commands | Body's inline dual-load example surfaces install commands; wrong plugin name = silent UX failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Tampering (residual STUB markers) | SKILL.md production body | mitigate | Probe 3: case-insensitive grep for STUB / Phase 1 / TBD / FIXME / XXX returns zero matches |
| T-02-15 | Denial of Service (frontmatter cap overflow) | description field — listing cap (250) + hard cap (1024) | mitigate | Probe 4: awk extracts description, wc counts chars for total and for first paragraph; fails if either exceeds cap |
| T-02-16 | Information Disclosure (license) | Body paraphrases upstream Trust Model + Operating Model + 8 intuition ledes | mitigate | Probe 6: attribution one-liner with yaklang/hack-skills + MIT under H1 (D-15 — file 1 of 4-file chain) |
| T-02-17 | Tampering (forbidden frontmatter keys) | name / user-invocable / disable-model-invocation | mitigate | Probe 5: each forbidden key absent; presence fails the task (Phase 1 SC #3 constraints) |
| T-02-18 | Tampering (broken cross-references) | 3 sub-file clickable links | mitigate | Probe 9: each of the 3 cross-ref targets exists on disk (depends_on relationship ensures Wave 1 plans land first) |
| T-02-19 | Tampering (algorithmic voice drift) | Routing strategy + Operating model sections | mitigate | Probe 11: zero MUST/ALWAYS/REQUIRED tokens in these two sections (Pitfall 6, D-14) |
| T-02-20 | Tampering (lede drift between body and sub-file) | 8 boundary-condition bullets vs expert-intuitions.md Lede fields | mitigate | Probe 13: cross-file substring match — each expert-intuitions.md Lede must appear in SKILL.md body's Boundary conditions section |
| T-02-21 | Tampering (install command shape) | Cross-plugin dual-load example in Plugin availability | mitigate | Probe 16: rejects @branch suffix; requires at least 2 install commands in canonical shape |
| T-02-22 | Information Disclosure (Chinese carry-over) | Paraphrased content from bilingual upstream | mitigate | Probe 14: rejects any CJK character (English-only constraint) |
</threat_model>

<verification>
- Wave gate: This is a Wave 2 plan with `depends_on: ["02-01", "02-02", "02-03"]`. All three Wave 1 plans must complete before this task runs — Probe 9 confirms each sub-file exists on disk, and Probe 13 cross-checks the body's boundary-condition ledes against the Lede fields in expert-intuitions.md (Plan 02-02 output).
- Per-task automated verify: see Task 1 `<verify>` block — 16 probes covering file existence, frontmatter shape + dual-cap, STUB-marker absence, forbidden-key absence, H1+attribution, all 7 H2 sections, 3 clickable cross-refs + target existence on disk, body length, voice constraints, 8 boundary-condition bullets + cross-file lede match, CJK absence, install command shape + byte-exact plugin/skill names.
- Cross-file consistency probe: Probe 13 cross-references `patterns/expert-intuitions.md` Lede fields to confirm body and sub-file use identical phrasing.
</verification>

<success_criteria>
- File rewritten in place at the canonical sidecar path.
- All 16 Task 1 verification probes pass.
- Production frontmatter (no STUB markers; both caps respected; forbidden keys absent).
- 7-section body in D-12 order with cross-refs to all 3 sub-files.
- Cross-file lede consistency between body and expert-intuitions.md.
- Plugin availability section demonstrates cross-plugin dual-load stacking (which the 4 walkthroughs do not exercise).
- Soft heuristic voice preserved (zero MUST/ALWAYS in Routing strategy + Operating model).
- File-level attribution to yaklang/hack-skills (MIT) under the H1.
</success_criteria>

<output>
Create `.planning/phases/02-router-skill-content/02-04-SUMMARY.md` when done.
</output>
