---
phase: 02-router-skill-content
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/hack-skills-router/skills/hack-skills-router/examples/workflow-walkthroughs.md
autonomous: true
requirements: [ROUTER-04, ROUTER-05]
tags: [workflow-walkthroughs, end-to-end-traces, dual-load, plugin-recommendation, content-authoring]

must_haves:
  truths:
    - "examples/workflow-walkthroughs.md exists at the canonical sidecar path"
    - "File opens with an H1 title and a blockquote attribution header citing yaklang/hack-skills (MIT)"
    - "Exactly 4 scenarios appear, addressed by H2 headings of the form '## Scenario N: <title>'"
    - "Scenarios are in the locked order from D-01: (1) Admin panel + JWT, (2) GraphQL introspection, (3) .env webroot, (4) coupon-reuse checkout"
    - "Each scenario uses the medium-structured format with section labels: Prompt, Testing phase identified, Signal route, Dual-load, Boundary conditions surfaced, Next-test recommendation"
    - "Every deep-skill name in scenario rationales is byte-exact against marketplace.json plugins[].skills[]"
    - "Every plugin name referenced in scenario rationales is byte-exact against marketplace.json plugins[].name"
    - "Scenarios 1 and 4 demonstrate same-plugin dual-load; scenarios 2 and 3 demonstrate single-plugin focus"
    - "Each scenario surfaces at least one boundary condition by 'intuition #N' notation (no clickable links to expert-intuitions.md)"
    - "Scenarios 1, 2, 3, 4 each include a plugin-recommendation template invocation OR install command in canonical /plugin install ...@hack-skills-marketplace shape"
  artifacts:
    - path: "plugins/hack-skills-router/skills/hack-skills-router/examples/workflow-walkthroughs.md"
      provides: "4 worked traces (admin/JWT, GraphQL, .env, coupon-reuse) demonstrating the router's hybrid routing + dual-load + plugin-recommendation flow"
      contains: ["yaklang/hack-skills", "Scenario 1:", "Scenario 4:", "intuition #", "/plugin install"]
  key_links:
    - from: "plugins/hack-skills-router/skills/hack-skills-router/examples/workflow-walkthroughs.md"
      to: ".claude-plugin/marketplace.json"
      via: "every deep-skill and plugin name byte-exact against marketplace.json"
      pattern: "comm -23 mentioned-names canonical-names returns zero lines"
    - from: "plugins/hack-skills-router/skills/hack-skills-router/examples/workflow-walkthroughs.md"
      to: "patterns/expert-intuitions.md (sibling sub-file)"
      via: "prose mentions by 'intuition #N' notation; NEVER as clickable markdown link"
      pattern: "grep -q 'intuition #' && ! grep -E '\\]\\(.*expert-intuitions\\.md'"
---

<objective>
Create `examples/workflow-walkthroughs.md` — the progressive-disclosure sub-file containing
4 end-to-end worked traces that show the router applied to realistic security prompts. Each
trace walks from prompt → testing phase → signal route → dual-load (if any) → boundary
conditions surfaced → next-test recommendation.

Purpose: ROUTER-04 (router has 3-4 end-to-end example traces) and ROUTER-05 (each scenario
ends with an install command recommendation per the plugin-recommendation template, doubling
as discovery surface).

Output: One new markdown file at the sidecar path with attribution header, 4 H2 scenarios
in locked D-01 order, each ~20-25 lines using consistent section labels. Total ~100-120 lines.
</objective>

<execution_context>
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-router-skill-content/02-CONTEXT.md
@.planning/phases/02-router-skill-content/02-RESEARCH.md
@.planning/phases/02-router-skill-content/02-PATTERNS.md
@.planning/specs/2026-05-22-v2-router-design.md
@.claude-plugin/marketplace.json

<interfaces>
<!-- Source-of-truth scenario destinations the executor must mirror byte-exact.
     Extracted from 02-CONTEXT.md D-01 and 02-RESEARCH.md §"The 4 Walkthrough Scenarios". -->

4 scenarios in locked D-01 order with destination skills:

| # | Scenario title (use in H2) | Primary deep skill | Dual-load partner | Plugin(s) | Dual-load type |
|---|----------------------------|---------------------|-------------------|-----------|----------------|
| 1 | Admin panel at `/admin`, JWT in cookie | `401-403-bypass-techniques` | `jwt-oauth-token-attacks` | `hack-skills-auth-bypass` (both) | Same-plugin |
| 2 | GraphQL endpoint with introspection enabled | `graphql-and-hidden-parameters` | none (single focus) | `hack-skills-recon` | None |
| 3 | `.env` exposed in webroot | `insecure-source-code-management` | none (single focus) | `hack-skills-recon` | None |
| 4 | E-commerce checkout, coupon-reuse | `business-logic-vulnerabilities` | `race-condition` | `hack-skills-web-client-attacks` (both) | Same-plugin |

Per-scenario section labels (in this exact order):
  **Prompt:**
  **Testing phase identified:**
  **Signal route:**
  **Dual-load:**
  **Boundary conditions surfaced:**
  **Next-test recommendation:**

(Per D-02 — medium-structured trace depth, ~20-25 lines per scenario)

Boundary-condition reference notation (per 02-PATTERNS.md §4 "Per-scenario pattern"):
  Use "intuition #N" notation (e.g., "intuition #2", "intuition #5") as a prose hook to
  patterns/expert-intuitions.md. NEVER as a clickable markdown link.

Per-scenario boundary conditions to surface (researcher recommendations from 02-RESEARCH.md):
- Scenario 1 (Admin/JWT): intuition #8 (JWT — verify alg/kid/JWKS before payloads), intuition #1 (filter logic reuse — 401/403 bypass patterns often replicate)
- Scenario 2 (GraphQL): intuition #2 (parameter names as attack surface — GraphQL field names), intuition #5 (older API versions — schema v1/v2 prefixes)
- Scenario 3 (.env exposed): intuition #3 (second-order vulns — leaked secrets become chain into other systems)
- Scenario 4 (coupon-reuse): intuition #6 (business-logic highest impact), intuition #7 (race conditions on one-time ops)

Canonical install command shape:
  /plugin install <plugin-name>@hack-skills-marketplace

Plugin-recommendation template invocation (per D-07, used in each scenario's Next-test
recommendation section when surfacing the install command):
  > **Recommended deep skill:** <skill> (in `<plugin>`, not currently installed)
  > **Install:** `/plugin install <plugin>@hack-skills-marketplace`

For same-plugin dual-load scenarios (1 and 4), a single install command covers both deep
skills (one plugin = one install). For single-focus scenarios (2 and 3), one install command.

Header attribution paragraph (verbatim — 02-PATTERNS.md §"Pattern 3"):
  Four worked traces showing the router applied to realistic security prompts. The signal-
  routing methodology is paraphrased from upstream `yaklang/hack-skills` `hack` SKILL.md
  (MIT-licensed); the scenarios, dual-load patterns, and recommendation flow are tailored
  to this marketplace's 13-plugin layout.

Reference template for scenario shape — see 02-RESEARCH.md §"Example 4" (lines 502-530)
which renders Scenario 2 in full as a reference exemplar. The other 3 scenarios use the
same shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author examples/workflow-walkthroughs.md — 4 scenarios in locked D-01 order with medium-structured trace format</name>
  <files>plugins/hack-skills-router/skills/hack-skills-router/examples/workflow-walkthroughs.md</files>
  <read_first>
    - plugins/hack-skills-router/skills/hack-skills-router/SKILL.md (current Phase 1 stub — confirms parent skill directory exists; this task creates the new examples/ subdir)
    - .claude-plugin/marketplace.json (canonical source for plugin and skill names)
    - .planning/phases/02-router-skill-content/02-RESEARCH.md lines 502-530 (Example 4 — full Scenario 2 rendering as the reference exemplar)
    - .planning/phases/02-router-skill-content/02-RESEARCH.md lines 943-989 (§"The 4 Walkthrough Scenarios — Destination Skills" — per-scenario destination tables + rationales + which intuitions to surface)
    - .planning/phases/02-router-skill-content/02-PATTERNS.md lines 444-559 (pattern assignment #4 — workflow-walkthroughs.md structure, per-scenario pattern, exact-role analog at rust-router examples/workflow.md, critical constraints)
    - .planning/phases/02-router-skill-content/02-CONTEXT.md §Implementation Decisions D-01, D-02, D-07, D-15 (locked structural decisions)
    - .planning/specs/2026-05-22-v2-router-design.md §5.10 (design source-of-truth — 4 suggested traces)
  </read_first>
  <action>
    Create `plugins/hack-skills-router/skills/hack-skills-router/examples/workflow-walkthroughs.md`. The parent `examples/` directory does NOT yet exist — create it with `mkdir -p` first, then use the Write tool to author the file. No heredoc / cat-EOF.

    Structure:

    1. **H1 title:** `# Workflow Walkthroughs`
    2. **Attribution header paragraph** (blockquote `>` form), verbatim from `<interfaces>` block above: "Four worked traces showing the router applied to realistic security prompts. The signal-routing methodology is paraphrased from upstream `yaklang/hack-skills` `hack` SKILL.md (MIT-licensed); the scenarios, dual-load patterns, and recommendation flow are tailored to this marketplace's 13-plugin layout."
    3. **4 H2 scenarios** in locked D-01 order. Use the exact titles from `<interfaces>`:
       - `## Scenario 1: Admin panel at \`/admin\`, JWT in cookie`
       - `## Scenario 2: GraphQL endpoint with introspection enabled`
       - `## Scenario 3: \`.env\` exposed in webroot`
       - `## Scenario 4: E-commerce checkout, coupon-reuse`
    4. **Each scenario uses the 6-label medium-structured format** (per D-02 + 02-PATTERNS.md §4):
       ```
       **Prompt:** "<one-sentence example user prompt for this scenario>"

       **Testing phase identified:** <recon / validation / privesc / chain — single sentence>

       **Signal route:** <signal terms → primary deep-skill (in plugin) — describe why the router
       routes here>

       **Dual-load:** <for same-plugin scenarios (1 and 4): identify the partner skill and note
       same-plugin dual-load means one install covers both. For single-focus scenarios (2 and 3):
       state "None for this phase" + a one-sentence note on when a follow-on dual-load might apply>

       **Boundary conditions surfaced:**
       - *<intuition lede (matches expert-intuitions.md Lede)> (intuition #N):* <one-sentence why
         it matters for THIS scenario>
       - *<second intuition if applicable> (intuition #M):* <one-sentence why>

       **Next-test recommendation:** <concrete action: what to test next, with an inline
       plugin-recommendation template block (the two-line `**Recommended deep skill:** ...
       **Install:** ...` form) for the destination plugin>
       ```

    Per-scenario content sourcing:

    - **Scenario 1 (Admin/JWT):** Primary `401-403-bypass-techniques`, dual-load `jwt-oauth-token-attacks`, both in `hack-skills-auth-bypass`. Intuition #8 (JWT key/algorithm context first) and intuition #1 (filter logic reuses across pages — many 401/403 bypass patterns replicate across endpoints). Rationale text from 02-RESEARCH.md §"Scenario 1".
    - **Scenario 2 (GraphQL):** Primary `graphql-and-hidden-parameters` in `hack-skills-recon`, no dual-load for this phase. Intuition #2 (parameter names — GraphQL field names) and intuition #5 (older API versions — schema v1/v2 prefixes). Use 02-RESEARCH.md §"Example 4" as the reference rendering (drop in nearly verbatim, but the executor may make minor stylistic edits for consistency with the other 3 scenarios).
    - **Scenario 3 (.env exposed):** Primary `insecure-source-code-management` in `hack-skills-recon`, no dual-load. Intuition #3 (second-order vulns — leaked secrets become chain into other systems). Rationale text from 02-RESEARCH.md §"Scenario 3".
    - **Scenario 4 (coupon-reuse):** Primary `business-logic-vulnerabilities`, dual-load `race-condition`, both in `hack-skills-web-client-attacks`. Intuition #6 (business-logic highest impact) and intuition #7 (race conditions on one-time ops). Rationale text from 02-RESEARCH.md §"Scenario 4".

    Style constraints (enforced by acceptance criteria):
    - Section labels appear in the prescribed order in every scenario: Prompt → Testing phase identified → Signal route → Dual-load → Boundary conditions surfaced → Next-test recommendation (6 labels per scenario × 4 scenarios = 24 label occurrences).
    - Boundary-condition entries use `intuition #N` prose notation, NEVER `[intuition](../patterns/expert-intuitions.md)` clickable links (Pitfall 5).
    - Each scenario's Next-test recommendation includes a plugin-recommendation template invocation in canonical `/plugin install <name>@hack-skills-marketplace` shape (no `@branch` suffix).
    - Deep-skill names in scenario rationales appear in backticks (`401-403-bypass-techniques`, `jwt-oauth-token-attacks`, `graphql-and-hidden-parameters`, `insecure-source-code-management`, `business-logic-vulnerabilities`, `race-condition`) and must be byte-exact against marketplace.json.
    - Plugin names in scenario rationales appear in backticks (`hack-skills-auth-bypass`, `hack-skills-recon`, `hack-skills-web-client-attacks`) and must be byte-exact against marketplace.json.
    - File-level attribution only — no per-scenario attribution annotations (D-15).
    - English only — no Chinese characters carried over from upstream.
    - Each scenario: ~20-25 lines (per D-02). File total: ~100-120 lines.

    After writing the file, run the verification probes under `<verify>` to confirm correctness.
  </action>
  <verify>
    <automated>
ROUTER_DIR=/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/plugins/hack-skills-router/skills/hack-skills-router
F="$ROUTER_DIR/examples/workflow-walkthroughs.md"
REPO=/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace

# Probe 1: File exists
test -f "$F" || { echo "FAIL: file not created"; exit 1; }

# Probe 2: H1 title present
grep -q '^# Workflow Walkthroughs' "$F" || { echo "FAIL: missing H1 title"; exit 1; }

# Probe 3: Attribution header cites yaklang/hack-skills + MIT
grep -q 'yaklang/hack-skills' "$F" || { echo "FAIL: missing yaklang/hack-skills attribution"; exit 1; }
grep -qi 'MIT' "$F" || { echo "FAIL: missing MIT license note"; exit 1; }

# Probe 4: Exactly 4 H2 scenarios, in order 1, 2, 3, 4
SCENARIOS=$(grep -cE '^## Scenario [1-4]:' "$F")
[ "$SCENARIOS" = "4" ] || { echo "FAIL: expected 4 scenarios, got $SCENARIOS"; exit 1; }

# Probe 5: Scenario titles match locked D-01 order (substring check on each H2)
grep -q '^## Scenario 1:.*Admin panel.*JWT' "$F" || { echo "FAIL: Scenario 1 title mismatch"; exit 1; }
grep -q '^## Scenario 2:.*GraphQL.*introspection' "$F" || { echo "FAIL: Scenario 2 title mismatch"; exit 1; }
grep -qE '^## Scenario 3:.*\.env' "$F" || { echo "FAIL: Scenario 3 title mismatch (.env)"; exit 1; }
grep -qiE '^## Scenario 4:.*(coupon|checkout)' "$F" || { echo "FAIL: Scenario 4 title mismatch (coupon/checkout)"; exit 1; }

# Probe 6: Scenarios in numeric order (no shuffling)
ORDER_OK=$(awk '/^## Scenario [1-4]:/{
  n = substr($3, 1, length($3)-1) + 0
  if (n != prev + 1) { print "FAIL"; exit }
  prev = n
} END { print "OK" }' "$F")
[ "$ORDER_OK" = "OK" ] || { echo "FAIL: scenarios not in 1-4 order (D-01 violation)"; exit 1; }

# Probe 7: Each scenario has all 6 section labels
PROMPT=$(grep -cE '^\*\*Prompt:\*\*' "$F")
PHASE=$(grep -cE '^\*\*Testing phase identified:\*\*' "$F")
ROUTE=$(grep -cE '^\*\*Signal route:\*\*' "$F")
DUAL=$(grep -cE '^\*\*Dual-load:\*\*' "$F")
BC=$(grep -cE '^\*\*Boundary conditions surfaced:\*\*' "$F")
NEXT=$(grep -cE '^\*\*Next-test recommendation:\*\*' "$F")
[ "$PROMPT" = "4" ] || { echo "FAIL: expected 4 Prompt labels, got $PROMPT"; exit 1; }
[ "$PHASE" = "4" ] || { echo "FAIL: expected 4 Testing phase labels, got $PHASE"; exit 1; }
[ "$ROUTE" = "4" ] || { echo "FAIL: expected 4 Signal route labels, got $ROUTE"; exit 1; }
[ "$DUAL" = "4" ] || { echo "FAIL: expected 4 Dual-load labels, got $DUAL"; exit 1; }
[ "$BC" = "4" ] || { echo "FAIL: expected 4 Boundary conditions labels, got $BC"; exit 1; }
[ "$NEXT" = "4" ] || { echo "FAIL: expected 4 Next-test recommendation labels, got $NEXT"; exit 1; }

# Probe 8: Plugin/skill names byte-exact against marketplace.json (focused on key destinations)
for SKILL in '401-403-bypass-techniques' 'jwt-oauth-token-attacks' 'graphql-and-hidden-parameters' 'insecure-source-code-management' 'business-logic-vulnerabilities' 'race-condition'; do
  grep -q "\`$SKILL\`" "$F" || { echo "FAIL: missing reference to deep skill '$SKILL'"; exit 1; }
  jq -r '.plugins[].skills[]?' "$REPO/.claude-plugin/marketplace.json" | sed 's|^\./||' | grep -qxF "$SKILL" \
    || { echo "FAIL: '$SKILL' not in marketplace.json"; exit 1; }
done
for PLUGIN in 'hack-skills-auth-bypass' 'hack-skills-recon' 'hack-skills-web-client-attacks'; do
  grep -q "\`$PLUGIN\`" "$F" || { echo "FAIL: missing reference to plugin '$PLUGIN'"; exit 1; }
  jq -r '.plugins[].name' "$REPO/.claude-plugin/marketplace.json" | grep -qxF "$PLUGIN" \
    || { echo "FAIL: '$PLUGIN' not in marketplace.json"; exit 1; }
done

# Probe 9: Boundary-condition references use 'intuition #N' notation
INTUITION_REFS=$(grep -cE 'intuition #[1-8]' "$F")
[ "$INTUITION_REFS" -ge 4 ] || { echo "FAIL: expected at least 4 'intuition #N' references (one per scenario), got $INTUITION_REFS"; exit 1; }

# Probe 10: No clickable markdown link to sibling sub-files
! grep -E '\]\((\.\.?/)?(patterns/routing-tables\.md|patterns/expert-intuitions\.md)' "$F" \
  || { echo "FAIL: clickable link to sibling sub-file detected (Pitfall 5)"; exit 1; }

# Probe 11: Each scenario contains an install command in canonical shape (no @branch)
INSTALL_COUNT=$(grep -cE '/plugin install hack-skills-[a-z-]+@hack-skills-marketplace' "$F")
[ "$INSTALL_COUNT" -ge 4 ] || { echo "FAIL: expected at least 4 install commands (one per scenario), got $INSTALL_COUNT"; exit 1; }
! grep -E '/plugin install hack-skills-[a-z-]+@hack-skills-marketplace@' "$F" || { echo "FAIL: @branch suffix detected"; exit 1; }

# Probe 12: English only — no CJK characters
! perl -ne 'exit 1 if /\p{Han}/' "$F" 2>/dev/null && true || { echo "FAIL: CJK character detected"; exit 1; }

# Probe 13: Reasonable file length (80-180 lines — D-02 target ~100-120)
LINES=$(wc -l < "$F")
[ "$LINES" -ge 80 ] && [ "$LINES" -le 180 ] || { echo "FAIL: file has $LINES lines (expected 80-180)"; exit 1; }

echo "PASS: all 13 probes succeeded"
    </automated>
  </verify>
  <done>
    All 13 verification probes return PASS:
    1. File exists at `plugins/hack-skills-router/skills/hack-skills-router/examples/workflow-walkthroughs.md`.
    2. H1 title `# Workflow Walkthroughs` present.
    3. Attribution header cites yaklang/hack-skills and MIT.
    4. Exactly 4 `## Scenario N:` headings.
    5. Each scenario title matches the D-01 locked content (admin/JWT, GraphQL/introspection, .env, coupon/checkout).
    6. Scenarios in numeric order 1, 2, 3, 4.
    7. Each scenario has all 6 section labels (Prompt, Testing phase identified, Signal route, Dual-load, Boundary conditions surfaced, Next-test recommendation) — 24 total label occurrences.
    8. All 6 key deep-skill names + 3 key plugin names present in backticks and byte-exact against marketplace.json.
    9. At least 4 `intuition #N` notation references (one per scenario minimum).
    10. No clickable links to `routing-tables.md` or `expert-intuitions.md`.
    11. At least 4 install commands in canonical shape, zero `@branch` suffixes.
    12. No Chinese characters.
    13. File length 80-180 lines.
  </done>
</task>

</tasks>

<acceptance_criteria>
- `plugins/hack-skills-router/skills/hack-skills-router/examples/workflow-walkthroughs.md` exists.
- File opens with `# Workflow Walkthroughs` H1 + blockquote attribution paragraph mentioning yaklang/hack-skills (MIT).
- Exactly 4 H2 scenarios in locked D-01 order.
- Each scenario has all 6 section labels in the prescribed order (24 total label occurrences).
- All 6 key deep-skill names + 3 key plugin names byte-exact against marketplace.json.
- At least 4 `intuition #N` prose references (one per scenario minimum).
- At least 4 install commands in canonical shape (no `@branch` suffix).
- Zero clickable markdown links to `routing-tables.md` or `expert-intuitions.md`.
- No Chinese characters.
- File-level attribution only.
- File length 80-180 lines (D-02 target ~100-120).
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream paraphrase → router file | Walkthrough methodology paraphrased from yaklang/hack-skills (MIT); license/attribution boundary |
| Router → marketplace install | Walkthroughs surface install commands; wrong plugin name = silent UX failure |
| Multi-hop link chain | Cross-references to sibling sub-files must be prose-only, not clickable links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering (drift) | Scenario destination plugin/skill names | mitigate | Probe 8 cross-references 6 deep-skill + 3 plugin name references against marketplace.json — any drift fails the task |
| T-02-10 | Information Disclosure (license) | Paraphrased upstream methodology | mitigate | Header attribution paragraph cites yaklang/hack-skills + MIT — probe 3 enforces presence |
| T-02-11 | Tampering (install command shape) | Per-scenario install commands | mitigate | Probe 11 rejects `@branch` suffix (CLAUDE.md constraint) |
| T-02-12 | Tampering (multi-hop link) | Cross-references to sibling sub-files | mitigate | Probe 10 rejects clickable markdown links to `routing-tables.md` / `expert-intuitions.md` (Pitfall 5) |
| T-02-13 | Information Disclosure (Chinese content carry-over) | Paraphrased content from bilingual upstream | mitigate | Probe 12 fails on any CJK character (English-only constraint) |
</threat_model>

<verification>
- Wave gate: This is a Wave 1 plan with `depends_on: []` — no upstream plan dependencies (skill/plugin name verification grounds out at marketplace.json, not at the other Wave 1 plans).
- Per-task automated verify: see Task 1 `<verify>` block — 13 probes covering file existence, H1, attribution, scenario count + order + titles, 6-label structure per scenario (24 total), byte-exact plugin/skill name matches against marketplace.json for 6 skills + 3 plugins, intuition #N notation usage, Pitfall 5 sub-file linking, canonical install command shape, CJK absence, line-count sanity.
- Cross-file consistency (run after all Wave 1 plans complete): the deep-skill names + plugin names referenced here must also appear in `routing-tables.md` (Plan 02-01) section headings — this is implicitly satisfied because both files ground out at marketplace.json.
</verification>

<success_criteria>
- File exists at the canonical sidecar path.
- All 13 Task 1 verification probes pass.
- 4 worked traces in locked D-01 order, each with 6-label medium-structured format.
- Zero spelling drift against marketplace.json.
- Boundary-condition references use `intuition #N` notation (no clickable links).
- File-level attribution to yaklang/hack-skills (MIT).
- English only.
</success_criteria>

<output>
Create `.planning/phases/02-router-skill-content/02-03-SUMMARY.md` when done.
</output>
