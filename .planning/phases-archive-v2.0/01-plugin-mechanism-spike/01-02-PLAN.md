---
phase: 01-plugin-mechanism-spike
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/marketplace.json
autonomous: true
requirements:
  - PLUGIN-01
  - PLUGIN-02
user_setup: []

must_haves:
  truths:
    - "marketplace.json `.plugins` array has length 14 (was 13)"
    - "The 14th entry has name=hack-skills-router and source=./plugins/hack-skills-router (relative-path string form, not git-subdir object)"
    - "All 13 pre-existing v1 entries remain present and byte-identical (no v1 entry removed or altered)"
    - "JSON is syntactically valid (jq empty exits 0)"
    - "The new entry's `source` field is a STRING (not an object) and begins with `./`"
  artifacts:
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace catalog with 14 plugin entries (13 v1 + 1 new sidecar router)"
      contains: 'hack-skills-router'
  key_links:
    - from: ".claude-plugin/marketplace.json"
      to: "plugins/hack-skills-router/"
      via: 'source field: "./plugins/hack-skills-router"'
      pattern: '"source"\s*:\s*"\./plugins/hack-skills-router"'
    - from: ".claude-plugin/marketplace.json"
      to: "plugins/hack-skills-router/.claude-plugin/plugin.json"
      via: 'plugin install resolves source → reads plugin.json'
      pattern: 'plugin\.json'
---

<objective>
Append a single new plugin entry to `.claude-plugin/marketplace.json` that registers `hack-skills-router` as the 14th plugin in the marketplace. The new entry uses the relative-path `source` string form (NOT the `git-subdir` object form that the 13 v1 entries use) so that `/plugin install hack-skills-router@hack-skills-marketplace` resolves to the in-repo `plugins/hack-skills-router/` directory created by Plan 01.

Purpose: This plan is the wire-up between Plan 01's new plugin tree and the marketplace catalog. Plan 01 creates the plugin files; this plan tells the marketplace they exist. Together they enable Plan 03's install verification.

Output:
- `.claude-plugin/marketplace.json` modified: `.plugins` array goes from length 13 to length 14. All 13 v1 entries remain byte-identical. The 14th entry is the new `hack-skills-router` registration.

STUB-vs-final boundary: The marketplace entry is FINAL shape in Phase 1 — there is no Phase 2 or Phase 3 follow-up that modifies this entry's structure (Phase 2 modifies SKILL.md content; Phase 3 modifies hook script content; the marketplace entry stays as-written here).

Pitfall 4 decision (RESEARCH Open Question 1): we OMIT the `version` field from the marketplace entry. The design spec §5.1 includes it, but RESEARCH §"Common Pitfalls" §Pitfall 4 + RESEARCH §"Open Questions" §1 explicitly recommend dropping it from the marketplace entry to keep `plugin.json` as the single source of truth for version. This avoids the silent-wins maintenance trap where bumping marketplace version but not plugin.json version causes users to miss updates. The four canonical fields in the new entry are: `name`, `source`, `description`, `keywords`.
</objective>

<execution_context>
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/spencerpresley/code/hack-skills-wip/hack-skills-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-plugin-mechanism-spike/01-RESEARCH.md
@.planning/phases/01-plugin-mechanism-spike/01-PATTERNS.md
@.planning/specs/2026-05-22-v2-router-design.md

<interfaces>
<!-- The current marketplace.json is the in-repo schema ground truth. -->
<!-- This plan READS the existing file before modifying it (the Edit tool will require that). -->
<!-- The shape of any of the 13 v1 entries (e.g., lines 8-26 — hack-skills-active-directory-and-windows) -->
<!-- is the REFERENCE for the root-array shape, but the new entry uses a DIFFERENT source form per spec §5.1. -->

Existing root structure (lines 1-7 — must remain intact):
  Top-level keys: name="hack-skills-marketplace", owner.name="Spencer Presley", description, plugins (array).

13 existing v1 entries: lines 8-258 (each entry has source as a git-subdir OBJECT form, with strict:false and a skills array).

NEW entry shape (per design spec §5.1, with Pitfall 4 fix — drop `version`):
  Four keys: name (string), source (string starting with "./"), description (string), keywords (array of strings).
  NO `version` field (Pitfall 4 — plugin.json owns versioning).
  NO `strict` field (only relevant for upstream-curating plugins with a skills array).
  NO `skills` array (irrelevant — the plugin's own structure under plugins/hack-skills-router/ is the source of truth for its components).
  Source value must be the EXACT string "./plugins/hack-skills-router" (path resolves relative to marketplace ROOT = repo root, NOT to .claude-plugin/, per RESEARCH §"Pattern 1" + Pitfall 4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the hack-skills-router entry to .claude-plugin/marketplace.json</name>
  <files>.claude-plugin/marketplace.json</files>
  <read_first>
    - .claude-plugin/marketplace.json (the file being modified — must be read first; pay attention to the closing `}` of the 13th entry around line 257-258 and the closing `]` on line 259; the new entry will be inserted between those)
    - .planning/phases/01-plugin-mechanism-spike/01-PATTERNS.md §"1. .claude-plugin/marketplace.json (MODIFY — append 14th entry)" (canonical pattern with exact text to append AND critical constraints — append-only, preserve all 13 v1 entries byte-identical)
    - .planning/phases/01-plugin-mechanism-spike/01-RESEARCH.md §"Pattern 1: Marketplace entry with relative-path source" + §"Code Examples" (cited docs for relative-path semantics — path resolves to marketplace root, must start with `./`, no `../`)
    - .planning/phases/01-plugin-mechanism-spike/01-RESEARCH.md §"Common Pitfalls" §Pitfall 4 (version-in-both-places trap — we drop version from THIS entry; keep it only in plugin.json from Plan 01)
    - .planning/specs/2026-05-22-v2-router-design.md §5.1 (canonical 5-field shape — we use 4 of those 5, omitting `version` per Pitfall 4)
    - .planning/ROADMAP.md §"Phase 1: Plugin Mechanism Spike" Success Criteria #1 (the requirement: "alongside the existing 13 v1 topical plugin entries — no v1 entries removed or altered")
  </read_first>
  <action>
    Read .claude-plugin/marketplace.json. Append exactly ONE new object as the 14th (last) element of the `.plugins` array. The new object has exactly four keys, in this order (matches design spec §5.1 minus `version`):
      1. `name`: the string `hack-skills-router`
      2. `source`: the string `./plugins/hack-skills-router` (relative-path string form per RESEARCH §Pattern 1 — starts with `./`, resolves to repo root)
      3. `description`: the string `Routing + scaffolding for hack-skills topical plugins. Adapted from yaklang/hack-skills upstream router.` (from design spec §5.1)
      4. `keywords`: the JSON array `["security", "pentest", "router", "hooks", "methodology"]` (from design spec §5.1)

    Constraints:
    - Use the Edit tool. Read the file first.
    - The 13 existing v1 entries (lines 8 through 258 roughly) MUST remain BYTE-IDENTICAL — same indentation, same key order, same trailing whitespace. The append is a strict addition.
    - The top-level keys `name`, `owner`, `description`, `plugins` and their order must remain intact.
    - Mechanically: add a comma after the closing `}` of the 13th entry (currently the `hack-skills-web-protocol-attacks` entry ending around line 257), then insert the new object before the closing `]` on line 259. Match the indentation style of the existing entries (4-space or 2-space — whatever the file already uses; the existing file uses 2-space indentation per the read).
    - DO NOT add a `version` field to the new entry (Pitfall 4 — keep version in plugin.json only).
    - DO NOT add a `strict` field (irrelevant for relative-path source).
    - DO NOT add a `skills` array (irrelevant for relative-path source — the plugin's own structure is the source of truth).
    - DO NOT modify any of the 13 existing entries' `name`, `source`, `strict`, `description`, or `skills` fields.

    After the edit, the file must validate as JSON (jq empty exits 0) and the `.plugins` array length must be 14.
  </action>
  <verify>
    <automated>jq -e '.plugins | length == 14' .claude-plugin/marketplace.json && jq -e '.plugins[] | select(.name == "hack-skills-router") | .source == "./plugins/hack-skills-router" and (.keywords | length == 5) and (has("version") | not) and (has("strict") | not) and (has("skills") | not)' .claude-plugin/marketplace.json</automated>
  </verify>
  <acceptance_criteria>
    - `jq empty .claude-plugin/marketplace.json` exits 0 (valid JSON)
    - `jq '.plugins | length' .claude-plugin/marketplace.json` outputs exactly `14`
    - `jq -e '.plugins[] | select(.name == "hack-skills-router")' .claude-plugin/marketplace.json` is non-empty (the new entry exists)
    - `jq -r '.plugins[] | select(.name == "hack-skills-router") | .source' .claude-plugin/marketplace.json` outputs exactly: `./plugins/hack-skills-router`
    - `jq -r '.plugins[] | select(.name == "hack-skills-router") | .source | type' .claude-plugin/marketplace.json` outputs exactly: `string` (NOT object — relative-path form is string, not git-subdir)
    - `jq -e '.plugins[] | select(.name == "hack-skills-router") | has("version") | not' .claude-plugin/marketplace.json` is true (Pitfall 4 — no `version` in marketplace entry)
    - `jq -e '.plugins[] | select(.name == "hack-skills-router") | has("strict") | not' .claude-plugin/marketplace.json` is true
    - `jq -e '.plugins[] | select(.name == "hack-skills-router") | has("skills") | not' .claude-plugin/marketplace.json` is true
    - `jq -r '.plugins[] | select(.name == "hack-skills-router") | .description' .claude-plugin/marketplace.json` outputs the description text starting with `Routing + scaffolding`
    - `jq -e '.plugins[] | select(.name == "hack-skills-router") | .keywords == ["security", "pentest", "router", "hooks", "methodology"]' .claude-plugin/marketplace.json` is true
    - All 13 v1 entries still present — `jq '[.plugins[] | select(.name | startswith("hack-skills-") and . != "hack-skills-router")] | length' .claude-plugin/marketplace.json` outputs exactly `13`
    - Specifically verify the first v1 entry is byte-untouched — `jq -e '.plugins[0].name == "hack-skills-active-directory-and-windows" and (.plugins[0].source.source == "git-subdir") and (.plugins[0].skills | length == 7)' .claude-plugin/marketplace.json` is true
    - Top-level keys preserved — `jq -r 'keys | sort | @csv' .claude-plugin/marketplace.json` outputs exactly: `"description","name","owner","plugins"` (no new top-level keys)
    - Git diff sanity (informational, no hard gate): `git diff --stat .claude-plugin/marketplace.json` shows ONE file changed, with insertions only (no deletions of v1 entries)
  </acceptance_criteria>
  <done>marketplace.json contains 14 plugin entries — the original 13 v1 entries byte-identical plus the new hack-skills-router entry with the four-field shape (name, source, description, keywords), valid JSON, source pointing at the relative path Plan 01 creates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Marketplace catalog (read by CC at install) | The marketplace.json is the contract between this repo and the Claude Code CLI. Any malformed entry breaks install for the whole marketplace, including all 13 v1 entries. |
| Source path resolution (`./plugins/hack-skills-router`) | Resolved relative to marketplace root at install time. A future fork or branch switch could change what tree this path points at. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | marketplace.json (the 13 v1 entries) | mitigate | Acceptance criteria explicitly verify all 13 v1 entries still present, with the first entry checked byte-level (its `source.source == "git-subdir"`, its `skills` array length = 7). The append-only constraint is enforced. Any structural change to a v1 entry would fail the JQ assertion. |
| T-02-02 | Denial of Service | marketplace catalog | mitigate | A malformed JSON edit would break `/plugin install` for ALL 14 entries (one bad apple = whole catalog rejected). Mitigation: `jq empty` is in the acceptance criteria as a hard gate. Phase 1 install in Plan 03 would surface the issue immediately. |
| T-02-03 | Tampering | Relative-path source | accept | `source: "./plugins/hack-skills-router"` is in-repo; whoever owns the marketplace ref owns what gets installed. This is the standard Git trust model — accepted by design (PROJECT.md Core Value depends on the user being able to publish their own marketplace). Plan 03's UAT step uses the LOCAL marketplace (re-registered via `claude plugin marketplace add ./` per RESEARCH §"Runtime State Inventory") so trust boundary is the user's own working tree, not any remote ref. |
| T-02-04 | Information Disclosure | marketplace.json (public) | accept | This file is checked into the public repo. No secrets in the new entry — only plugin metadata (name, description, keywords). Accepted. |
| T-02-SC | Tampering | Package supply chain | n/a | Zero npm/pip/cargo dependencies in this plan. Edit is in-tree only. |
</threat_model>

<verification>
Plan-level checks (after Task 1 completes):

1. JSON validity:
   ```
   jq empty .claude-plugin/marketplace.json && echo "JSON OK"
   ```
2. Plugin count went from 13 → 14:
   ```
   jq '.plugins | length' .claude-plugin/marketplace.json
   ```
3. New entry present with correct shape:
   ```
   jq '.plugins[] | select(.name == "hack-skills-router")' .claude-plugin/marketplace.json
   ```
4. v1 entries byte-identical sanity (no deletions, only insertion):
   ```
   git diff --stat .claude-plugin/marketplace.json
   git diff --no-color .claude-plugin/marketplace.json | grep -c '^-' # should equal 0 or 1 (the closing `]` line being moved doesn't count as deletion in most diffs)
   ```
5. Plan 01 directory exists (sanity — this plan's source target):
   ```
   test -d plugins/hack-skills-router || echo "WARN: Plan 01 has not yet created the plugin tree — install in Plan 03 will fail"
   ```
   (This is informational only — Plan 01 and Plan 02 run in parallel; the install in Plan 03 is the integration point. If Plan 01 hasn't completed when this verification runs, the warning is expected.)
</verification>

<success_criteria>
This plan succeeds when:
- `.claude-plugin/marketplace.json` is valid JSON with `.plugins | length == 14`.
- The 14th entry has the four required fields (name, source, description, keywords) in the design-spec values and NONE of the forbidden-for-this-form fields (version, strict, skills).
- All 13 v1 entries are still present and byte-identical to their pre-edit state.
- No file other than `.claude-plugin/marketplace.json` is modified by this plan.

Goal-backward: Plan 03 will run `/plugin install hack-skills-router@hack-skills-marketplace` and expect the install to resolve to Plan 01's directory. The install resolution path is: (a) CLI reads this marketplace.json, (b) finds entry with name=hack-skills-router, (c) sees source=./plugins/hack-skills-router, (d) reads `plugins/hack-skills-router/.claude-plugin/plugin.json`. This plan owns step (a)→(c); Plan 01 owns step (d).
</success_criteria>

<output>
Write a SUMMARY at `.planning/phases/01-plugin-mechanism-spike/01-02-SUMMARY.md` covering:
- The diff summary (line count of insertions, confirmation that the existing 13 entries are untouched)
- The 14th entry's final shape (jq -c output of just that entry)
- The Pitfall 4 decision applied: `version` field NOT included in the marketplace entry (kept in plugin.json only)
- Handoff to Plan 03: the install command and the expected `claude plugin details` shape.
</output>
